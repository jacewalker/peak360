---
phase: 260512-ru4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/Sidebar.tsx
  - src/components/layout/Header.tsx
autonomous: false
requirements:
  - RU4-01
must_haves:
  truths:
    - "Portal sidebar logo displays the new landing-brand mark (peak360-logo.png), not /logo.png"
    - "Legacy assessment Header logo displays the new landing-brand mark, not /logo.png"
    - "Neither logo appears horizontally or vertically squished — aspect ratio ≈ 3.35:1 is preserved"
    - "All other layout, classNames, links, and surrounding text are unchanged"
  artifacts:
    - path: "src/components/layout/Sidebar.tsx"
      provides: "Sidebar logo Image with src='/landing/peak360-logo.png' and width/height matching ~3.35:1"
      contains: "/landing/peak360-logo.png"
    - path: "src/components/layout/Header.tsx"
      provides: "Header logo Image with src='/landing/peak360-logo.png' and width/height matching ~3.35:1"
      contains: "/landing/peak360-logo.png"
  key_links:
    - from: "src/components/layout/Sidebar.tsx"
      to: "public/landing/peak360-logo.png"
      via: "next/image src prop"
      pattern: "src=\"/landing/peak360-logo.png\""
    - from: "src/components/layout/Header.tsx"
      to: "public/landing/peak360-logo.png"
      via: "next/image src prop"
      pattern: "src=\"/landing/peak360-logo.png\""
---

<objective>
Swap the portal logo asset in both the Sidebar (portal layout) and the legacy assessment Header from `/logo.png` (old square-ish mark, 1024×683 ≈ 1.5:1) to `/landing/peak360-logo.png` (wider landing brand mark, 1230×367 ≈ 3.35:1).

Purpose: Visually align the portal and legacy assessment surfaces with the landing-page brand mark so the brand reads consistently across all entry points.

Output: Two component files updated — `src` prop pointing at the new asset, and `width`/`height` props adjusted so the wider aspect ratio renders without distortion. All other markup (classNames, links, surrounding text, layout) is preserved verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@./CLAUDE.md
@src/components/layout/Sidebar.tsx
@src/components/layout/Header.tsx

<interfaces>
<!-- Current logo usage extracted from the two files. Keep className, alt, Link, and surrounding markup identical. Only `src` and `width`/`height` change. -->

Sidebar.tsx (around lines 118–125), inside `<Link href="/portal" className="flex items-center gap-3 group">`:
- src="/logo.png"
- alt="Peak360 Logo"
- width={40}
- height={28}
- className="group-hover:scale-105 transition-transform"

Header.tsx (around lines 28–35), inside `<Link href="/portal" className="flex items-center gap-3 group">`:
- src="/logo.png"
- alt="Peak360 Logo"
- width={48}
- height={32}
- className="group-hover:scale-105 transition-transform"

Asset facts:
- Old: /logo.png — 1024×683 (aspect ≈ 1.50:1, near-square)
- New: /landing/peak360-logo.png — 1230×367 (aspect ≈ 3.35:1, wide horizontal wordmark)

Aspect-ratio math for new asset (preserve the existing visual height each surface uses):
- Sidebar previously rendered at 40w × 28h (height ≈ 28px). At 3.35:1, width for height=28 → round(28 × 3.35) = 94. Use width={94} height={28}.
- Header previously rendered at 48w × 32h (height ≈ 32px). At 3.35:1, width for height=32 → round(32 × 3.35) = 107. Use width={107} height={32}.

These widths keep the logo at the same rendered height as before (so vertical rhythm in the sidebar header block and the 14h header bar is unchanged), and expand horizontally to honor the new aspect ratio without squishing. The surrounding `flex items-center gap-3` Link layout will naturally accommodate the wider mark.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap Sidebar logo src and resize for new aspect ratio</name>
  <files>src/components/layout/Sidebar.tsx</files>
  <action>
    In `src/components/layout/Sidebar.tsx`, locate the `<Image>` element inside the portal-logo `<Link href="/portal" ...>` block (around lines 119–125). Make exactly these three prop changes on that single `<Image>` element, leaving every other attribute (alt, className, the wrapping Link, the sibling `<div>` with "PEAK360" wordmark text, MonoEyebrow, padding, and all surrounding markup) byte-identical:

    1. Change `src="/logo.png"` → `src="/landing/peak360-logo.png"`.
    2. Change `width={40}` → `width={94}`.
    3. Change `height={28}` → `height={28}` (unchanged value, but verify it stays 28).

    Width 94 with height 28 matches the new asset's 1230×367 ≈ 3.35:1 aspect ratio (28 × 3.35 ≈ 94), preserving the previous rendered height so the sidebar's vertical rhythm is unaffected.

    Do NOT modify: the `MonoEyebrow` line, the `<h1>PEAK<span>360</span></h1>` wordmark text, the "Longevity" caption, the `px-5 pt-6 pb-8` wrapper padding, `className="group-hover:scale-105 transition-transform"` on the Image, or the `flex items-center gap-3 group` classes on the Link. This is a pure asset+dimensions swap.
  </action>
  <verify>
    <automated>grep -n 'landing/peak360-logo.png' src/components/layout/Sidebar.tsx | grep -c 'landing/peak360-logo.png' | grep -q '^1$' && grep -n 'width={94}' src/components/layout/Sidebar.tsx | grep -q 'width={94}' && ! grep -q '"/logo.png"' src/components/layout/Sidebar.tsx && echo OK</automated>
  </verify>
  <done>
    Sidebar.tsx imports the new logo at `/landing/peak360-logo.png` with `width={94} height={28}`; the old `/logo.png` reference is gone from Sidebar.tsx; all other markup, classes, and surrounding components in the file are unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Swap Header logo src and resize for new aspect ratio</name>
  <files>src/components/layout/Header.tsx</files>
  <action>
    In `src/components/layout/Header.tsx`, locate the `<Image>` element inside the `<Link href="/portal" ...>` block (around lines 28–35). Make exactly these three prop changes on that single `<Image>` element, leaving every other attribute (alt, className, the wrapping Link, sibling wordmark `<div>`, header `<header>` element, max-w container, logout button, MonoEyebrow CLIENT eyebrow logic) byte-identical:

    1. Change `src="/logo.png"` → `src="/landing/peak360-logo.png"`.
    2. Change `width={48}` → `width={107}`.
    3. Change `height={32}` → `height={32}` (unchanged value, but verify it stays 32).

    Width 107 with height 32 matches the new asset's 1230×367 ≈ 3.35:1 aspect ratio (32 × 3.35 ≈ 107), preserving the previous rendered height inside the `h-14` header so vertical alignment with the wordmark and logout button is unaffected.

    Do NOT modify: the `<h1>PEAK<span>360</span></h1>` wordmark text, the "Longevity Assessment" caption, the `<header className="bg-bg-2 border-b border-line text-text h-14">` shell, the CLIENT eyebrow conditional block, the logout button, or any classNames. This is a pure asset+dimensions swap.
  </action>
  <verify>
    <automated>grep -n 'landing/peak360-logo.png' src/components/layout/Header.tsx | grep -c 'landing/peak360-logo.png' | grep -q '^1$' && grep -n 'width={107}' src/components/layout/Header.tsx | grep -q 'width={107}' && ! grep -q '"/logo.png"' src/components/layout/Header.tsx && echo OK</automated>
  </verify>
  <done>
    Header.tsx imports the new logo at `/landing/peak360-logo.png` with `width={107} height={32}`; the old `/logo.png` reference is gone from Header.tsx; all other markup, classes, conditional rendering, and surrounding components in the file are unchanged.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification — sidebar and assessment header show the new mark, undistorted</name>
  <what-built>
    Both the portal Sidebar (top-left mark) and the legacy assessment Header (top-left mark) now reference `/landing/peak360-logo.png` with width/height props sized to the new ~3.35:1 aspect ratio. No other markup changed.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000/portal (or any portal route that renders the Sidebar — e.g. `/portal/clients`). Visually confirm the top-left of the sidebar shows the new wide landing-brand mark, not the old square-ish logo. The mark should sit to the left of the "PEAK360 / Longevity" wordmark with the existing `gap-3` spacing and should not look horizontally squashed or vertically stretched.
    2. Open any in-progress assessment URL that renders the legacy `Header` component, e.g. http://localhost:3000/assessment/{any-existing-id}/section/1. Visually confirm the top-left of the header shows the same new wide brand mark inside the `h-14` header bar, aligned with the wordmark and logout button. It should not be cropped, squished, or overflowing the header height.
    3. Confirm the rendered mark is crisp (not blurry from upscaling) at both sizes.
    4. Confirm no console errors about missing image `/landing/peak360-logo.png` (next/image will warn loudly if the asset path is wrong).
    5. If either render looks squished, note which surface and the apparent ratio so the width can be re-tuned (the math assumes 1230×367; if the actual decoded dimensions differ, recompute width = round(height × actual_aspect)).
  </how-to-verify>
  <resume-signal>Type "approved" if both surfaces show the new mark cleanly; otherwise describe which surface looks wrong and how (squished horizontally / vertically / cropped / wrong asset).</resume-signal>
</task>

</tasks>

<verification>
- `grep -R '"/logo.png"' src/components/layout/` returns no matches (old asset path fully removed from layout components).
- `grep -R 'landing/peak360-logo.png' src/components/layout/` returns exactly two matches (Sidebar.tsx and Header.tsx).
- `npm run build` succeeds with no new TypeScript or next/image warnings about the swapped Image components.
- Visual verification (Task 3) confirms no distortion on either surface.
</verification>

<success_criteria>
- Sidebar.tsx and Header.tsx both reference `/landing/peak360-logo.png` exactly once each.
- Width/height props produce a ~3.35:1 box on both surfaces (94×28 in Sidebar, 107×32 in Header).
- Every other line, className, and child element in both files is unchanged from the pre-edit state.
- `/logo.png` is no longer referenced from `src/components/layout/`.
- Human verification confirms the new mark renders undistorted in both the portal sidebar and the legacy assessment header.
</success_criteria>

<output>
After completion, create `.planning/quick/260512-ru4-swap-portal-logo-to-landing-brand-mark/260512-ru4-SUMMARY.md` summarizing the asset swap and any width/height tuning applied during human verification.
</output>
