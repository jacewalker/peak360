---
phase: 11-report-marker-detail-coach-insights
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/app/portal/admin/marker-content/page.tsx
  - src/app/portal/admin/marker-content/[marker]/page.tsx
  - src/app/portal/admin/page.tsx
autonomous: false
requirements: [D-07, D-10, D-14]
must_haves:
  truths:
    - "An admin sees a 'Marker Content' card on the admin index and can navigate to the marker-content list"
    - "The marker-content list shows every REPORT_MARKERS marker grouped by category, each linking to its editor"
    - "A non-admin hitting either marker-content page is redirected away (server-side gate)"
    - "The editor loads existing content, lets the admin edit Definition + Impact + a 5-tier x Male/Female coach-insight matrix, and saves via optimistic-concurrency PUT"
    - "Navigating away with unsaved edits triggers a beforeunload guard"
    - "A 409 conflict on save surfaces a clear 'updated by another admin' message"
  artifacts:
    - path: "src/app/portal/admin/marker-content/page.tsx"
      provides: "SSR admin-gated category-grouped marker list linking to editors"
      contains: "REPORT_CATEGORIES"
    - path: "src/app/portal/admin/marker-content/[marker]/page.tsx"
      provides: "Client editor: definition + impact textareas, coach-insight matrix (5 tiers x male/female), optimistic PUT, beforeunload guard, tone guidance"
      contains: "beforeunload"
    - path: "src/app/portal/admin/page.tsx"
      provides: "Marker Content nav card in ADMIN_SECTIONS"
      contains: "marker-content"
  key_links:
    - from: "src/app/portal/admin/marker-content/[marker]/page.tsx"
      to: "/api/admin/marker-content/[marker]"
      via: "fetch GET on mount + PUT on save with updatedAt"
      pattern: "/api/admin/marker-content/"
    - from: "src/app/portal/admin/marker-content/page.tsx"
      to: "src/app/portal/admin/marker-content/[marker]/page.tsx"
      via: "next/link href per marker testKey"
      pattern: "/portal/admin/marker-content/"
    - from: "src/app/portal/admin/page.tsx"
      to: "/portal/admin/marker-content"
      via: "ADMIN_SECTIONS card href"
      pattern: "/portal/admin/marker-content"
---

<objective>
Build the admin authoring UI for marker content: a category-grouped marker list, a per-marker editor (Definition + Impact + the 5-tier x Male/Female coach-insight matrix) cloned from the normative editor, and a nav card on the admin index.

Purpose: Admins author and refine the seeded draft content that the report surfaces. The editor must clone the proven normative-editor concurrency + dirty-guard behaviour so concurrent edits are safe.
Output: Two new portal pages + one ADMIN_SECTIONS entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-report-marker-detail-coach-insights/11-CONTEXT.md
@.planning/phases/11-report-marker-detail-coach-insights/11-PATTERNS.md

<interfaces>
<!-- API contracts from Plan 11-02 (merged before this wave runs in parallel; both depend only on 11-01 files, no shared files between 02 and 03): -->
GET  /api/admin/marker-content/[marker] → { success, data: { testKey, definition, impact, coachInsights, updatedBy, updatedAt } }
PUT  /api/admin/marker-content/[marker] body { definition, impact, coachInsights, updatedAt } → 200 { success, data:{updated:1} } | 409 { success:false, error }

RatingTier = 'poor'|'cautious'|'normal'|'great'|'elite'; TIER_LABELS (src/types/normative.ts): Attention/Cautious/Normal/Optimal/Peak
coachInsights shape: Record<RatingTier, { male: string|null; female: string|null }>
REPORT_MARKERS, REPORT_CATEGORIES (src/lib/report-markers.ts)
</interfaces>

NOTE on wave parallelism: Plan 11-02 (API routes) and this plan (UI pages) run in the same wave and share NO files. The editor fetches the API routes at runtime; the pages compile against the route paths as strings (no compile-time import of the route modules), so there is no build-time dependency on 11-02.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin marker-content list page + admin index nav card</name>
  <files>src/app/portal/admin/marker-content/page.tsx, src/app/portal/admin/page.tsx</files>
  <read_first>
    - src/app/portal/admin/pillars/page.tsx (SSR admin gate: auth.api.getSession → role !== 'admin' redirect — clone lines ~1-23)
    - src/app/portal/admin/normative/page.tsx (category-grouped marker list, status pills, dark-portal styling)
    - src/app/portal/admin/page.tsx (ADMIN_SECTIONS array — add a card following the existing object shape: label/href/description/stat/icon)
    - src/lib/report-markers.ts (REPORT_MARKERS, REPORT_CATEGORIES for grouping)
    - .planning/phases/11-report-marker-detail-coach-insights/11-PATTERNS.md (section: portal/admin/marker-content/page.tsx)
  </read_first>
  <action>
    Create `src/app/portal/admin/marker-content/page.tsx` as a server component using the SSR admin gate from `pillars/page.tsx` (`auth.api.getSession({ headers: await headers() })`; redirect `/login` if no user; cast to `AuthSession`; redirect `/portal` if `role !== 'admin'`). Render a category-grouped list: iterate `REPORT_CATEGORIES`, within each, filter `REPORT_MARKERS` by category and render each as a `next/link` to `/portal/admin/marker-content/${m.testKey}` showing `m.label`. Use dark-portal tokens (MonoEyebrow hero like the admin index, `bg-bg-3`, `border-line`, `font-mono` category eyebrows, `text-text`/`text-text-dim`). Optionally fetch the admin list (`/api/admin/marker-content`) server-side or via the queries layer to show an "Authored" vs "Draft" status pill per marker (use authoredKeys); if simpler, omit pills this pass — the link list is the required deliverable.

    In `src/app/portal/admin/page.tsx`, add a new object to `ADMIN_SECTIONS` for Marker Content: `label: 'Marker Content'`, `href: '/portal/admin/marker-content'`, a description (definitions, impact, and per-tier per-gender coach insights for every marker), a `stat` (e.g. the marker count), and an icon (reuse an existing heroicon path in the file's style). Place it alongside the Normative Ranges card.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "marker-content/page|admin/page" || echo "no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/portal/admin/marker-content/page.tsx` is a server component with the SSR admin gate (`grep -c "role !== 'admin'" ...` >= 1 AND `grep -c "redirect" ...` >= 1)
    - The list iterates categories and links each marker (`grep -c "REPORT_CATEGORIES" ...` >= 1 AND `grep -c "/portal/admin/marker-content/" ...` >= 1)
    - `src/app/portal/admin/page.tsx` ADMIN_SECTIONS contains a `/portal/admin/marker-content` href (`grep -c "/portal/admin/marker-content" src/app/portal/admin/page.tsx` >= 1)
    - `npx tsc --noEmit` shows no errors for these files
  </acceptance_criteria>
  <done>Admin can reach a category-grouped marker list from the admin index, each marker linking to its editor.</done>
</task>

<task type="auto">
  <name>Task 2: Admin per-marker editor (Definition + Impact + coach-insight matrix) with optimistic concurrency + dirty guard</name>
  <files>src/app/portal/admin/marker-content/[marker]/page.tsx</files>
  <read_first>
    - src/app/portal/admin/normative/[marker]/page.tsx (PRIMARY analog — full editor: use(params), fetch-on-mount, gender tabs, isDirty/isSaving/saveSuccess state, beforeunload guard, 409-aware handleSave, save action bar)
    - src/lib/report-markers.ts (markerDef lookup by testKey for the header)
    - src/types/normative.ts (RatingTier order + TIER_LABELS for tier rows)
    - mockups/marker-detail-modal.html (target content tone the admin authors against)
    - .planning/phases/11-report-marker-detail-coach-insights/11-PATTERNS.md (section: portal/admin/marker-content/[marker]/page.tsx — exact state shape, beforeunload, fetch, handleSave with 409, gender tabs, save bar, tone guidance text)
  </read_first>
  <action>
    Create `src/app/portal/admin/marker-content/[marker]/page.tsx` as a `'use client'` component cloning the normative editor. State: `markerDef` (from REPORT_MARKERS by testKey), `definition` string, `impact` string, `coachInsights` as `Record<RatingTier, { male: string; female: string }>` (default empty matrix for all 5 tiers), `isDirty`, `isSaving`, `saveSuccess`, `error`, `serverUpdatedAt`, `loading`, `activeGender: 'male'|'female'`. `TIER_ORDER = ['poor','cautious','normal','great','elite']`.

    On mount: look up markerDef; `fetch('/api/admin/marker-content/'+marker)`, populate definition/impact/coachInsights/serverUpdatedAt, handle `!json.success`. Add the `beforeunload` guard (preventDefault when isDirty) — clone verbatim.

    UI: a Definition textarea and an Impact textarea (both update state + set isDirty). A coach-insight matrix: Male/Female tabs (no 'all' tab — clone the gender-tab affordance for two values only), and under the active gender, one textarea per tier rendered in TIER_ORDER labelled with TIER_LABELS (Attention/Cautious/Normal/Optimal/Peak). Add the D-14 tone guidance text below the heading (consumer-friendly; no disease-prevention/longevity claims; no fabricated numbers). Save bar: `handleSave` PUTs `{ definition, impact, coachInsights, updatedAt: serverUpdatedAt }`; on `res.status === 409` show the conflict error; on `!res.ok` show the error; on success clear isDirty, set saveSuccess, update serverUpdatedAt to Date.now(). Disable Save when `!isDirty || isSaving`. Use dark-portal tokens throughout (bg-bg-3, border-line, text-text, gold-brand for the active tab + Save).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "marker-content/\[marker\]/page" || echo "no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - Component is `'use client'` and reads `marker` via `use(params)` (`grep -c "use(params)" ...` >= 1)
    - Renders Definition + Impact textareas AND a coach-insight matrix over 5 tiers x {male,female} (`grep -cE "TIER_ORDER|'poor'" ...` >= 1 AND male/female gender tabs present)
    - beforeunload dirty guard present (`grep -c "beforeunload" ...` >= 1)
    - handleSave sends `updatedAt: serverUpdatedAt` and handles `res.status === 409` (`grep -c "409" ...` >= 1)
    - D-14 tone guidance text present (`grep -ciE "fabricated|disease|guarantee|consumer-friendly" ...` >= 1)
    - `npx tsc --noEmit` shows no errors for this file
  </acceptance_criteria>
  <done>Editor loads, edits definition/impact + the full tier x gender matrix, guards unsaved changes, and saves with 409-safe optimistic concurrency.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify admin authoring round-trip in the browser</name>
  <what-built>The admin index has a Marker Content card; the list page groups all markers by category; the editor loads seeded content, edits Definition/Impact + the 5-tier x Male/Female matrix, and saves via optimistic-concurrency PUT with a dirty guard.</what-built>
  <how-to-verify>
    1. Start dev server on :8080 (BETTER_AUTH_URL=http://localhost:8080). Log in as admin (admin@admin.com / password123).
    2. Visit http://localhost:8080/portal/admin — confirm a "Marker Content" card appears; click it.
    3. On the list, confirm markers are grouped by category; click "Total Testosterone".
    4. Confirm the editor shows seeded Definition + Impact and a coach-insight matrix; switch Male/Female tabs and confirm per-tier textareas. Edit one tier's text.
    5. Try to navigate away — confirm the beforeunload guard warns. Then Save — confirm "Saved!" and no error.
    6. (Optional) In a second tab, edit + save the same marker, then save the first stale tab — confirm a 409 "updated by another admin" message.
    7. As a non-admin (or logged out), visit /portal/admin/marker-content — confirm redirect to /portal (or /login).
  </how-to-verify>
  <resume-signal>Type "approved" once the nav card, list, editor save, dirty guard, and non-admin redirect all behave. Or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→SSR page | Session role gate enforced server-side before render |
| browser→API | Editor PUT crosses to admin-gated write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Elevation of Privilege | /portal/admin/marker-content* pages | mitigate | SSR admin gate (auth.api.getSession → role !== 'admin' redirect) on both pages, cloned from pillars/page.tsx; API enforces independently (Plan 02) |
| T-11-09 | Tampering | concurrent edits in two browser tabs | mitigate | Editor sends serverUpdatedAt; API returns 409 on stale; UI surfaces conflict and blocks silent overwrite |
| T-11-10 | Repudiation | unsaved-edit data loss | accept | beforeunload guard mitigates accidental loss; not a security control |
</threat_model>

<verification>
- `npx tsc --noEmit` passes for all three files
- Browser round-trip (checkpoint): nav card → list → editor → save → dirty guard → 409 → non-admin redirect
</verification>

<success_criteria>
- Marker Content card on admin index → category-grouped list → per-marker editor
- Editor edits Definition + Impact + 5-tier x male/female matrix; saves with optimistic concurrency + dirty guard
- Both pages server-gated to admins
- D-14 tone guidance surfaced in the editor
</success_criteria>

<output>
Create `.planning/phases/11-report-marker-detail-coach-insights/11-03-SUMMARY.md` when done
</output>
