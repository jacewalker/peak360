---
phase: 11-report-marker-detail-coach-insights
plan: 04
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified:
  - src/components/report/PillarsDisplayModal.tsx
  - src/components/report/PillarsDisplay.tsx
  - src/components/sections/Section11.tsx
autonomous: false
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-12, D-13, D-14]
must_haves:
  truths:
    - "A client viewing the report can click a marker row inside a pillar modal and see a detail panel"
    - "The detail panel shows the marker's Definition ('What it is') and Impact ('How it affects you') when authored"
    - "The detail panel shows a Coach Insight matched to the marker's tier AND the client's gender"
    - "When no authored coach insight exists for (testKey, tier, gender), the panel falls back to generatePeak360Insights output labelled 'Auto-generated · no coach insight authored yet'"
    - "Desktop renders a two-pane master/detail (wider modal); mobile drills in (list → detail with a back control)"
    - "ESC, backdrop click, and focus trap still work; the existing hero, score breakdown, tier grouping, and pillar-level insights block are preserved"
  artifacts:
    - path: "src/components/report/PillarsDisplayModal.tsx"
      provides: "Two-pane master/detail modal with interactive marker rows + MarkerDetailPanel + (tier,gender) insight resolution with D-06 fallback"
      contains: "selectedMarker"
    - path: "src/components/report/PillarsDisplay.tsx"
      provides: "Threads markerContentMap + gender into PillarsDisplayModal"
      contains: "markerContentMap"
    - path: "src/components/sections/Section11.tsx"
      provides: "Fetches /api/marker-content in loadReport(), builds Map<testKey,MarkerContent>, threads it + gender to PillarsDisplay"
      contains: "marker-content"
  key_links:
    - from: "src/components/sections/Section11.tsx"
      to: "/api/marker-content"
      via: "fetch inside loadReport() effect, build Map<testKey,MarkerContent>"
      pattern: "/api/marker-content"
    - from: "src/components/sections/Section11.tsx"
      to: "src/components/report/PillarsDisplay.tsx"
      via: "markerContentMap + gender props"
      pattern: "markerContentMap"
    - from: "src/components/report/PillarsDisplay.tsx"
      to: "src/components/report/PillarsDisplayModal.tsx"
      via: "markerContentMap + gender props"
      pattern: "markerContentMap"
    - from: "src/components/report/PillarsDisplayModal.tsx"
      to: "src/lib/normative/insights.ts"
      via: "fallback to generatePeak360Insights output by markerKey when no authored (tier,gender) insight"
      pattern: "insights"
---

<objective>
Make markers inside the pillar modal interactive: clicking a marker reveals a detail panel with Definition, Impact, and a tier+gender-matched Coach Insight (with D-06 auto fallback). Desktop = two-pane master/detail; mobile = drill-in. Thread the marker-content map + client gender from `Section11` through `PillarsDisplay` into the modal. Web report only — NO PDF / @react-pdf changes (D-12).

Purpose: This is the user-facing payoff of the phase — a client reads what each marker is, why it matters, and tailored coaching for their result. Backed by the table/API/seed from Plans 01-02.
Output: Extended `PillarsDisplayModal` + thread-through in `PillarsDisplay` and `Section11`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-report-marker-detail-coach-insights/11-CONTEXT.md
@.planning/phases/11-report-marker-detail-coach-insights/11-PATTERNS.md
@mockups/marker-detail-modal.html

<interfaces>
<!-- From Plans 01-02 (merged before this wave): -->
MarkerContent (src/lib/marker-content/queries.ts): { testKey, definition: string|null, impact: string|null, coachInsights: Record<RatingTier,{male:string|null;female:string|null}>|null, updatedBy, updatedAt }
GET /api/marker-content -> { success, data: MarkerContent[] }  (requireSession, any role)

ReportMarker (src/lib/pdf/types): carries .key (=testKey), .label, .value, .unit, .tier (RatingTier|null)
Insight (src/lib/normative/insights.ts): { markerKey: string; title: string; why: string; doNow: string[] }  -- the D-06 fallback source; routed by markerKey === testKey
PillarsDisplay current props (src/components/report/PillarsDisplay.tsx): { pillars, markers?, insights? }
PillarsDisplayModal current props (src/components/report/PillarsDisplayModal.tsx): { open, onClose, pillar, markers, insights? }
TIER_THEME already defined inside PillarsDisplayModal (tier->color map); TIER_LABELS in src/types/normative.ts
Section11 already derives gender = info.clientGender inside loadReport()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch marker content in Section11 and thread map + gender to PillarsDisplay</name>
  <files>src/components/sections/Section11.tsx, src/components/report/PillarsDisplay.tsx</files>
  <read_first>
    - src/components/sections/Section11.tsx (the loadReport() effect ~lines 165-237: parallel section fetches, gender = info.clientGender derivation, the <PillarsDisplay> render ~line 226)
    - src/components/report/PillarsDisplay.tsx (Props interface lines 10-14; the PillarsDisplayModal render with pillar/markers/insights)
    - src/lib/marker-content/queries.ts (MarkerContent type -- import with `import type`)
    - .planning/phases/11-report-marker-detail-coach-insights/11-PATTERNS.md (sections: Section11.tsx and PillarsDisplay note)
  </read_first>
  <action>
    In `src/components/sections/Section11.tsx`: add state `markerContentMap: Map<string, MarkerContent>` (default `new Map()`) and `gender: string | null`. Inside `loadReport()`, fetch `/api/marker-content` (in parallel with the existing section fetches or immediately after), and on `json.success` build a `Map` keyed by `row.testKey`; `setMarkerContentMap(map)`. Set `gender` from the already-derived `info.clientGender` via `setGender(...)`. Pass `markerContentMap={markerContentMap}` and `gender={gender}` into `<PillarsDisplay>`. Import `MarkerContent` with `import type`.

    In `src/components/report/PillarsDisplay.tsx`: extend the `Props` interface with `markerContentMap?: Map<string, MarkerContent>` and `gender?: string | null`; destructure them in the component signature; pass both through to `<PillarsDisplayModal>`. Import `MarkerContent` with `import type`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "Section11|PillarsDisplay" || echo "no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/sections/Section11.tsx` fetches `/api/marker-content` and builds a Map keyed by testKey (`grep -c "/api/marker-content" ...` >= 1 AND `grep -c "markerContentMap" ...` >= 1)
    - Section11 threads `markerContentMap` and `gender` into `<PillarsDisplay` (both appear as JSX props)
    - `PillarsDisplay.tsx` Props interface includes `markerContentMap` and `gender`, and passes them to `<PillarsDisplayModal` (`grep -c "markerContentMap" src/components/report/PillarsDisplay.tsx` >= 2)
    - `npx tsc --noEmit` shows no errors for these files
  </acceptance_criteria>
  <done>marker-content map + client gender flow from the API through Section11 and PillarsDisplay into the modal.</done>
</task>

<task type="auto">
  <name>Task 2: Extend PillarsDisplayModal into two-pane master/detail with MarkerDetailPanel + D-06 fallback</name>
  <files>src/components/report/PillarsDisplayModal.tsx</files>
  <read_first>
    - src/components/report/PillarsDisplayModal.tsx (ENTIRE file -- Props interface ~17-23, TIER_THEME tier-color map ~95-134, focus trap + body-scroll lock ~180-238, the static <li> marker rows ~462-499, the wrapper/container ~311-312, the pillar-level Insights block ~508-541)
    - mockups/marker-detail-modal.html (the VISUAL CONTRACT -- desktop two-pane `.body` grid `minmax(280px,38%) 1fr`, `.pane-list` / `.pane-detail`, interactive `.marker` buttons with aria-selected, `.detail` panel structure: d-top header + value + tier pill, .block "What it is"/"How it affects you", .coach gold-rail card with Male/Female badge + bullets + .fallback-note, the mobile drill-in `.m-back` + media query)
    - src/lib/normative/insights.ts (Insight shape -- fallback content: title/why/doNow routed by markerKey === testKey)
    - src/types/normative.ts (RatingTier, TIER_LABELS -- Attention/Cautious/Normal/Optimal/Peak)
    - .planning/phases/11-report-marker-detail-coach-insights/11-PATTERNS.md (section: PillarsDisplayModal.tsx -- exact prop additions, container split, button conversion, MarkerDetailPanel guidance, preserve focus trap)
  </read_first>
  <action>
    Extend `src/components/report/PillarsDisplayModal.tsx`. Add to Props: `markerContentMap?: Map<string, MarkerContent>` and `gender?: string | null` (import MarkerContent with `import type`). Add `selectedMarker` state (the selected marker key | null). PRESERVE verbatim: the outer `<aside>` wrapper, focus trap, body-scroll lock, ESC/backdrop close, the hero, the score breakdown bar, the tier grouping, and the existing pillar-level "Insights & recommendations" block (it stays as pillar-level, below/after the pane area per D-01).

    Widen the desktop container from `max-w-[640px]` to ~`max-w-[980px]` and split the body into a two-pane grid `md:grid-cols-[minmax(280px,38%)_1fr]` (per mockup `.body`). LEFT pane = the existing tier-grouped marker list, but each static `<li>` row becomes a `<button>` with `onClick={() => setSelectedMarker(m.key)}`, `aria-selected`, the tier rail, label, value+unit, and a chevron -- selected state styled with the gold gradient/border per the mockup `.marker[aria-selected="true"]`. RIGHT pane = a `MarkerDetailPanel` for `selectedMarker` (or an empty-state prompt when none selected); hidden on mobile (`hidden md:block`). MOBILE = single column drill-in: when `selectedMarker` is set, show a full-width detail overlay with a back button (`<- {pillar.label}`, clears selectedMarker) per the mockup `.m-back`; the media query collapses the grid to one column.

    Build a `MarkerDetailPanel` sub-component (in-file) taking the selected ReportMarker, its `MarkerContent` (from `markerContentMap.get(m.key)`), `gender`, and the pillar's `insights`. Render per D-03 order: (1) header -- marker name + value+unit + tier pill (color from TIER_THEME); (2) "What it is" block from `content.definition` -- OMIT the block if blank (no fallback per D-06); (3) "How it affects you" block from `content.impact` -- OMIT if blank; (4) Coach Insight gold-railed card. Coach-insight resolution per D-05/D-06: normalize gender to `'male'|'female'`; if `content.coachInsights?.[tier]?.[gender]` is non-empty, render it in the card with a "[Male|Female] - [TIER_LABEL]" badge (tier-dot color). Otherwise FALL BACK to `generatePeak360Insights` output for this marker -- find the `Insight` where `markerKey === m.key` from the `insights` prop, render its why/doNow, badge it "[TIER_LABEL]" (no gender), and show the `.fallback-note` "Auto-generated - no coach insight authored yet" per the mockup hs-CRP example. Render all text fields as plain React text children (React auto-escapes) -- never inject raw HTML. Use only dark-portal tokens (D-13): bg-bg-2/bg-bg-3, text-text/text-dim/text-faint, border-line/line-2, gold-brand/champagne, the tier hues; Inter Tight + JetBrains Mono mono eyebrows. No PDF files touched (D-12).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "PillarsDisplayModal" || echo "no type errors"; git diff --name-only 2>/dev/null | grep -E "src/lib/pdf/|Peak360Report" && echo "VIOLATION: PDF files modified" || echo "OK: no PDF changes (D-12)"</automated>
  </verify>
  <acceptance_criteria>
    - Props gain `markerContentMap` and `gender` (`grep -c "markerContentMap" src/components/report/PillarsDisplayModal.tsx` >= 2)
    - `selectedMarker` state added and marker rows are interactive `<button>` elements with `aria-selected` (`grep -c "selectedMarker" ...` >= 3 AND `grep -c "aria-selected" ...` >= 1)
    - Desktop container widened (`grep -cE "max-w-\[980px\]|max-w-\[9" ...` >= 1) and a two-pane grid present (`grep -c "md:grid-cols-" ...` >= 1)
    - Mobile drill-in back control present (`grep -ciE "back|pillar.label" ...` for the back button)
    - Coach-insight resolution reads `coachInsights` by tier+gender AND falls back to the `insights` prop with the auto-generated note (`grep -ci "coachInsights" ...` >= 1 AND `grep -ci "auto-generated" ...` >= 1)
    - Definition/Impact blocks are omitted when blank (no fallback) -- conditional render present
    - Content rendered as escaped React text (no raw-HTML injection used for definition/impact/coachInsights)
    - No `src/lib/pdf/*` or `Peak360Report` file in the diff (D-12); the verify script prints "OK: no PDF changes (D-12)"
    - `npx tsc --noEmit` shows no errors for this file
  </acceptance_criteria>
  <done>The modal is a working master/detail: marker rows select; the detail panel shows definition/impact (when authored) + a tier+gender coach insight with D-06 fallback; desktop two-pane, mobile drill-in; focus trap and pillar insights preserved; no PDF changes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the report marker-detail experience against the mockup</name>
  <what-built>Pillar modal markers are now clickable; the detail panel shows Definition + Impact + a tier+gender-matched Coach Insight (or labelled auto fallback). Desktop is two-pane master/detail; mobile is drill-in. Hero, score bar, tier grouping, focus trap, ESC/backdrop, and pillar-level insights are preserved. No PDF changes.</what-built>
  <how-to-verify>
    1. Start dev server on :8080. Log in (admin@admin.com / password123) and open a completed assessment's report (Section 11 / "Section 10 of 10").
    2. Open a pillar modal (e.g. Cardiometabolic). Confirm the desktop modal is wider with a two-pane layout: marker list left, detail right.
    3. Click a marker that HAS authored seed content -- confirm the right pane shows: marker header + value + tier pill; "What it is"; "How it affects you"; a gold-railed Coach Insight card with a "[Male|Female] · [Tier]" badge matching the client's gender and that marker's tier.
    4. Verify gender match: for a male client the badge says "Male"; spot-check a female client's report shows female-authored text.
    5. Find a marker with NO authored coach insight (or temporarily clear one in the admin editor) -- confirm the fallback shows the generatePeak360Insights content with the "Auto-generated · no coach insight authored yet" note.
    6. Shrink the viewport to mobile width: confirm tapping a marker drills into a full-width detail with a back control that returns to the list.
    7. Confirm ESC and backdrop click still close the modal, focus is trapped, and the pillar-level "Insights & recommendations" block still renders. Confirm the PDF export is unchanged (open it -- no marker-detail content added).
  </how-to-verify>
  <resume-signal>Type "approved" once authored insights match (tier+gender), the fallback note appears for unauthored markers, desktop two-pane + mobile drill-in both work, focus trap/ESC/backdrop are intact, and the PDF is unchanged. Or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API->client render | marker_content fetched client-side and rendered into the report DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11 | Information Disclosure | client sees coach insight for their own gender/tier | accept | Content is global non-PII clinical guidance gated by requireSession; the (tier,gender) selection is presentational, not an authorization boundary |
| T-11-12 | Tampering | rendered seed/admin content is plain text | mitigate | Render definition/impact/coachInsights as escaped React text children; do not inject raw HTML for these fields |
| T-11-13 | Spoofing | fabricated clinical claims in displayed content | mitigate | D-14 tone constraint enforced at authoring time (Plan 03 guidance); seed content avoids fabricated numbers (Plan 01) |
</threat_model>

<verification>
- `npx tsc --noEmit` passes for all three files
- No `src/lib/pdf/*` or `Peak360Report` files modified (D-12 enforced in verify + acceptance)
- Browser checkpoint: clickable markers, tier+gender insight match, fallback note, desktop two-pane + mobile drill-in, preserved focus trap/ESC/backdrop/pillar insights, unchanged PDF
</verification>

<success_criteria>
- Markers inside the pillar modal are clickable and reveal a detail panel (D-01)
- Detail shows Definition + Impact (gender-neutral, omitted if blank) and a (tier,gender)-matched Coach Insight (D-03, D-04, D-05)
- Unauthored coach insight falls back to generatePeak360Insights with the labelled note (D-06)
- Desktop two-pane master/detail; mobile drill-in (D-02), per the mockup (D-13)
- Existing hero/score/tier-grouping/focus-trap/pillar-insights preserved; no PDF changes (D-12)
</success_criteria>

<output>
Create `.planning/phases/11-report-marker-detail-coach-insights/11-04-SUMMARY.md` when done
</output>
