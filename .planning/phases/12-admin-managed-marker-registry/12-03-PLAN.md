---
phase: 12-admin-managed-marker-registry
plan: 03
type: execute
wave: 3
depends_on: ['12-02']
files_modified:
  - src/app/portal/admin/markers/page.tsx
  - src/app/portal/admin/markers/MarkersList.tsx
  - src/app/portal/admin/markers/new/page.tsx
  - src/app/portal/admin/markers/[testKey]/page.tsx
  - src/app/portal/admin/page.tsx
autonomous: false
requirements: [D-05, D-12, D-15, D-16]
must_haves:
  truths:
    - "An admin can navigate from /portal/admin to a new 'Markers' card landing on /portal/admin/markers"
    - "The list page shows all markers grouped by section, with a 'SEEDED' badge on REPORT_MARKERS rows (read-only) and edit/delete actions on DB rows"
    - "The /portal/admin/markers/new page captures: label (auto-derives test_key + data_key preview), section dropdown (1..10), pillar dropdown (5 PILLAR_KEYS), category, subcategory, fallbackUnit, has_norms toggle, inline 5-tier ranges editor (only when has_norms=true), AI aliases textarea (comma-separated), severity weight slider"
    - "Submitting the create form POSTs to /api/admin/markers and on success redirects to /portal/admin/marker-content/[testKey] for content authoring (D-06)"
    - "The edit page prefills, blocks data_key edits (input shown but disabled with tooltip), supports save (PUT with updatedAt) and cascade delete (inline confirm)"
    - "All three new pages render with Phase 9 dark portal brand tokens (bg-bg-3, border-line, text-text, text-gold-brand) and gate non-admin sessions"
  artifacts:
    - path: "src/app/portal/admin/markers/page.tsx"
      provides: "SSR-gated list page; Hero header; child MarkersList client component"
    - path: "src/app/portal/admin/markers/MarkersList.tsx"
      provides: "client list grouped by section with seed/db distinction"
    - path: "src/app/portal/admin/markers/new/page.tsx"
      provides: "Create form with inline ranges editor + post-create redirect"
    - path: "src/app/portal/admin/markers/[testKey]/page.tsx"
      provides: "Edit form with dirty guard, optimistic concurrency, cascade delete confirm"
    - path: "src/app/portal/admin/page.tsx"
      provides: "ADMIN_SECTIONS extended with the Markers card"
      contains: "Markers"
  key_links:
    - from: "src/app/portal/admin/markers/new/page.tsx"
      to: "/api/admin/markers POST"
      via: "form submit -> on 201 redirect to /portal/admin/marker-content/[testKey]"
      pattern: "router.push.*marker-content"
    - from: "src/app/portal/admin/markers/[testKey]/page.tsx"
      to: "/api/admin/markers/[testKey] DELETE"
      via: "inline confirm -> DELETE -> redirect back to list"
      pattern: "DELETE"
    - from: "src/app/portal/admin/page.tsx"
      to: "/portal/admin/markers"
      via: "new entry in ADMIN_SECTIONS array"
      pattern: "/portal/admin/markers"
---

<objective>
Build the admin-facing UI for the marker registry: a section-grouped list, a create form with inline initial ranges and AI aliases, and an edit form with cascade-aware delete. Add a nav card to the admin landing. All surfaces inherit Phase 9 dark portal brand styling and clone the existing Phase 11 marker-content / Phase 3 normative editors for visual consistency.

Purpose: This is the admin's only way to actually add a marker without a code change. The form must collect everything Plan 02's POST route validates, and on success route the admin to the existing Phase 11 marker-content editor (D-06) so they can author the definition / impact / coach insights matrix.
Output: Three new pages + a small list client component + the admin nav card; all four route handlers from Plan 02 are exercised end-to-end by these pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-admin-managed-marker-registry/12-CONTEXT.md
@.planning/phases/12-admin-managed-marker-registry/12-PATTERNS.md
@.planning/phases/12-admin-managed-marker-registry/12-02-SUMMARY.md

<interfaces>
<!-- UI consumes these API contracts (defined fully in Plan 02). -->

GET  /api/admin/markers           -> { success, data: { markers: MarkerRow[] } }
POST /api/admin/markers           -> 201 { success, data: { marker: MarkerRow } } | 400 | 409
GET  /api/admin/markers/[testKey] -> { success, data: { marker } } | 404
PUT  /api/admin/markers/[testKey] -> { success, data: { marker } } | 400 (data_key change) | 409 (optimistic conflict)
DELETE /api/admin/markers/[testKey] -> { success, data: { deletedMarker, deletedContent, deletedRanges } }
GET  /api/markers                 -> { success, data: { markers: RegistryMarker[] } } (used by list to merge in seeded rows)

POST body shape (re-pasted for the create form):
  { testKey, label, section, dataKey, pillar, category, subcategory?, fallbackUnit?,
    hasNorms, aiAliases?, severityWeight?, initialTiers? (required iff hasNorms), initialUnit? }

PILLAR_KEYS: 'cardiometabolic' | 'bodyComposition' | 'strength' | 'balance' | 'vo2'
SECTION_OPTIONS: 1..10 (Section 11 is report-only per D-08)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin landing card + list page + MarkersList client component</name>
  <files>src/app/portal/admin/page.tsx, src/app/portal/admin/markers/page.tsx, src/app/portal/admin/markers/MarkersList.tsx</files>
  <read_first>
    - src/app/portal/admin/page.tsx:6-71 (ADMIN_SECTIONS array - clone an existing entry like 'Marker Content' lines 22-38)
    - src/app/portal/admin/marker-content/page.tsx (the SSR session gate at lines 17-20 and Hero header block at 25-47 - clone verbatim)
    - src/app/portal/admin/marker-content/MarkerContentList.tsx (the section-grouped list pattern with useMemo + search input + hover row link)
    - src/app/portal/admin/normative/page.tsx (the StatusPill 'HC' badge pattern at lines 18-28 for the SEEDED badge)
    - .planning/phases/12-admin-managed-marker-registry/12-PATTERNS.md sections 7, 8
  </read_first>
  <action>
    1. In `src/app/portal/admin/page.tsx` ADMIN_SECTIONS array (insert between the 'Marker Content' entry and the 'Audit Logs' entry per 12-PATTERNS.md section 8):
       ```typescript
       {
         label: 'Markers',
         href: '/portal/admin/markers',
         description: 'Add or remove markers from any assessment section. Configure pillar, normative ranges, and AI extraction aliases.',
         stat: 'Registry',
         icon: (<svg className="w-7 h-7" ... />),  // pick a plus-in-circle or list-plus icon matching the existing icon style
       }
       ```
       Do NOT change anything else in ADMIN_SECTIONS - this card is rendered automatically by the `.map()` at line 97.

    2. Create `src/app/portal/admin/markers/page.tsx` cloning `src/app/portal/admin/marker-content/page.tsx` verbatim, but:
       - Title: `Markers`
       - Eyebrow: `REGISTRY` (or similar - match the marker-content eyebrow's tone)
       - Description: `Add or remove markers from any assessment section. Configure pillar, AI aliases, and initial normative ranges.`
       - Top-right CTA button linking to `/portal/admin/markers/new` labelled `Add marker` (mirror the visual style of any existing primary CTA button in the admin area - check normative or marker-content list pages)
       - Child component: `<MarkersList />` (the new client component below)
       - Same SSR gate (lines 17-20 of marker-content/page.tsx).

    3. Create `src/app/portal/admin/markers/MarkersList.tsx` as a 'use client' component:
       - On mount, `fetch('/api/markers')` (the merged list - shows both seeded and DB rows so admin sees full inventory). Set `markers` state.
       - Optional search input (clone the search-by-label input from MarkerContentList.tsx).
       - Group markers by `section` (1..10) using `useMemo`. Render each section as a collapsible block or sticky-header group (planner's discretion - prefer the same group pattern MarkerContentList uses).
       - Per row, render label + testKey (mono) + category. For seeded rows (`m.source === 'seed'`), render a muted `SEEDED` badge and DO NOT show edit/delete actions (the row links to `/portal/admin/marker-content/[testKey]` and `/portal/admin/normative/[testKey]` instead, mirroring the existing seeded-marker editing flow). For DB rows (`m.source === 'db'`), render `Edit` and `Delete` link/buttons. `Edit` navigates to `/portal/admin/markers/[testKey]`; `Delete` invokes Plan 02's DELETE endpoint after an inline confirm (or defers to the edit-page's delete confirm and just links to edit).
       - For the per-row delete: use the same inline-confirm pattern adopted in Task 3 (two-click: button label changes to 'Confirm delete' on first click; second click fires DELETE).

    All three files inherit the Phase 9 dark portal brand tokens (bg-bg, bg-bg-3, border-line, text-text, text-text-dim, text-gold-brand) per D-15 by cloning the markup of the marker-content list page.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "portal/admin/(markers|page)" || echo "no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "/portal/admin/markers" src/app/portal/admin/page.tsx` >= 1 (nav card added)
    - `grep -c "label: 'Markers'" src/app/portal/admin/page.tsx` >= 1
    - `src/app/portal/admin/markers/page.tsx` contains the SSR gate (`grep -c "session.user.role !== 'admin'" src/app/portal/admin/markers/page.tsx` >= 1) and imports `MarkersList`
    - `src/app/portal/admin/markers/MarkersList.tsx` is 'use client', fetches /api/markers, groups by section, shows SEEDED badge for `source === 'seed'`
    - `grep -c "Add marker" src/app/portal/admin/markers/page.tsx` >= 1 (CTA present)
    - `npx tsc --noEmit` clean for the three files
  </acceptance_criteria>
  <done>Admin nav card and section-grouped list page live; admin can see every marker (seeded + DB) and click through to edit or add.</done>
</task>

<task type="auto">
  <name>Task 2: Create-marker form with inline initial ranges editor (/portal/admin/markers/new)</name>
  <files>src/app/portal/admin/markers/new/page.tsx</files>
  <read_first>
    - src/app/portal/admin/marker-content/[marker]/page.tsx (form scaffolding: `use(params)` at line 54, useState block at 56-66, fetch-on-mount at 69-91, isDirty + beforeunload, save button success state)
    - src/app/portal/admin/normative/[marker]/page.tsx:26-57 (emptyTiers default + validateTiers - the inline ranges sub-component the new form needs at create time)
    - src/components/forms/FormField.tsx (the FormField primitive used for label/category/etc.)
    - src/lib/pillars/keys.ts (PILLAR_KEYS for the pillar dropdown)
    - .planning/phases/12-admin-managed-marker-registry/12-PATTERNS.md section 7 (Edit page analog + form composition primitive)
  </read_first>
  <action>
    Create `src/app/portal/admin/markers/new/page.tsx` as an SSR-gated page that renders a 'use client' form. SSR gate same as Task 1's list page (lines 17-20 of marker-content/page.tsx).

    Form fields (single-column layout - acceptable per CONTEXT "Claude's Discretion"; planner may group them visually):
    - `label` (FormField, text, required) - on change, auto-derive a preview `testKey` (lowercase, strip non-`a-z0-9_`, replace spaces with `_`, regex-collapse `_+`) and `dataKey` (camelCase the words: first word lowercase, subsequent words capitalised, strip non-alphanumeric). Both previews shown as read-only mono text below the label input, with a small `Edit` link toggling them into editable text inputs so admin can override the derivation. Both must satisfy their regex (testKey: `^[a-z][a-z0-9_]*$`; dataKey: `^[a-z][a-zA-Z0-9]*$`) - show inline validation error if not.
    - `section` (native `<select>`, options 1..10, required, default 5) - styled to match the dark portal form aesthetic.
    - `pillar` (native `<select>`, options 'cardiometabolic'/'bodyComposition'/'strength'/'balance'/'vo2', required) - labels shown as human-readable ('Cardiometabolic', 'Body Composition', 'Strength', 'Balance', 'VO2').
    - `category` (FormField, text, required)
    - `subcategory` (FormField, text, optional)
    - `fallbackUnit` (FormField, text, optional, placeholder 'mg/dL')
    - `hasNorms` (toggle/switch, default true) - when toggled off, the inline ranges block hides per D-05.
    - `initialUnit` (FormField, text, optional) - shown only when hasNorms=true; defaults to fallbackUnit if blank.
    - **Inline 5-tier ranges editor** (rendered only when hasNorms=true): five rows (poor, cautious, normal, great, elite), each with two numeric inputs (min, max). Clone the JSX shape from `src/app/portal/admin/normative/[marker]/page.tsx` (the per-tier min/max grid). Use `emptyTiers` as the initial state. Run `validateTiers` on submit and block submit on failure.
    - `aiAliases` (textarea, optional) - comma-separated; on submit, split by comma, trim, drop empties, send as `string[]`. Include a guidance helper text per D-16 / CONTEXT risk #4: `Be specific. Prefer multi-word terms (e.g. "apolipoprotein b" not "iron"). Markers without aliases are manual-entry only.`
    - `severityWeight` (range slider or number input, optional, integer 0..10 with default unset) - matches normativeRanges.severity_weight semantics.

    Submit handler:
    1. Build the request body matching Plan 02's interface block.
    2. `const res = await fetch('/api/admin/markers', { method: 'POST', body: JSON.stringify(body) })`.
    3. On 201, parse `data.marker.testKey` and `router.push(\`/portal/admin/marker-content/${data.marker.testKey}\`)` per D-06.
    4. On 400/409, render the server's `error` string inline near the submit button.
    5. On other errors, render a generic 'Could not save - please try again.' message.

    Page-level wiring: Hero header per 12-PATTERNS section 7 (breadcrumb back-link to `/portal/admin/markers`, MonoEyebrow `NEW MARKER`, `<h1>Add marker</h1>`, description sentence). Phase 9 dark portal tokens throughout per D-15.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "portal/admin/markers/new" || echo "no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/portal/admin/markers/new/page.tsx` exists and includes the SSR admin gate
    - Form has fields for label, testKey (auto-derived + editable), dataKey (auto-derived + editable), section dropdown, pillar dropdown, category, subcategory, fallbackUnit, hasNorms toggle, aiAliases textarea, severityWeight, and the inline 5-tier ranges editor (grep for `'poor'`, `'cautious'`, `'normal'`, `'great'`, `'elite'` strings should match >= 5)
    - Submit POSTs to `/api/admin/markers` (`grep -c "fetch.*'/api/admin/markers'" src/app/portal/admin/markers/new/page.tsx` >= 1)
    - On success, redirects to `/portal/admin/marker-content/<testKey>` (`grep -c "marker-content/" src/app/portal/admin/markers/new/page.tsx` >= 1)
    - testKey regex `^[a-z][a-z0-9_]*$` is enforced client-side before submit (defensive UX; server is the authority)
    - The aiAliases helper text contains the multi-word guidance per D-16
    - `npx tsc --noEmit` clean for this file
  </acceptance_criteria>
  <done>Admin can fill the create form, submit, watch the marker land in DB + initial ranges, and arrive at the marker-content editor for the next step.</done>
</task>

<task type="auto">
  <name>Task 3: Edit-marker page with data_key lock, optimistic concurrency, cascade delete confirm (/portal/admin/markers/[testKey])</name>
  <files>src/app/portal/admin/markers/[testKey]/page.tsx</files>
  <read_first>
    - src/app/portal/admin/marker-content/[marker]/page.tsx (full file - the closest edit-page analog; `use(params)` at line 54, isDirty + beforeunload, save button success state, optimistic concurrency via updatedAt in PUT body)
    - src/app/portal/admin/normative/[marker]/page.tsx (the inline-ranges editor shape if a DB-marker's range editing is to be partially exposed here - or just defer with a link)
    - .planning/phases/12-admin-managed-marker-registry/12-PATTERNS.md section 7
  </read_first>
  <action>
    Create `src/app/portal/admin/markers/[testKey]/page.tsx` as an SSR-gated page that:
    - Reads `testKey` from `use(params)`.
    - On mount, `fetch('/api/admin/markers/' + testKey)` to load the marker. 404 -> render a friendly 'Marker not found' card with a back link to the list.
    - **Seeded-marker redirect:** If the GET succeeds and the marker came from the seed (this route is admin-API which returns ONLY DB rows per Plan 02 GET, so a seeded testKey will 404; nothing to do beyond the friendly 404 above). Optionally add a comment noting that seeded markers are edited via `/portal/admin/normative/[testKey]` and `/portal/admin/marker-content/[testKey]` per D-12.
    - Renders the same form fields as Task 2 (label, section, pillar, category, subcategory, fallbackUnit, hasNorms toggle, aiAliases textarea, severityWeight) prefilled from the loaded marker.
    - **testKey is shown read-only** (mono text, no input - PK is immutable in this UI).
    - **dataKey is shown but DISABLED** with a tooltip / helper text: `Locked after creation - changing this would orphan existing assessment data.` (matches the server-side D-13 guard). Do NOT include dataKey in the PUT body to avoid the 400.
    - Track `isDirty`; on `beforeunload` warn if dirty (clone Phase 11 marker-content editor's pattern at lines 172-179 of normative editor).
    - Save button: POSTs `PUT /api/admin/markers/[testKey]` with the updated fields + `updatedAt: marker.updatedAt` for optimistic concurrency. On 409, show the server's error string ('This marker was updated by another admin...') and offer a `Reload` button that re-fetches the marker. On 200, update local `marker.updatedAt` from the response, clear `isDirty`, show a transient 'Saved' state on the button.
    - **Delete cascade confirm (inline, two-click)** per 12-PATTERNS section 7 / CONTEXT "Claude's Discretion": a destructive button labelled `Delete marker` at the bottom of the form. On first click, the button label changes to `Confirm delete - this also clears marker_content and normative ranges` (use a destructive color token). On second click within ~5 seconds, fires `DELETE /api/admin/markers/[testKey]`. On 200, `router.push('/portal/admin/markers')`. On error, show inline error.

    Page-level wiring: Hero header (breadcrumb back to `/portal/admin/markers`, MonoEyebrow `EDIT MARKER`, `<h1>{marker.label}</h1>`, description with `testKey: {marker.testKey}` in mono). Dark portal brand tokens.

    Below the save button, surface two cross-links per D-12:
    - `Edit ranges (gender / age) -> /portal/admin/normative/[testKey]`
    - `Edit content (definition / impact / coach insights) -> /portal/admin/marker-content/[testKey]`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "portal/admin/markers/\\[testKey\\]" || echo "no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - File exists with SSR admin gate
    - Loads marker via `GET /api/admin/markers/<testKey>`, renders prefilled form
    - dataKey input is disabled or rendered as read-only text (`grep -c "disabled\\|readOnly" src/app/portal/admin/markers/\\[testKey\\]/page.tsx` >= 1 near dataKey, plus a 'Locked after creation' helper string)
    - testKey is rendered read-only
    - Save PUTs with updatedAt for optimistic concurrency (`grep -c "updatedAt" src/app/portal/admin/markers/\\[testKey\\]/page.tsx` >= 2)
    - 409 path shows the server error string AND a Reload button
    - Delete button uses two-click inline confirm and fires DELETE
    - Cross-links to `/portal/admin/normative/<testKey>` and `/portal/admin/marker-content/<testKey>` are present
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Admin can edit any DB-driven marker safely (dataKey locked, optimistic concurrency working) and delete it with full cascade.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [VERIFY] Admin UI smoke test - create, list, edit, delete a marker</name>
  <what-built>Three new admin pages and a list component plus the admin landing card; all four routes from Plan 02 are now reachable via the UI. Visual styling, form behavior, and the post-create redirect to marker-content authoring all need a human eyeball.</what-built>
  <how-to-verify>
    1. Boot the dev server: `npm run dev` (port 8080 per dev memory). Sign in as `admin@admin.com` / `password123`.
    2. Navigate to `/portal/admin`. Confirm a new `Markers` card is visible between Marker Content and Audit Logs. Click it.
    3. On `/portal/admin/markers`, confirm: section-grouped list of all markers; seeded markers carry a `SEEDED` badge and have no edit/delete actions; an `Add marker` CTA is top-right.
    4. Click `Add marker`. Fill out a test marker: label `Apolipoprotein B`, watch test_key auto-derive to `apolipoprotein_b` and data_key to `apolipoproteinB`. Section 5, pillar `cardiometabolic`, category `lipids`, subcategory `apo`, fallbackUnit `mg/dL`, hasNorms ON, fill in plausible tier ranges (poor >130, cautious 110-130, normal 90-110, great 70-90, elite <70), aliases `apolipoprotein b, apo b, apob`, severityWeight 7. Submit.
    5. Confirm you are redirected to `/portal/admin/marker-content/apolipoprotein_b`. (Plan 04 will fix the marker-content editor's REPORT_MARKERS sweep so this page actually loads for DB markers - for now it may 404 or render the editor; that's tracked in Plan 04. Note any 404 here.)
    6. Navigate back to `/portal/admin/markers`. Confirm the new marker appears under Section 5 with edit/delete actions (no SEEDED badge).
    7. Click `Edit` on the new marker. Confirm: testKey is read-only, dataKey is disabled with the 'Locked after creation' tooltip, all other fields are prefilled. Edit the label (e.g. `Apolipoprotein B (ApoB)`), save. Confirm a transient `Saved` indicator.
    8. From a second browser tab, open the same edit page. Save in tab 1 again. Save in tab 2 - confirm you see the 409 error and a Reload button.
    9. On the edit page, click `Delete marker`. Confirm the button label changes to `Confirm delete...`. Click again. Confirm you land back on the list with the marker gone, and that the live DB shows no orphan rows in marker_content or normative_ranges for that testKey.
    10. Try collision: Add marker with testKey `hdl` (collides with seed). Confirm a 409 'testKey conflicts with a seeded marker' inline error.
  </how-to-verify>
  <resume-signal>Type "approved" once the 10 steps pass (or note step 5 deferred to Plan 04). Or describe issues for revision.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> admin pages | SSR session gate; client form posts JSON to admin APIs validated server-side |
| client form -> Plan 02 APIs | Validation is dual-layer (UX + server); server is authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-10 | Elevation of Privilege | non-admin reaching /portal/admin/markers/* | mitigate | SSR session gate (lines 17-20 pattern) on every new page |
| T-12-11 | Tampering | client form bypassing dataKey lock | accept (server is authority) | Server-side 400 on PUT with changed dataKey (Plan 02 D-13 guard); UI lock is UX-only |
| T-12-12 | Information Disclosure | error strings leaking internals | mitigate | Plan 02 returns human-readable error strings; UI displays verbatim; no stack traces surfaced |
| T-12-13 | Tampering | accidental delete of populated marker | mitigate | Two-click inline confirm (12-PATTERNS section 7) - intentional friction for destructive action |
</threat_model>

<verification>
- All four files type-check (`npx tsc --noEmit`)
- The smoke test in Task 4 passes end-to-end
- Visual: dark portal tokens applied; no light-mode bleed on the new pages
- Audit log shows `marker.create`, `marker.update`, `marker.delete` events after the smoke test
</verification>

<success_criteria>
- Admin landing page has the Markers card
- List page renders all markers grouped by section with seeded vs DB distinction
- Create form captures every field Plan 02 validates and redirects to marker-content on success
- Edit page enforces dataKey lock, optimistic concurrency, and cascade delete via two-click confirm
- All surfaces use Phase 9 dark portal brand tokens
</success_criteria>

<output>
Create `.planning/phases/12-admin-managed-marker-registry/12-03-SUMMARY.md` when done
</output>
</content>
