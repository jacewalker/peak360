---
phase: 08-client-report-design-refresh
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src/components/ui/Dialog.tsx
  - src/components/report/PillarsGrid.tsx
  - src/components/report/PillarCard.tsx
  - src/components/report/PillarModal.tsx
  - src/components/report/DetailedMarkerResultsDisclosure.tsx
  - src/components/report/ReportShell.tsx
  - src/app/portal/assessment/[id]/report/page.tsx
autonomous: true
requirements:
  - REQ-08-01  # Replace Section 11 hero with five-pillar Peak Living module (portal)
  - REQ-08-02  # Five clickable pillar cards open modal drill-down
  - REQ-08-09  # Preserve marker grid in collapsed disclosure
  - D-01       # Portal interactive
  - D-02       # SSR ownership gate preserved
  - D-03       # Section 11 replaced; marker grid moves to disclosure
  - D-21       # Read paths SSR; props passed in
  - D-22       # Pillar grid layout (3+2 desktop, single column mobile, sort order)
  - D-23       # Modal: centred desktop, bottom-sheet mobile
  - D-24       # Modal a11y contract
  - D-25       # Page heading + intro from page copy
  - D-29       # Premium glass-card visual

must_haves:
  truths:
    - "Visiting /portal/assessment/{id}/report renders the heading + intro sourced from pillar_page_copy"
    - "Five pillar cards render in pillarDefinitions sortOrder (cardiometabolic=0, vo2=1, bodyComposition=2, strength=3, balance=4)"
    - "Each pillar card shows label, score (or em-dash), traffic-light status pill from STATUS_LABEL, and shortSummary"
    - "Clicking a pillar card opens PillarModal with seven sections in locked order"
    - "Modal closes on Escape, backdrop click, and dedicated close button"
    - "Modal traps focus while open and restores focus to triggering element on close"
    - "Body scroll is locked while modal is open"
    - "Below 768px the modal renders as a bottom-sheet; from 768px it renders as a centred dialog"
    - "Below the pillar grid a details element labelled Detailed marker results renders the existing dense marker grid, collapsed by default"
    - "Phase 7 BL-05 SSR ownership gate is preserved verbatim (auth then assessment fetch then hasAccess then redirect)"
    - "Markers and prescriptions are passed in as props (no client-side fetches)"
    - "Markers come from loadReportData server-side so portal and PDF derive identical pillar scores"
  artifacts:
    - path: "src/components/ui/Dialog.tsx"
      provides: "Dialog primitive with focus trap, body-scroll lock, mode auto/centered/bottom-sheet"
      contains: "Dialog"
    - path: "src/components/report/PillarCard.tsx"
      provides: "Per-pillar card UI"
      contains: "PillarCard"
    - path: "src/components/report/PillarModal.tsx"
      provides: "Seven-section drill-down modal"
      contains: "PillarModal"
    - path: "src/components/report/PillarsGrid.tsx"
      provides: "Orchestrator: scores + grid + modal state"
      contains: "PillarsGrid"
    - path: "src/components/report/DetailedMarkerResultsDisclosure.tsx"
      provides: "details wrapper around the existing dense marker grid"
      contains: "DetailedMarkerResultsDisclosure"
    - path: "src/components/report/ReportShell.tsx"
      provides: "Top-level client component for the report body"
      contains: "ReportShell"
    - path: "src/app/portal/assessment/[id]/report/page.tsx"
      provides: "SSR-gated report page extended with pillar/page-copy/prescriptions/markers data load"
      contains: "getPillarDefinitions"
  key_links:
    - from: "src/app/portal/assessment/[id]/report/page.tsx"
      to: "src/lib/pillars/queries.ts"
      via: "Promise.all of getPillarDefinitions, getPillarPageCopy, getPillarPrescriptions"
      pattern: "getPillarDefinitions"
    - from: "src/app/portal/assessment/[id]/report/page.tsx"
      to: "src/lib/report/load-report-data.ts"
      via: "loadReportData(id) for server-side markers"
      pattern: "loadReportData"
    - from: "src/components/report/PillarsGrid.tsx"
      to: "src/lib/pillars/mapping.ts"
      via: "computeAllPillarScores and groupMarkersByPillar"
      pattern: "computeAllPillarScores"
    - from: "src/components/report/PillarModal.tsx"
      to: "src/components/ui/Dialog.tsx"
      via: "Dialog with mode auto"
      pattern: "Dialog"
---

<objective>
Build the portal-side interactive five-pillar module. Replace Section 11's body with a new ReportShell that renders the heading/intro from pillar_page_copy, the PillarsGrid (interactive), and DetailedMarkerResultsDisclosure (collapsed details element wrapping the existing dense marker grid). Add a hand-rolled Dialog primitive with full a11y (focus trap, body-scroll lock, ESC, backdrop close) supporting both centred-dialog (desktop) and bottom-sheet (mobile) modes. Extend the SSR report page to load pillar definitions, page copy, prescriptions, and pre-computed markers (via the existing loadReportData helper) server-side so the portal and PDF share an identical data path (Pitfall #8 fix).

Purpose: Wave 3 delivers REQ-08-01, REQ-08-02, REQ-08-09. Section 11 is replaced (not augmented) per D-03; the existing dense marker grid is preserved underneath as a collapsed disclosure for coaches.

Output: 6 new components plus 1 page edit. Phase 7 BL-05 SSR ownership gate is preserved verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-client-report-design-refresh/08-CONTEXT.md
@.planning/phases/08-client-report-design-refresh/08-RESEARCH.md
@.planning/phases/08-client-report-design-refresh/08-PATTERNS.md
@.planning/phases/08-client-report-design-refresh/08-UI-SPEC.md
@.planning/phases/08-client-report-design-refresh/08-01-SUMMARY.md
@.planning/phases/08-client-report-design-refresh/08-02-SUMMARY.md
@CLAUDE.md
@src/app/portal/assessment/[id]/report/page.tsx
@src/components/sections/Section11.tsx
@src/components/ui/ConfirmDeleteModal.tsx
@src/lib/report/load-report-data.ts
@src/lib/pillars/types.ts
@src/lib/pillars/colors.ts
@src/lib/pillars/mapping.ts
@src/lib/pillars/queries.ts
@src/types/normative.ts

<interfaces>
Plan 02 deliverables (already in tree):
- `src/lib/pillars/types.ts` exports `PillarKey`, `PillarStatus`, `PillarDefinition`, `PillarPageCopy`, `PillarPrescription`, `PillarScoreResult`.
- `src/lib/pillars/colors.ts` exports `TRAFFIC_LIGHT_HEX`, `TRAFFIC_LIGHT_TEXT`, `STATUS_LABEL`, `PILLAR_THRESHOLDS`.
- `src/lib/pillars/mapping.ts` exports `computeAllPillarScores`, `groupMarkersByPillar`, `markerToPillar`, `computePillarScore`.
- `src/lib/pillars/queries.ts` exports `getPillarDefinitions()`, `getPillarPageCopy()`, `getPillarPrescriptions(id)`.

Existing helper that already pre-computes server-side markers (Pitfall #8 fix point):
- `src/lib/report/load-report-data.ts` exports `loadReportData(assessmentId)` which returns `ReportData` containing `markers: ReportMarker[]`. Currently consumed only by the PDF route. Plan 03 wires it into the report page so the portal and PDF use the same path.

Existing analog components to read:
- `src/components/ui/ConfirmDeleteModal.tsx` — seed for ESC handler and backdrop click; lacks focus trap, body lock, mode switching, drag handle.
- `src/components/sections/Section11.tsx` — currently rendered by the report page; lines 30 to 60 contain TIER_DOT/TIER_ROW_BG/TIER_ROW_BORDER/TIER_TEXT colour maps; lines 73 to 86 contain the TierPill component; the body contains the dense category-grouped marker grid that this plan extracts into DetailedMarkerResultsDisclosure.

Phase 7 BL-05 ownership gate (KEEP UNCHANGED in the report page edit):
- `auth.api.getSession` then `redirect('/login')` on unauth
- `db.select().from(assessments).where(eq(assessments.id, id))` then `notFound()` on missing
- `hasAccess(role, userId, row)` then `redirect('/portal')` on cross-client access

Locked copy (verbatim) used in this plan:
- Drill-down chevron screen-reader text on each card: `Open detailed view for {pillar.label}`
- Card status labels come from the `STATUS_LABEL` constant exported by Plan 02.
- Empty score numeral: em dash character.
- Card score suffix: `/100` (smaller, after the numeral).
- Modal close button accessible label: `Close`.
- Modal section labels (locked order from UI-SPEC): no label for header; `What this pillar means`; `Your results`; `What you are doing well`; `What needs attention`; `Score breakdown`; `Recommended plan`.
- Empty-state strings (verbatim from UI-SPEC):
  - Recommended plan empty: `Your coach hasn't written a recommendation for this pillar yet. Check back soon.`
  - Doing well empty: `No standout strengths in this pillar yet — every score below tells you where momentum can build.`
  - Needs attention empty: `Nothing in this pillar is flagged for attention right now — keep it up.`
  - Your results empty: `We don't have any rated markers for this pillar in this assessment yet. Check the detailed marker results below the pillars grid for any raw values your coach has entered.`
- Score breakdown footer line: `Pillar score is the average tier value across {n} rated marker{s}. Markers without normative ranges are excluded.` (singular `marker` when n equals 1, plural `markers` otherwise).
- Updated-by attribution footer: `Updated by {name} · {relative_time}` rendered only when prescription.updatedBy is present.
- Open full plan CTA label: `Open full plan` (only when prescription.fullPlanHref is set).
- Disclosure summary label: `Detailed marker results`.
- Disclosure helper text: `For coaches and curious clients — every rated marker with raw values and ranges.`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Hand-rolled Dialog primitive with focus trap, body lock, and mode switching</name>
  <read_first>
    - src/components/ui/ConfirmDeleteModal.tsx (full file — seed for ESC and backdrop click)
    - .planning/phases/08-client-report-design-refresh/08-PATTERNS.md (Dialog primitive section — analog and contract)
    - .planning/phases/08-client-report-design-refresh/08-RESEARCH.md (Pattern 4 and Pitfall 7 sections — focus trap rationale)
    - .planning/phases/08-client-report-design-refresh/08-UI-SPEC.md (Design System and Layout & Responsive Contract sections — mode contract; Focus & keyboard contract)
  </read_first>
  <files>src/components/ui/Dialog.tsx</files>
  <behavior>
    - Mark with the use-client directive on the first line.
    - Default-export a `Dialog` component with the props described below.
    - Props: `open: boolean`, `onClose: () => void`, `mode?: 'centered' | 'bottom-sheet' | 'auto'` (default `'auto'`), `ariaLabel: string`, `children: React.ReactNode`.
    - When `open === false`, return `null`.
    - When `open === true`:
      - The overlay element has `role="dialog"`, `aria-modal="true"`, and the supplied `aria-label`.
      - Backdrop click invokes `onClose`; clicks inside the panel are stopped from propagating.
      - Pressing Escape invokes `onClose`.
      - The body scroll is locked by setting `document.body.style.overflow = 'hidden'`; the previous value is restored on unmount.
      - On open, the previously-focused element is captured into a ref and focus is moved to the first child marked with `data-autofocus`, falling back to the first focusable element inside the panel.
      - Tab key cycles forward through tabbables inside the panel; Shift+Tab cycles backward; both wrap.
      - On unmount, focus is restored to the captured element.
      - In `mode === 'auto'`, the panel uses Tailwind responsive classes so it sits at the bottom on mobile (rounded top corners) and centred from the `md` breakpoint upward (rounded all corners).
      - In `mode === 'centered'`, the panel is always centred regardless of viewport width.
      - In `mode === 'bottom-sheet'`, the panel always sits at the bottom.
      - In auto and bottom-sheet modes, render a small decorative drag handle at the top of the panel for mobile users (hidden from `md` upward).
      - All transitions respect `prefers-reduced-motion: reduce` (use Tailwind `motion-safe:` variant for any transition utilities; do not animate when reduced motion is requested).
  </behavior>
  <action>
    Create the file `src/components/ui/Dialog.tsx`. The component is a thin wrapper over a fixed-position div with two layered children (overlay backdrop and panel). Implementation notes:

    - Use two `useEffect` hooks: one for body lock plus initial focus plus restoring focus on unmount, and a second for the keydown listener (Escape and Tab focus trap).
    - Capture the previous focus into `previousFocusRef` (a `React.useRef<HTMLElement | null>(null)`). On open the effect saves `document.activeElement` to that ref. The same effect's cleanup function restores `document.body.style.overflow` and calls `previousFocusRef.current?.focus()`.
    - Tabbable selector: `'a[href], button:not([disabled]), textarea:not([disabled]), input:not([disabled]), select:not([disabled]), [tabindex]:not([tabindex="-1"])'`. Filter out hidden elements via `el.offsetParent !== null`.
    - When deriving the overlay layout class from `mode`:
      - `'centered'` produces `flex items-center justify-center`.
      - `'bottom-sheet'` produces `flex items-end justify-center`.
      - `'auto'` produces `flex items-end justify-center md:items-center` so CSS handles the breakpoint switch (no JS match-media call).
    - When deriving the panel class from `mode`:
      - `'centered'`: `max-w-[640px] w-full mx-4 rounded-2xl bg-white p-6 md:p-8 max-h-[90vh] overflow-y-auto`.
      - `'bottom-sheet'`: `w-full rounded-t-2xl bg-white p-6 max-h-[90vh] overflow-y-auto`.
      - `'auto'`: `w-full md:max-w-[640px] md:mx-4 md:rounded-2xl rounded-t-2xl bg-white p-6 md:p-8 max-h-[90vh] overflow-y-auto`.
    - Drag handle: render a 36x4px rounded pill (`mx-auto mb-3 h-1 w-9 rounded-full bg-gray-300`) wrapped in `md:hidden` when mode is not centred, with `aria-hidden="true"`.
    - The panel root wrapper holds the `data-autofocus` lookup and gets `tabIndex={-1}` so a fallback focus target exists.
    - Stop click propagation on the panel; the outer overlay handler invokes `onClose`.
    - The component is intentionally NOT rendered through a portal; PillarsGrid keeps it mounted only when open.

    Do not add any new npm dependency. Do not import Radix or Headless UI.
  </action>
  <verify>
    <automated>cd /Users/jace/Code/peak360 && grep -c "role=\"dialog\"" src/components/ui/Dialog.tsx && grep -c "previousFocusRef" src/components/ui/Dialog.tsx && grep -c "shiftKey" src/components/ui/Dialog.tsx && npx tsc --noEmit 2>&1 | grep -c "Dialog.tsx" | awk '{ exit !($1 == 0) }'</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/ui/Dialog.tsx` exists with `'use client'` on line 1.
    - `grep -c "role=\"dialog\"" src/components/ui/Dialog.tsx` returns 1.
    - `grep -c "aria-modal=\"true\"" src/components/ui/Dialog.tsx` returns 1.
    - `grep -c "Escape" src/components/ui/Dialog.tsx` returns 1.
    - `grep -c "previousFocusRef" src/components/ui/Dialog.tsx` returns at least 2 (declaration plus restore).
    - `grep -c "document.body.style.overflow" src/components/ui/Dialog.tsx` returns at least 2 (set plus restore).
    - `grep -c "shiftKey" src/components/ui/Dialog.tsx` returns 1 (focus-trap backward branch present).
    - `grep -c "items-end" src/components/ui/Dialog.tsx` returns at least 1 and `grep -c "md:items-center" src/components/ui/Dialog.tsx` returns at least 1 (auto mode breakpoint switch).
    - `grep -c "data-autofocus" src/components/ui/Dialog.tsx` returns 1 (initial focus selector).
    - `npx tsc --noEmit` produces no errors mentioning `Dialog.tsx`.
  </acceptance_criteria>
  <done>
    Dialog primitive ships with all UI-SPEC contract behaviour. PillarModal (Task 3) and Plan 04's destructive confirm both consume it.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: PillarCard presentational component</name>
  <read_first>
    - src/lib/pillars/types.ts
    - src/lib/pillars/colors.ts (STATUS_LABEL, TRAFFIC_LIGHT_HEX, TRAFFIC_LIGHT_TEXT)
    - .planning/phases/08-client-report-design-refresh/08-UI-SPEC.md (Layout & Responsive Contract — PillarCard row; Pillar card copy section)
    - .planning/phases/08-client-report-design-refresh/08-PATTERNS.md (PillarCard analog pattern)
  </read_first>
  <files>src/components/report/PillarCard.tsx</files>
  <behavior>
    - Use-client at line 1.
    - Default-export `PillarCard` accepting `{ pillar: PillarDefinition, score: number | null, status: PillarStatus, onOpen: () => void }`.
    - Render a single button (type="button") whose entire surface is the click target. The button calls `onOpen` on click.
    - Set the accessible label on the button to: `Open detailed view for {pillar.label}`.
    - Layout classes (CLAUDE.md tokens — `text-navy`, `text-muted`, `border-border`, `bg-white`):
      - Min height: `min-h-[96px] md:min-h-[160px]`.
      - Padding: `p-4 md:p-6`.
      - Rounded: `rounded-2xl`.
      - Border: dashed grey when `status === 'pending'`, otherwise the standard `border border-border`.
      - Shadow: subtle elevation per UI-SPEC Dimension 2 — `shadow-[0_1px_2px_rgba(15,36,64,0.04),0_4px_12px_rgba(15,36,64,0.06)]`.
      - Hover: `motion-safe:md:hover:-translate-y-0.5 transition-transform duration-150`.
      - Focus visible ring: `focus-visible:ring-2 focus-visible:ring-[rgba(245,166,35,0.25)]`.
    - Visual content stack (top to bottom):
      1. Pillar label heading: `text-[20px] font-semibold leading-[1.25] text-navy`, content `pillar.label`.
      2. Score numeral: `text-navy font-semibold leading-none mt-2` with inline style `fontSize: 'clamp(36px, 4vw, 40px)'` and `fontVariantNumeric: 'tabular-nums'`. When `score === null`, render an em dash; otherwise the number. Append a small span with classes `text-sm font-normal text-muted ml-1 align-baseline` containing `/100`.
      3. Status pill row: `mt-3`. Render a span with `inline-flex items-center gap-1 px-3 py-1 rounded-full text-sm font-semibold`; inline style sets `backgroundColor` to `TRAFFIC_LIGHT_HEX[status]` (or `'transparent'` when pending), `color` to `TRAFFIC_LIGHT_TEXT[status]`, and a `1px solid #e2e8f0` border when pending. Inside the pill, render an 8px round dot via a span with `aria-hidden="true"`, classes `inline-block w-2 h-2 rounded-full`, and inline `backgroundColor` set to `TRAFFIC_LIGHT_TEXT[status]` (or `#94a3b8` for pending). Content text: `STATUS_LABEL[status]`.
      4. Short summary paragraph: `mt-3 text-sm text-muted leading-[1.5]`, content `pillar.shortSummary`.
      5. Drill-down row pinned to the bottom (`mt-auto pt-3`): a small chevron rendered as inline SVG (16x16, currentColor stroke) inside `flex items-center justify-end text-navy/70 group-hover:text-gold transition-colors`. Add a small text hint (only on mobile, hidden from `md` upward) that says `Tap for breakdown`.
    - Reuse the `group` Tailwind variant on the button so the chevron colour change keys off button hover.
    - Do not import Tier colour constants here — the 5-tier palette is reserved for the modal and the disclosure (D-11).
  </behavior>
  <action>
    Create `src/components/report/PillarCard.tsx` exactly as described in the Behavior block. Keep all hex values inside inline style attributes that read from `TRAFFIC_LIGHT_HEX`/`TRAFFIC_LIGHT_TEXT`. The pending dot colour `#94a3b8` is a literal in this file (it is the only pending-specific hex not in the colours module — acceptable as a minor literal but document it with a one-line comment referencing `TRAFFIC_LIGHT_TEXT.pending` rationale).
  </action>
  <verify>
    <automated>cd /Users/jace/Code/peak360 && grep -c "TRAFFIC_LIGHT_HEX" src/components/report/PillarCard.tsx && grep -c "STATUS_LABEL" src/components/report/PillarCard.tsx && grep -c "score === null" src/components/report/PillarCard.tsx && grep -c "border-dashed" src/components/report/PillarCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File exists with use-client directive.
    - `grep -c "export default function PillarCard" src/components/report/PillarCard.tsx` returns 1.
    - `grep -c "TRAFFIC_LIGHT_HEX\[status\]" src/components/report/PillarCard.tsx` returns 1.
    - `grep -c "STATUS_LABEL\[status\]" src/components/report/PillarCard.tsx` returns 1.
    - `grep -c "Open detailed view for" src/components/report/PillarCard.tsx` returns 1.
    - `grep -c "score === null" src/components/report/PillarCard.tsx` returns 1.
    - `grep -c "border-dashed" src/components/report/PillarCard.tsx` returns 1.
    - `grep -c "min-h-\[96px\]" src/components/report/PillarCard.tsx` returns 1.
    - `grep -c "/100" src/components/report/PillarCard.tsx` returns 1.
    - `npx tsc --noEmit` produces no errors mentioning `PillarCard.tsx`.
  </acceptance_criteria>
  <done>
    PillarCard renders the four status variants. The whole card is the click target. Tailwind tokens carry the chrome; only TRAFFIC_LIGHT_HEX and TRAFFIC_LIGHT_TEXT carry status colour.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: PillarModal — seven-section drill-down</name>
  <read_first>
    - src/components/sections/Section11.tsx (full file — TIER_DOT colour map at lines 30 to 60, TierPill at lines 73 to 86)
    - src/lib/pillars/types.ts, src/lib/pillars/colors.ts, src/lib/pillars/mapping.ts
    - src/types/normative.ts (TIER_LABELS)
    - .planning/phases/08-client-report-design-refresh/08-UI-SPEC.md (Modal sections — locked order; Empty states; Recommended plan; Layout — bottom-sheet vs centred)
    - .planning/phases/08-client-report-design-refresh/08-PATTERNS.md (PillarModal analog and tier-pill reuse)
  </read_first>
  <files>src/components/report/PillarModal.tsx</files>
  <behavior>
    - Use-client at line 1. Wrap content with the Dialog component using `mode="auto"`.
    - Default-export `PillarModal` with props `{ open, onClose, definition: PillarDefinition, prescription: PillarPrescription | null, score: number | null, status: PillarStatus, primaryMarkers: ReportMarker[], supportingMarkers: ReportMarker[] }`.
    - Render a sticky top-right close button: 44x44 hit area, accessible label `Close`, `data-autofocus` so initial focus moves there.
    - Section 1 (Header — no heading label): pillar.label as h2 (`text-[28px] font-semibold leading-[1.15] text-navy` with mobile downscale via `clamp(24px, 4vw, 28px)`), score numeral 48px tabular-nums (em dash when null), `/100` suffix, status pill (same shape as PillarCard's pill — reuse the inline-style pattern, do not import the card).
    - Section 2 — heading `What this pillar means` (`text-[20px] font-semibold leading-[1.25] text-navy`), body paragraph from `definition.plainMeaning`.
    - Section 3 — heading `Your results`. Body: rated primary markers rendered in a list with `marker.label`, `marker.value` plus unit, and a TierPill. When zero rated primary markers, render the `Your results` empty-state copy verbatim.
    - Section 4 — heading `What you are doing well`. Body: rated primary markers with tier in great or elite, label only, prefixed with a tier-coloured dot (`TIER_DOT[tier]`). When empty, render the locked empty copy.
    - Section 5 — heading `What needs attention`. Body: rated primary markers with tier in cautious or poor, label only, tier-coloured dot. Locked empty copy when none.
    - Section 6 — heading `Score breakdown`. Body: a table (`<table className="w-full text-sm">`) with one row per rated primary marker — columns: label, value+unit, TierPill, tier value contribution (`+20` to `+100`, derived from a local `TIER_VALUE` map). Below the table render the locked footer line with correct singular/plural for `marker(s)`. When `supportingMarkers.length > 0` (effectively only Cardiometabolic), append a sub-block with heading `Supporting markers`, the locked transparency note, and a list of the supporting markers (label, value+unit, TierPill).
    - Section 7 — heading `Recommended plan`. When prescription is null, render the locked empty copy. Otherwise render the summary paragraph, optional bullets list, optional `Open full plan` link button (`px-4 py-2 bg-navy text-white text-sm font-semibold rounded-lg hover:bg-navy/90`), and an `Updated by {name} · {relative_time}` attribution footer when both updatedBy and updatedAt are present.
    - Add a local `relativeTime(epochMs: number): string` helper covering minutes/hours/days/months/years buckets with correct singular/plural.
    - Lift TIER_DOT (5-entry record) and TIER_VALUE (5-entry record with elite=100, great=80, normal=60, cautious=40, poor=20) directly into the file. Lift TierPill verbatim from Section11 lines 73 to 86.
    - Anti-pattern: do not reuse `TRAFFIC_LIGHT_HEX` for marker rows; the 5-tier palette is reserved for marker-row colour (D-11).
  </behavior>
  <action>
    Create `src/components/report/PillarModal.tsx`. Reuse the PillarCard's pill rendering shape for the header pill (inline style from `TRAFFIC_LIGHT_HEX[status]` / `TRAFFIC_LIGHT_TEXT[status]`). Reuse Section11's `TierPill` shape verbatim by lifting it into this file (Section11 will be deprecated; we re-home the maps and the pill). Empty-state strings, section headings, footer line, attribution string, and CTA label all come from the Interfaces — Locked copy block above. Use the same apostrophe character (regular `'`) that appears in 08-UI-SPEC.md so unit-test grep gates work without typographic-quote issues.
  </action>
  <verify>
    <automated>cd /Users/jace/Code/peak360 && grep -c "Recommended plan" src/components/report/PillarModal.tsx && grep -c "Score breakdown" src/components/report/PillarModal.tsx && grep -c "What needs attention" src/components/report/PillarModal.tsx && grep -c "What you are doing well" src/components/report/PillarModal.tsx && grep -c "Your results" src/components/report/PillarModal.tsx && grep -c "What this pillar means" src/components/report/PillarModal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File exists with use-client directive.
    - All six labelled section headings present (each individual grep above returns 1; the unlabelled header section is the seventh).
    - `grep -c "Your coach hasn't written a recommendation" src/components/report/PillarModal.tsx` returns 1.
    - `grep -c "No standout strengths in this pillar" src/components/report/PillarModal.tsx` returns 1.
    - `grep -c "Nothing in this pillar is flagged" src/components/report/PillarModal.tsx` returns 1.
    - `grep -c "We don't have any rated markers" src/components/report/PillarModal.tsx` returns 1.
    - `grep -c "Open full plan" src/components/report/PillarModal.tsx` returns 1.
    - `grep -c "data-autofocus" src/components/report/PillarModal.tsx` returns 1.
    - `grep -c "Supporting markers" src/components/report/PillarModal.tsx` returns 1.
    - `grep -c "TIER_VALUE" src/components/report/PillarModal.tsx` returns at least 1.
    - `grep -c "Pillar score is the average" src/components/report/PillarModal.tsx` returns 1.
    - `grep -c "Updated by" src/components/report/PillarModal.tsx` returns 1.
    - `npx tsc --noEmit` produces no errors mentioning `PillarModal.tsx`.
  </acceptance_criteria>
  <done>
    PillarModal renders all seven sections with locked copy. Supporting block only renders when supportingMarkers is non-empty (effectively Cardiometabolic only). Dialog auto mode handles the breakpoint switch.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: PillarsGrid orchestrator and DetailedMarkerResultsDisclosure (lift from Section 11) and ReportShell</name>
  <read_first>
    - src/components/sections/Section11.tsx (full file — must be read end to end so the disclosure component captures the existing dense category-grouped marker grid and any helper functions it relies on)
    - src/lib/pillars/mapping.ts (computeAllPillarScores, groupMarkersByPillar)
    - src/lib/pillars/types.ts
    - .planning/phases/08-client-report-design-refresh/08-UI-SPEC.md (Layout & Responsive Contract — PillarsGrid; Detailed marker results disclosure copy)
    - .planning/phases/08-client-report-design-refresh/08-PATTERNS.md (PillarsGrid, DetailedMarkerResultsDisclosure, Section 11 replacement)
    - .planning/phases/08-client-report-design-refresh/08-CONTEXT.md (D-22 — sort order; D-03 — collapsed disclosure)
  </read_first>
  <files>src/components/report/PillarsGrid.tsx, src/components/report/DetailedMarkerResultsDisclosure.tsx, src/components/report/ReportShell.tsx</files>
  <behavior>
    - PillarsGrid (use-client): props `{ definitions: PillarDefinition[], prescriptions: PillarPrescription[], markers: ReportMarker[] }`. Compute `computeAllPillarScores(markers)` and `groupMarkersByPillar(markers)` once per render. Render five PillarCards in `definitions` order (already sorted by sortOrder from queries.ts). Layout: `grid grid-cols-1 gap-4 md:grid-cols-3 md:gap-6`. With five children at `md:grid-cols-3`, CSS naturally wraps into a 3+2 layout. Own `openKey: PillarKey | null` state. Render PillarModal once with the active pillar's definition, prescription, score, status, primaryMarkers, supportingMarkers; pass `open={true}` only when openKey is non-null and unmount when null.
    - DetailedMarkerResultsDisclosure (use-client): props `{ markers: ReportMarker[] }`. Render a native HTML details element collapsed by default. Summary text: `Detailed marker results` plus a helper line beneath: `For coaches and curious clients — every rated marker with raw values and ranges.`. Body: lift the existing dense category-grouped marker grid out of Section11 verbatim, replacing any local marker-computation logic with direct usage of the `markers` prop. Lift any helpers (TIER_DOT, TIER_ROW_BG, TIER_ROW_BORDER, TIER_TEXT, TierPill, the category grouping helper, range bar / referral flag rendering helpers if any) into the file or a small co-located helper module — do not leave dangling imports from Section11. The on-screen output of the lifted block must remain visually equivalent to what Section11 currently renders inside its category-grouped section.
    - ReportShell (use-client): props `{ assessmentId, definitions, pageCopy: PillarPageCopy | null, prescriptions, markers }`. Render heading and intro from pageCopy (with defensive fallback to UI-SPEC seed copy if pageCopy is null), then PillarsGrid, then DetailedMarkerResultsDisclosure. Heading element: h2, `text-navy font-semibold leading-[1.15]` with `clamp(24px, 4vw, 28px)`. Intro: `mt-3 text-base leading-[1.5] text-muted max-w-3xl`.
  </behavior>
  <action>
    Three files. Implementation discipline:

    **PillarsGrid:** straight orchestrator. Call `computeAllPillarScores` and `groupMarkersByPillar` at the top of the component. Use a single grid container as described. Track `openKey` with `useState<PillarKey | null>(null)`. Find the active definition and prescription with `Array.find` over the props.

    **DetailedMarkerResultsDisclosure (the heaviest of the three):** open `src/components/sections/Section11.tsx`, identify the section that currently renders the category-grouped marker grid (the block iterating over markers grouped by category), and lift it intact into this new component. Specifically:
    - Lift TIER_DOT, TIER_ROW_BG, TIER_ROW_BORDER, TIER_TEXT colour maps verbatim.
    - Lift TierPill verbatim (it is the same one PillarModal also lifts; living in two places is acceptable for now since Section11 itself will be deprecated when the report page edit lands).
    - Lift any range-bar / referral-flag helper functions Section11 uses inside the grid.
    - Replace any `useEffect` data-fetch / rating computation Section11 currently performs with direct use of the `markers` prop. Markers arrive already evaluated (`marker.tier`, `marker.value`, `marker.resolvedStandards` already populated by `loadReportData`).
    - The disclosure shell wraps the lifted JSX in a native HTML details element with the locked summary text. Add a small caret-glyph SVG inside the summary that rotates on `[open]` via `group-open:rotate-90`.
    - Do not change the visual style of the lifted grid — same grouping order, same row layout, same per-tier colours.

    **ReportShell:** glue. Render heading, intro, PillarsGrid, DetailedMarkerResultsDisclosure in that order. Defensive fallback constants for heading and intro inline to this file (the same verbatim values used as seed in Plan 01).

    Do NOT modify `src/components/sections/Section11.tsx` in this task — Task 5 (the page edit) is what stops importing it. Section11 stays in the codebase as dead code for one wave; it can be deleted later.
  </action>
  <verify>
    <automated>cd /Users/jace/Code/peak360 && grep -c "computeAllPillarScores" src/components/report/PillarsGrid.tsx && grep -c "groupMarkersByPillar" src/components/report/PillarsGrid.tsx && grep -c "Detailed marker results" src/components/report/DetailedMarkerResultsDisclosure.tsx && grep -c "ReportShell" src/components/report/ReportShell.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/report/PillarsGrid.tsx` exists with use-client; `grep -c "useState<PillarKey | null>" src/components/report/PillarsGrid.tsx` returns 1; `grep -c "computeAllPillarScores" src/components/report/PillarsGrid.tsx` returns 1; `grep -c "PillarModal" src/components/report/PillarsGrid.tsx` returns at least 2 (import plus render).
    - File `src/components/report/DetailedMarkerResultsDisclosure.tsx` exists with use-client and contains a details element; `grep -c "<details" src/components/report/DetailedMarkerResultsDisclosure.tsx` returns 1; `grep -c "Detailed marker results" src/components/report/DetailedMarkerResultsDisclosure.tsx` returns 1; `grep -c "For coaches and curious clients" src/components/report/DetailedMarkerResultsDisclosure.tsx` returns 1; `grep -c "TIER_DOT\|TIER_ROW_BG" src/components/report/DetailedMarkerResultsDisclosure.tsx` returns at least 1 (lifted from Section11).
    - File `src/components/report/ReportShell.tsx` exists with use-client; `grep -c "PillarsGrid" src/components/report/ReportShell.tsx` returns 1; `grep -c "DetailedMarkerResultsDisclosure" src/components/report/ReportShell.tsx` returns 1; `grep -c "FALLBACK_HEADING\|The Peak Living Pillars" src/components/report/ReportShell.tsx` returns at least 1 (defensive fallback).
    - `npx tsc --noEmit` produces no new errors.
  </acceptance_criteria>
  <done>
    Three components in place. The dense marker grid has been migrated into the disclosure component without any visual or data-shape regression. PillarsGrid wires the orchestrator-level computations once and owns the modal state.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Extend the SSR report page — load pillar data and pre-computed markers; replace Section 11 with ReportShell</name>
  <read_first>
    - src/app/portal/assessment/[id]/report/page.tsx (full 97-line file — Phase 7 BL-05 ownership gate is at lines 37 to 69 and must be preserved verbatim)
    - src/lib/pillars/queries.ts (new in Plan 02)
    - src/lib/report/load-report-data.ts (existing — returns ReportData with markers)
    - src/lib/pdf/types.ts (ReportData shape)
    - .planning/phases/08-client-report-design-refresh/08-PATTERNS.md (report-page additive pattern; Pitfall #8 — markers must come from a shared server-side path)
    - .planning/phases/08-client-report-design-refresh/08-CONTEXT.md (D-21 — SSR; D-02 — gate preserved; D-03 — replace Section 11)
  </read_first>
  <files>src/app/portal/assessment/[id]/report/page.tsx</files>
  <behavior>
    - Phase 7 BL-05 ownership gate (auth, fetch row, hasAccess) is preserved character-for-character at lines 37 to 69.
    - After the gate, in parallel via Promise.all: call `getPillarDefinitions()`, `getPillarPageCopy()`, `getPillarPrescriptions(id)`, and `loadReportData(id)`.
    - The returned data is passed as props into ReportShell instead of `<Section11 assessmentId={id} />`.
    - Existing page chrome (h1 with `Assessment · {dateLabel}` and the `Download PDF` link) remains unchanged.
    - The Section11 import is removed from this file.
  </behavior>
  <action>
    Edit `src/app/portal/assessment/[id]/report/page.tsx`:

    1. Remove the line `import Section11 from '@/components/sections/Section11';` (currently line 7).
    2. Add `import ReportShell from '@/components/report/ReportShell';`.
    3. Add `import { getPillarDefinitions, getPillarPageCopy, getPillarPrescriptions } from '@/lib/pillars/queries';` and `import { loadReportData } from '@/lib/report/load-report-data';`.
    4. After the BL-05 ownership gate finishes (immediately after the `if (!hasAccess(...)) redirect('/portal');` line), insert a Promise.all that resolves all four reads. Approximate shape (executor adapts naming):

    ```
    const [definitions, pageCopy, prescriptions, reportData] = await Promise.all([
      getPillarDefinitions(),
      getPillarPageCopy(),
      getPillarPrescriptions(id),
      loadReportData(id),
    ]);
    ```

    5. Replace the JSX line that renders Section11 with a ReportShell render that passes:
       - `assessmentId={id}`
       - `definitions={definitions}`
       - `pageCopy={pageCopy}`
       - `prescriptions={prescriptions}`
       - `markers={reportData.markers}`

    6. Do NOT modify the BL-05 gate. Do NOT add try/catch around the gate. Do NOT bypass `hasAccess`. Do NOT add any new client-side fetch.

    7. Confirm via grep that the gate block is intact:
       - `grep -c "redirect('/login')" src/app/portal/assessment/[id]/report/page.tsx` returns 1.
       - `grep -c "notFound()" src/app/portal/assessment/[id]/report/page.tsx` returns 1.
       - `grep -c "redirect('/portal')" src/app/portal/assessment/[id]/report/page.tsx` returns 1.
       - `grep -c "hasAccess" src/app/portal/assessment/[id]/report/page.tsx` returns at least 2 (declaration plus call site, both preserved).
  </action>
  <verify>
    <automated>cd /Users/jace/Code/peak360 && grep -c "import Section11" "src/app/portal/assessment/[id]/report/page.tsx" | awk '{ exit !($1 == 0) }' && grep -c "ReportShell" "src/app/portal/assessment/[id]/report/page.tsx" && grep -c "getPillarDefinitions\|getPillarPageCopy\|getPillarPrescriptions" "src/app/portal/assessment/[id]/report/page.tsx" && grep -c "loadReportData" "src/app/portal/assessment/[id]/report/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import Section11" src/app/portal/assessment/\[id\]/report/page.tsx` returns 0 (Section11 import removed).
    - `grep -c "ReportShell" src/app/portal/assessment/\[id\]/report/page.tsx` returns at least 2 (import plus JSX site).
    - `grep -c "getPillarDefinitions\|getPillarPageCopy\|getPillarPrescriptions" src/app/portal/assessment/\[id\]/report/page.tsx` returns at least 3.
    - `grep -c "loadReportData" src/app/portal/assessment/\[id\]/report/page.tsx` returns at least 2 (import plus call).
    - `grep -c "Promise.all" src/app/portal/assessment/\[id\]/report/page.tsx` returns 1.
    - BL-05 gate preserved: `grep -c "redirect('/login')" src/app/portal/assessment/\[id\]/report/page.tsx` returns 1; `grep -c "notFound()" src/app/portal/assessment/\[id\]/report/page.tsx` returns 1; `grep -c "redirect('/portal')" src/app/portal/assessment/\[id\]/report/page.tsx` returns 1; `grep -c "hasAccess(" src/app/portal/assessment/\[id\]/report/page.tsx` returns at least 1.
    - `grep -c "<Section11" src/app/portal/assessment/\[id\]/report/page.tsx` returns 0.
    - `npx tsc --noEmit` produces no errors.
    - Functional smoke test (manual or via existing security tests): visiting `/portal/assessment/{id}/report` for a valid id renders the new heading from pillar_page_copy and five pillar cards. Visiting it for an unauthorised id still 302s to `/portal` (BL-05 preserved).
  </acceptance_criteria>
  <done>
    Report page loads pillar data and pre-computed markers in one Promise.all, passes everything to ReportShell, and preserves the Phase 7 BL-05 ownership gate. Section 11 is no longer imported by this page.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → SSR report page | Untrusted client sends GET with assessmentId in URL. Phase 7 BL-05 ownership gate (preserved) is the access boundary. |
| SSR → DB | Server-side reads via `getPillarDefinitions`, `getPillarPageCopy`, `getPillarPrescriptions`, `loadReportData`. All queries parameterised via Drizzle. |
| Server → Client island | ReportShell, PillarsGrid, PillarModal receive only the data the SSR resolved post-gate. No client-side fetch in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Information Disclosure | Cross-client IDOR via direct URL access to `/portal/assessment/{id}/report` | mitigate | Phase 7 BL-05 SSR ownership gate is preserved verbatim. Pillar reads happen AFTER `hasAccess`. Existing security regression tests continue to guard. Plan 04 adds a new regression test specifically covering pillar prescriptions inside the gated branch. |
| T-08-12 | Information Disclosure | Score drift between portal and PDF (Pitfall #8) | mitigate | Both surfaces consume `loadReportData(id)` for markers (single source of truth) and `computeAllPillarScores` for pillar rollups (single pure-function source from Plan 02). Verified by grep gate that the report page imports `loadReportData`. |
| T-08-13 | Tampering | Admin-authored prescription content rendered into the modal | mitigate | All admin-authored fields (`summary`, `bullets`, `fullPlanHref`) are rendered via React text children only — no raw-HTML injection prop is used. The `fullPlanHref` is rendered as a plain anchor; URL scheme validation lives at the API write boundary in Plan 04 (which rejects non-`http`/`https` schemes before persistence). |
| T-08-14 | Denial of Service | Modal focus trap regression escapes outside the panel (Pitfall #7) | mitigate | Dialog primitive implements bidirectional Tab cycling with explicit wrap; manual smoke test exercises the trap. WAI-ARIA Authoring Practices Modal Dialog pattern is the cited authority. |
| T-08-15 | Repudiation | Modal renders prescription updatedBy and updatedAt accurately | accept | Display-only. Audit trail is captured at the API write boundary (Plan 04). Display values come from the same row written by the audited PATCH. |
</threat_model>

<verification>
After all tasks complete, run from the repo root:

```bash
# Components exist and have use-client
test -f src/components/ui/Dialog.tsx && \
test -f src/components/report/PillarCard.tsx && \
test -f src/components/report/PillarModal.tsx && \
test -f src/components/report/PillarsGrid.tsx && \
test -f src/components/report/DetailedMarkerResultsDisclosure.tsx && \
test -f src/components/report/ReportShell.tsx && \
echo OK_FILES

# BL-05 gate intact
grep -c "redirect('/login')" "src/app/portal/assessment/[id]/report/page.tsx" | grep -q 1 && \
grep -c "notFound()" "src/app/portal/assessment/[id]/report/page.tsx" | grep -q 1 && \
grep -c "redirect('/portal')" "src/app/portal/assessment/[id]/report/page.tsx" | grep -q 1 && \
echo OK_BL05

# Section 11 no longer imported by the report page
test "$(grep -c 'import Section11' src/app/portal/assessment/\[id\]/report/page.tsx)" = "0" && echo OK_REPLACED

# Single source of truth for traffic-light hex outside the colours module
test "$(grep -rn '#10b981' src/lib/pillars/ src/components/report/ | wc -l | tr -d ' ')" -le "2" && echo OK_SSOT_REPORT

# Build compiles
npx tsc --noEmit && echo OK_TYPECHECK
```

All five OK markers must print.
</verification>

<success_criteria>
- 6 new components under `src/components/ui/` and `src/components/report/`.
- Report page extended with SSR Promise.all data load.
- Phase 7 BL-05 SSR ownership gate preserved verbatim.
- Section 11 no longer imported by the report page.
- All grep gates and TypeScript compile pass.
- D-22 sort order honoured (definitions arrive sorted by `sortOrder`).
- Pitfall #8 fix in place (markers come from `loadReportData`).
</success_criteria>

<output>
After completion, create `.planning/phases/08-client-report-design-refresh/08-03-SUMMARY.md` with:
- Files created (6 components) and modified (1 page).
- Manual smoke test result: visited `/portal/assessment/{id}/report` for at least one assessment; took screenshots of card grid + open modal (mobile + desktop) if practical.
- Confirmation that BL-05 gate is preserved (grep counts match).
- Notes on any Section11 helpers that were lifted into the disclosure component.
- Followup item: Section11.tsx is now dead code; deletion suggested in a future quick task.
</output>
