---
phase: 07-multi-tenant-auth-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/Sidebar.tsx
autonomous: true
requirements:
  - REQ-7.3  # Role-based sidebar nav (D-12, D-13)
tags:
  - sidebar
  - role-gating
  - auth-ui

must_haves:
  truths:
    - "Sidebar nav for client = Dashboard + My Assessments only (no Clients link, no Admin link)"
    - "Sidebar nav for coach = Dashboard + Assessments + Clients (no Admin link)"
    - "Sidebar nav for admin = Dashboard + Assessments + Clients + Admin"
    - "When useSession() is still resolving, only Dashboard renders — privileged items never flash for non-admins"
    - "The Admin link is not present in the DOM for non-admin sessions"
  artifacts:
    - path: src/components/layout/Sidebar.tsx
      provides: "Role-derived nav items via useSession()"
      contains: "useSession"
  key_links:
    - from: "src/components/layout/Sidebar.tsx"
      to: "src/lib/auth-client.ts authClient.useSession"
      via: "import + call inside the component"
      pattern: "authClient\\.useSession"
---

<objective>
Refactor `Sidebar.tsx` so its nav items derive from the authenticated user's role at render time, replacing the static module-scope `NAV_ITEMS` array. After this plan a client logged in to /portal sees only Dashboard + My Assessments; a coach sees Dashboard + Assessments + Clients; an admin sees Dashboard + Assessments + Clients + Admin. The Admin link is not present in the DOM for non-admin sessions, including during the initial loading window.

Purpose: Closes 07-SPEC req 3 ("Role-based sidebar nav") and the D-12 acceptance criterion that privileged items must not flash.

Output: Modified `src/components/layout/Sidebar.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-multi-tenant-auth-ux/07-SPEC.md
@.planning/phases/07-multi-tenant-auth-ux/07-CONTEXT.md
@.planning/phases/07-multi-tenant-auth-ux/07-UI-SPEC.md
@.planning/phases/07-multi-tenant-auth-ux/07-PATTERNS.md
@src/components/layout/Sidebar.tsx
@src/lib/auth-client.ts
@src/app/portal/page.tsx

<interfaces>
From src/lib/auth-client.ts:
```typescript
// authClient is a createAuthClient() instance from better-auth/react.
// useSession returns { data, isPending, error } where data?.user.role is 'admin' | 'coach' | 'client' | undefined.
authClient.useSession(): { data?: { user: { id, email, name, role: 'admin' | 'coach' | 'client' } } | null; isPending: boolean };
```

Existing Sidebar shape (from src/components/layout/Sidebar.tsx):
- Module-scope `NAV_ITEMS` array of `{ label, href, icon, matchPaths }` items.
- An `isActive(href, matchPaths, currentPath)` resolver.
- An `Admin` block (lines ~119-141) currently rendered unconditionally.
- Item visual classes use `px-3 py-2.5` literally — UI-SPEC §Spacing executor instruction: KEEP these literal values, do not refactor.
- Active-state classes use `bg-gold/15` background + active dot indicator (gold). Inherit verbatim for any conditional item.

The same role-gating pattern is already used in src/app/portal/page.tsx:
```typescript
const { data: sessionData } = authClient.useSession();
const userRole = sessionData?.user?.role;
{(userRole === 'coach' || userRole === 'admin') && <Block />}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Derive Sidebar nav items from useSession() role per D-13 + D-12</name>
  <files>src/components/layout/Sidebar.tsx</files>
  <read_first>
    - src/components/layout/Sidebar.tsx (current static NAV_ITEMS + Admin block)
    - src/app/portal/page.tsx (lines 17-18, 254 — established useSession() role-gating pattern)
    - 07-PATTERNS.md "src/components/layout/Sidebar.tsx (modify)" section
    - 07-UI-SPEC.md §Sidebar copywriting + §Component Inventory + §Reference Anchors (BINDING)
  </read_first>
  <action>
    Refactor `src/components/layout/Sidebar.tsx`:

    1. Import `authClient` from `@/lib/auth-client` and `useMemo` from `react` (if not already imported).

    2. Inside the component body, call `const { data: sessionData } = authClient.useSession();` and `const role = sessionData?.user?.role;`.

    3. Remove the module-scope `NAV_ITEMS` constant. Replace it with named per-item constants — or with a derived `navItems` array via `useMemo` — so the order matches D-13:
       - `client` → `[Dashboard, My Assessments]` where "My Assessments" `href` is `/portal` (D-13: "the My Assessments link points to /portal because their dashboard already lists their owned assessments").
       - `coach` → `[Dashboard, Assessments, Clients]`.
       - `admin` → `[Dashboard, Assessments, Clients, Admin]`.
       - `role === undefined` (still loading) → `[Dashboard]` only. Privileged items MUST NOT flash for non-admins (D-12 acceptance criterion).

    4. Render the conditional Admin link by wrapping the existing Admin block (currently lines ~119-141) in `{role === 'admin' && (...)}`. Use strict equality, NOT `role !== 'client'`, to honour D-12 (the loading state has `role === undefined`, which must NOT pass an `!== 'client'` check).

    5. Visual classes: every conditional nav item inherits the existing `px-3 py-2.5` plus the active-state classes (`bg-gold/15` + active dot) literally. UI-SPEC §Spacing — DO NOT change the existing literal `px-3 py-2.5` and DO NOT carry it into any new component (this file only).

    6. Copy contract (UI-SPEC §Sidebar — BINDING verbatim):
       - Client: `Dashboard`, `My Assessments`
       - Coach: `Dashboard`, `Assessments`, `Clients`
       - Admin: `Dashboard`, `Assessments`, `Clients`, `Admin`
       - Logout button label: `Logout` (already in place — KEEP).

    7. The `Logout` button, footer / branding, and all other visual chrome OUTSIDE the nav-items array remain unchanged.

    8. DO NOT introduce a new `useRoleNav()` hook or a separate file — the role-derivation lives inline in this component (Claude's Discretion noted in CONTEXT, simplest path chosen).

    Decision IDs implemented: D-12 (loading state — privileged items don't flash), D-13 (role → nav-items mapping), D-11 (UI gating is defence-in-depth, server is source of truth — no behaviour change required here).
  </action>
  <verify>
    <automated>grep -c "authClient.useSession" src/components/layout/Sidebar.tsx &amp;&amp; grep -c "role === 'admin'" src/components/layout/Sidebar.tsx &amp;&amp; grep -E "role !== 'client'" src/components/layout/Sidebar.tsx | wc -l | grep -q '^ *0$' &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "authClient.useSession" src/components/layout/Sidebar.tsx` returns ≥ 1
    - `grep -c "role === 'admin'" src/components/layout/Sidebar.tsx` returns ≥ 1
    - `grep -cE "role !== 'client'" src/components/layout/Sidebar.tsx` returns 0 (D-12: must not use negative checks because loading state would slip through)
    - `grep -c "My Assessments" src/components/layout/Sidebar.tsx` returns 1
    - `grep -c "px-3 py-2.5" src/components/layout/Sidebar.tsx` returns ≥ 1 (existing literal preserved)
    - `npx tsc --noEmit` exits 0
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Logging in as each of admin / coach / client renders the exact nav set listed in UI-SPEC §Sidebar; the Admin link is absent from the DOM for coach + client sessions including during the initial useSession() loading window.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Sidebar component | Untrusted role value if useSession() is spoofed; UI gating is defence-in-depth only — server enforces real authority on /api/admin/* (plan 07-07). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Information Disclosure | Sidebar role-flash exposing Admin nav before useSession() resolves | mitigate | D-12: render Dashboard immediately, conditionally hydrate role-specific items only when `role === 'admin'` (strict equality). Acceptance grep enforces `role !== 'client'` is NOT used. |
| T-07-08 | Elevation of Privilege | Client sees Admin link via DOM tampering and clicks it | accept | The /portal/admin/* server routes enforce role server-side (plan 07-07 D-11). UI hiding is cosmetic per SPEC Constraint "API authority". |
| T-07-09 | Tampering | Browser extension forces `role` to 'admin' in component state | accept | The /api/admin/* endpoints re-validate via `auth.api.getSession({ headers })` and return 403 for non-admins; UI cannot grant access. |
</threat_model>

<verification>
- `npm run build` succeeds
- Manual: log in as the seeded admin (`me@jace.au` from prod data) → Sidebar shows Dashboard + Assessments + Clients + Admin
- Manual: log in as a coach → Sidebar shows Dashboard + Assessments + Clients (no Admin link in DOM)
- Manual: log in as a client → Sidebar shows Dashboard + My Assessments (no Clients, no Admin)
- DOM inspection (DevTools → Elements): with a coach session, search the rendered HTML for `/portal/admin` — expect zero matches
</verification>

<success_criteria>
- All 5 must_haves.truths are observable across all three roles
- Sidebar.tsx artifact contains the useSession() call
- key_link wired (Sidebar.tsx → authClient.useSession)
- TypeScript compiles cleanly
- All threats dispositioned
</success_criteria>

<output>
After completion, create `.planning/phases/07-multi-tenant-auth-ux/07-02-SUMMARY.md` capturing the Sidebar role-derivation pattern for downstream plans (07-05 dashboard grouping, 07-08 admin home placeholder both reuse the same `useSession()` pattern).
</output>
