---
phase: 09-brand-language-alignment-across-portal-dashboard-assessment-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/lib/fonts.ts
  - src/app/layout.tsx
  - src/app/portal/layout.tsx
  - src/app/login/layout.tsx
  - src/app/reset-password/layout.tsx
  - src/components/layout/Sidebar.tsx
  - src/components/layout/Header.tsx
  - src/components/layout/ProgressBar.tsx
  - src/components/layout/NavigationButtons.tsx
  - src/app/login/page.tsx
  - src/app/reset-password/page.tsx
  - src/components/ui/MonoEyebrow.tsx
autonomous: true
requirements:
  - UI-SPEC-AC-1
  - UI-SPEC-AC-2
  - UI-SPEC-AC-3
  - UI-SPEC-AC-4
  - UI-SPEC-AC-5
  - UI-SPEC-AC-6
  - UI-SPEC-AC-7
  - UI-SPEC-AC-8
  - UI-SPEC-AC-9
  - UI-SPEC-AC-10
must_haves:
  truths:
    - "All Phase 9 dark surfaces (login, reset-password, portal shell) render with `--color-bg` canvas and cream `--color-text` body text — no white/light-surface flash"
    - "Body text on /login, /reset-password, /portal, and nested portal routes resolves to Inter Tight; mono eyebrows and meta resolve to JetBrains Mono"
    - "`--color-navy`, `--color-gold` legacy tokens remain defined in globals.css with original hex values (Phase 8 report + Phase 5 PDF unbroken)"
    - "/login and /reset-password show a gold-brand 11px mono eyebrow above a 40px Display title (mobile 32px) inside a centred 360px-max card"
    - "Sidebar renders with `--color-bg-2` background, gold-brand 2px left border on active item, mono PEAK360 / PORTAL logo eyebrow, mono role chip"
    - "ProgressBar renders 4px gold-brand fill on `--color-line` track inside `--color-bg-2` 56px sticky bar"
    - "NavigationButtons auto-save indicator renders three mono variants: SAVED · {time} / SAVING… / UNSAVED CHANGES with gold-brand pulse dot on Saving"
    - "Header renders 56px `--color-bg-2` bar with `text-gold-brand` `360` and `text-text-dim` logout — no inline navy hex literal"
    - "`npm run build` exits 0 after all changes; Phase 8 report at /portal/assessment/[id]/report still renders inside a light card surface"
    - "Every icon-only interactive element on touched surfaces (sidebar hamburger, ProgressBar/Header logout) has an explicit aria-label"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Phase 9 token additions (--color-bg/-2/-3, --color-text/-dim/-faint, --color-gold-brand, --color-champagne, --color-line/-2, --color-danger, --color-status-good, --font-mono), --font-sans rebind, .theme-dark utility, @keyframes pulse-gold, gold-brand focus ring"
      contains: "--color-bg-3"
    - path: "src/lib/fonts.ts"
      provides: "Inter_Tight rebind + JetBrains_Mono export with --font-mono variable"
      contains: "Inter_Tight"
    - path: "src/app/layout.tsx"
      provides: "Body className wires inter.variable + jetbrainsMono.variable; bg-background removed"
      contains: "jetbrainsMono"
    - path: "src/app/portal/layout.tsx"
      provides: "theme-dark wrapper around Sidebar + main"
      contains: "theme-dark"
    - path: "src/app/login/layout.tsx"
      provides: "theme-dark wrapper"
      contains: "theme-dark"
    - path: "src/app/reset-password/layout.tsx"
      provides: "NEW — theme-dark wrapper, pattern-matched against login/layout.tsx"
      contains: "theme-dark"
    - path: "src/components/ui/MonoEyebrow.tsx"
      provides: "Shared presentation primitive used by Sidebar logo/role chip, NavigationButtons auto-save, login/reset-password heroes, Header CLIENT chip (lifted per CONTEXT D-claude-1 since used >3 places per PATTERNS §Shared Patterns)"
      exports: ["default"]
    - path: "src/components/layout/Sidebar.tsx"
      provides: "Restyled to bg-bg-2 chrome with gold-brand active state, mono logo eyebrow + role chip"
      contains: "bg-bg-2"
    - path: "src/components/layout/Header.tsx"
      provides: "Restyled to 56px bg-bg-2 bar with gold-brand 360 accent; inline #0f2440 removed"
      contains: "bg-bg-2"
    - path: "src/components/layout/ProgressBar.tsx"
      provides: "4px gold-brand fill on line track inside bg-bg-2 sticky 56px bar"
      contains: "bg-gold-brand"
    - path: "src/components/layout/NavigationButtons.tsx"
      provides: "Ghost Prev with explicit destination label + gold-brand fill Next + three mono auto-save variants"
      contains: "Saving"
    - path: "src/app/login/page.tsx"
      provides: "Restyled hero card with PEAK360 · ACCESS mono eyebrow, gold-brand Sign In CTA, mono footer"
      contains: "PEAK360 · ACCESS"
    - path: "src/app/reset-password/page.tsx"
      provides: "Restyled hero card with PEAK360 · RECOVERY mono eyebrow, gold-brand Reset password CTA"
      contains: "PEAK360 · RECOVERY"
  key_links:
    - from: "src/app/layout.tsx <body>"
      to: "src/lib/fonts.ts"
      via: "${inter.variable} ${jetbrainsMono.variable} className"
      pattern: "jetbrainsMono\\.variable"
    - from: "src/app/portal/layout.tsx"
      to: ".theme-dark utility in globals.css"
      via: "<div className=\"theme-dark\">"
      pattern: "theme-dark"
    - from: "src/app/login/layout.tsx"
      to: ".theme-dark"
      via: "<div className=\"theme-dark\">"
      pattern: "theme-dark"
    - from: "src/app/reset-password/layout.tsx"
      to: ".theme-dark"
      via: "<div className=\"theme-dark\">"
      pattern: "theme-dark"
    - from: "src/components/layout/Sidebar.tsx"
      to: "src/components/ui/MonoEyebrow.tsx"
      via: "import + render for PEAK360/PORTAL logo eyebrow and role chip"
      pattern: "MonoEyebrow"
    - from: "src/components/layout/NavigationButtons.tsx"
      to: "@keyframes pulse-gold in globals.css"
      via: "style={{ animation: 'pulse-gold 2s ease-out infinite' }} on saving dot"
      pattern: "pulse-gold"
---

<objective>
Land the Phase 9 foundations and ship the two centred-hero auth surfaces.

Purpose: Once `globals.css` carries the new dark tokens, `--font-sans` resolves to Inter Tight, and the three segment layouts wrap children in `theme-dark`, every working surface restyle in 09-02 reduces to utility-class swaps inside existing components. This plan is the contract that 09-02 builds on.

Output: Updated tokens, rebound fonts, three theme-gated segment layouts (one NEW), restyled shared chrome (Sidebar, Header, ProgressBar, NavigationButtons), restyled `/login` and `/reset-password`, and a shared `<MonoEyebrow>` primitive that downstream surfaces will reuse. Phase 8 report + Phase 5 PDF remain untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-brand-language-alignment-across-portal-dashboard-assessment-/09-CONTEXT.md
@.planning/phases/09-brand-language-alignment-across-portal-dashboard-assessment-/09-UI-SPEC.md
@.planning/phases/09-brand-language-alignment-across-portal-dashboard-assessment-/09-RESEARCH.md
@.planning/phases/09-brand-language-alignment-across-portal-dashboard-assessment-/09-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- The contract surfaces this plan touches. Pull from PATTERNS.md verbatim — no codebase exploration required. -->

Current src/lib/fonts.ts (22 lines, full file — landmark):
- Exports `montserrat` (Montserrat), `openSans` (Open_Sans), `inter` (Inter with variable `--font-sans`)
- Target: replace `Inter` with `Inter_Tight` (KEEP export name `inter` per PATTERNS §3 + RESEARCH §Pitfall 8), add `jetbrainsMono` export with `variable: '--font-mono'`, weights `['400', '500']`, subsets `['latin']`, display `'swap'`. Keep montserrat + openSans untouched (landing consumes them).

Current src/app/layout.tsx body line (line 28):
  <body className={`${inter.variable} antialiased min-h-screen bg-background font-sans`}>
Target:
  <body className={`${inter.variable} ${jetbrainsMono.variable} antialiased min-h-screen font-sans`}>
(Drop `bg-background`; KEEP the existing Google Fonts <link> belt-and-braces per RESEARCH §Pattern 2 + §Open Question 4.)

Current src/app/login/layout.tsx (5 lines):
  export const dynamic = 'force-dynamic';
  export default function LoginLayout({ children }) { return children; }
Target:
  export default function LoginLayout({ children }) { return <div className="theme-dark">{children}</div>; }

Current src/app/portal/layout.tsx return (lines 19-24, fragment-wrapped):
  <><Sidebar /><div className="lg:pl-56">{children}</div></>
Target: wrap fragment in `<div className="theme-dark">`. Per PATTERNS §4 + RESEARCH §Pitfall 1: portal-level wrapper alone covers /portal/assessment/[id]/** — do NOT also wrap src/app/portal/assessment/[id]/layout.tsx.

NEW src/app/reset-password/layout.tsx — pattern-match login/layout.tsx target shape.

Current src/app/globals.css @theme inline block (lines 3-33):
- Existing `--color-navy` `#1a365d`, `--color-gold` `#F5A623`, `--color-navy-light`, `--color-gold-light`, `--color-gold-dark`, `--color-navy-950`, `--color-background` `#f8fafc`, `--color-foreground` `#1a202c`, `--color-surface`, `--color-surface-alt`, `--color-border`, `--color-muted`, rating tiers, gradient tokens, `--font-sans: "Inter", ...` (line 21)
- Body rule (lines 35-39): `body { background: var(--color-background); color: var(--color-foreground); font-family: var(--font-sans); }`
- Input focus ring (lines 42-46): `box-shadow: 0 0 0 2px rgba(245, 166, 35, 0.25); border-color: var(--color-gold);`
- Slider thumb rules (lines 49-69): hard-code `var(--color-gold)` + `border: 2px solid white`

Token-Naming Map (verbatim from UI-SPEC §Token-Naming Map — add these to globals.css `@theme inline` after existing tokens):
| Token | Value |
|-------|-------|
| --color-bg | #0a0a0b |
| --color-bg-2 | #0e0e10 |
| --color-bg-3 | #131316 |
| --color-text | #ece5d3 |
| --color-text-dim | rgba(236,229,211,0.62) |
| --color-text-faint | rgba(236,229,211,0.38) |
| --color-gold-brand | #c9a24a |
| --color-champagne | #e8d6a8 |
| --color-line | rgba(232,214,168,0.10) |
| --color-line-2 | rgba(232,214,168,0.20) |
| --color-danger | #d97557 |
| --color-status-good | #7fb37f |
| --font-mono | "JetBrains Mono", ui-monospace, monospace |
| --font-sans (rebind) | "Inter Tight", system-ui, -apple-system, sans-serif |

Pulse keyframes (add OUTSIDE @theme inline block):
  @keyframes pulse-gold {
    0% { box-shadow: 0 0 0 0 rgba(201, 162, 74, 0.6); }
    100% { box-shadow: 0 0 0 12px rgba(201, 162, 74, 0); }
  }
  @media (prefers-reduced-motion: reduce) {
    [style*="pulse-gold"] { animation: none !important; }
  }

theme-dark utility (add OUTSIDE @theme inline block):
  .theme-dark { background: var(--color-bg); color: var(--color-text); min-height: 100vh; font-family: var(--font-sans); }

Sidebar.tsx landmarks (PATTERNS §7):
- bg-navy-dark (lines 225, 236) → bg-bg-2 + border-r border-line
- Active item (lines 142-150): bg-gold/15 text-gold shadow-[inset_...] → border-l-2 border-gold-brand text-text font-medium (NO fill tint)
- Inactive item: text-white/50 hover:text-white/90 hover:bg-white/5 → text-text-dim hover:text-text hover:bg-line
- Logo block (lines 110-128): text-white + text-gold 360 → text-text + text-gold-brand; ADD MonoEyebrow above (PEAK360 / PORTAL, variant hero)
- Mobile hamburger (lines 213-222): bg-navy text-white/70 → bg-bg-2 text-text-dim; aria-label must be "Open navigation"
- Mobile overlay scrim (line 232): bg-black/50 → bg-[rgba(10,10,11,0.7)]
- ADD role chip at bottom: <MonoEyebrow variant="hero">{role}</MonoEyebrow> where role ∈ {COACH, ADMIN, CLIENT}

Header.tsx landmarks (PATTERNS §7):
- style={{ backgroundColor: '#0f2440' }} (line 8) → bg-bg-2 class, remove inline style
- text-white → text-text
- text-gold (the "360") → text-gold-brand
- text-white/60 → text-text-dim
- Logout button (lines 23-34): text-white/60 hover:text-white hover:bg-white/10 → text-text-dim hover:text-text hover:bg-line
- ADD bottom border `border-b border-line`; height 56px (`h-14`)

ProgressBar.tsx landmarks (PATTERNS §7):
- Container (line 18): bg-white border-b border-border shadow-sm → bg-bg-2 border-b border-line
- Section label (line 21): text-navy → MonoEyebrow variant="hero" 11px
- Section title (line 24): text-muted → text-text-dim
- Track (line 28): bg-surface-alt rounded-full h-1.5 → bg-line h-1 (4px per UI-SPEC)
- Fill (line 30): bg-gradient-to-r from-gold-dark to-gold → bg-gold-brand (solid)
- Step dots (lines 47-52): bg-gold/bg-navy/border-gray-300 → bg-gold-brand/bg-bg-3/border-line

NavigationButtons.tsx landmarks (PATTERNS §7 + RESEARCH §Code Examples lines 546-564):
- Auto-save block (lines 30-44, repeated 67-79): replace spinner+green-dot with mono variants per UI-SPEC §Copywriting Contract:
  * isSaving → mono 11px tracking-[0.18em] uppercase text-text-dim + gold-brand dot with pulse-gold animation, label "Saving…"
  * lastSaved → mono 11px tracking-[0.18em] uppercase text-text-dim + status-good dot, label "Saved · {lastSaved}"
  * isDirty && !isSaving && !lastSaved → mono 11px tracking-[0.18em] uppercase text-danger, label "Unsaved changes" (derived per RESEARCH §Assumption A5)
- Prev button (lines 49-55): bg-surface-alt text-navy hover:bg-border → bg-transparent border border-line-2 text-text hover:border-gold-brand hover:text-gold-brand; label "Back to section {n-1}" or "Back to dashboard" (section 1)
- Next button (lines 91-100): bg-gold/bg-navy → bg-gold-brand text-bg hover:bg-champagne; label "Save & continue"; on last section keep gold-brand fill with label "Complete assessment" (existing intent)

login/page.tsx + reset-password/page.tsx: hero shape per UI-SPEC §Layout & Responsive Contract — centred card max-w-[360px], 96px top padding (`pt-24` inline dimension), MonoEyebrow variant="hero" above 40px Display title (mobile 32px). CTA gold-brand fill, ink text. Login footer mono "AUTHORISED ACCESS ONLY · ACTIVITY MONITORED" (uppercase, letter-spacing 0.16em, gold-brand). Magic-link CTA = ghost button.

MonoEyebrow primitive — recommended shape (CONTEXT D-claude-1, lift since used in 7+ places per PATTERNS §Shared Patterns):
  interface MonoEyebrowProps {
    children: React.ReactNode;
    variant?: 'hero' | 'meta';   // hero = gold-brand 0.18em; meta = text-faint 0.16em
    as?: 'span' | 'div';
    className?: string;
  }
  // Renders: <span|div className="font-mono text-[11px] uppercase tracking-[0.18em|0.16em] text-gold-brand|text-text-faint">{children}</span>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 9 tokens, font rebind variable, theme-dark utility, and pulse-gold keyframes to globals.css</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (the entire current `@theme inline` block + body rule + input:focus rule + slider thumb rules — lines 1-69 minimum)
    - .planning/phases/09-brand-language-alignment-across-portal-dashboard-assessment-/09-UI-SPEC.md §Token-Naming Map (exact values)
    - .planning/phases/09-brand-language-alignment-across-portal-dashboard-assessment-/09-CONTEXT.md D-02, D-03, D-14, D-15, D-17, D-18 (additive only, --font-sans rebind permitted, anti-patterns)
    - .planning/phases/09-brand-language-alignment-across-portal-dashboard-assessment-/09-PATTERNS.md §2 (current `@theme inline` block landmarks)
  </read_first>
  <action>
    Inside the existing `@theme inline` block: PRESERVE every existing `--color-*`, `--font-heading`, `--font-body`, gradient, and rating-tier token verbatim (per D-02). REBIND `--font-sans` from `"Inter", system-ui, -apple-system, sans-serif` to `"Inter Tight", system-ui, -apple-system, sans-serif` (per D-03). APPEND the 12 new Phase 9 colour tokens from the UI-SPEC §Token-Naming Map (`--color-bg`, `--color-bg-2`, `--color-bg-3`, `--color-text`, `--color-text-dim`, `--color-text-faint`, `--color-gold-brand`, `--color-champagne`, `--color-line`, `--color-line-2`, `--color-danger`, `--color-status-good`) with the exact hex/rgba values listed there. APPEND `--font-mono: "JetBrains Mono", ui-monospace, monospace;`.

    OUTSIDE the `@theme inline` block: REMOVE the `background: var(--color-background);` line from the `body { }` rule (theme is now segment-gated per D-05); keep `color: var(--color-foreground);` and `font-family: var(--font-sans);` as legacy fallback. REPLACE the input/select/textarea focus ring `box-shadow: 0 0 0 2px rgba(245, 166, 35, 0.25)` and `border-color: var(--color-gold)` with `box-shadow: 0 0 0 2px rgba(201, 162, 74, 0.45)` and `border-color: var(--color-gold-brand)` (per UI-SPEC §Layout Focus & keyboard contract). UPDATE the slider thumb rules (current lines 49-69) to use `background: var(--color-gold-brand)` and `border: 2px solid var(--color-bg-3)` instead of `var(--color-gold)` + `solid white` (per D-14, D-15).

    ADD a `.theme-dark` utility class at the end of the file: `background: var(--color-bg); color: var(--color-text); min-height: 100vh; font-family: var(--font-sans);`.

    ADD `@keyframes pulse-gold { 0% { box-shadow: 0 0 0 0 rgba(201, 162, 74, 0.6); } 100% { box-shadow: 0 0 0 12px rgba(201, 162, 74, 0); } }` and a `@media (prefers-reduced-motion: reduce)` rule that disables animations for elements with inline `pulse-gold` animation (per RESEARCH §Open Question 6).

    Do NOT introduce any token outside `{--color-bg, -bg-2, -bg-3, -text, -text-dim, -text-faint, -gold-brand, -champagne, -line, -line-2, -danger, -status-good, -font-mono}`. Do NOT modify existing `--color-navy*`, `--color-gold*` (except `-gold-brand` is new), `--color-background`, `--color-foreground`, `--color-surface*`, `--color-border`, `--color-muted`, or gradient tokens.
  </action>
  <verify>
    <automated>grep -E "^\s*--color-(bg|bg-2|bg-3|text|text-dim|text-faint|gold-brand|champagne|line|line-2|danger|status-good)\s*:" src/app/globals.css | wc -l | awk '$1 == 12 { exit 0 } { exit 1 }' && grep -E "^\s*--font-mono\s*:" src/app/globals.css && grep -E "^\s*--font-sans\s*:\s*\"Inter Tight\"" src/app/globals.css && grep -E "^\s*--color-navy\s*:\s*#1a365d" src/app/globals.css && grep -E "^\s*--color-gold\s*:\s*#F5A623" src/app/globals.css && grep -E "^\.theme-dark\s*\{" src/app/globals.css && grep -E "^@keyframes pulse-gold" src/app/globals.css && ! grep -E "^\s*background:\s*var\(--color-background\)" src/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - 12 new colour tokens present in `@theme inline` with exact values from UI-SPEC §Token-Naming Map
    - `--font-mono` token present mapping to JetBrains Mono
    - `--font-sans` rebound to `"Inter Tight", system-ui, -apple-system, sans-serif`
    - All existing legacy tokens (`--color-navy`, `--color-gold`, `--color-navy-light`, `--color-gold-light`, `--color-gold-dark`, `--color-navy-950`, `--color-background`, `--color-foreground`, `--color-surface`, `--color-surface-alt`, `--color-border`, `--color-muted`, rating tiers, gradients, `--font-heading`, `--font-body`) preserved with original values
    - `.theme-dark` utility class defined with `background: var(--color-bg); color: var(--color-text);`
    - `@keyframes pulse-gold` defined; `prefers-reduced-motion` disables it
    - Body rule no longer sets `background: var(--color-background)` (segment-gated)
    - Input focus ring uses `rgba(201, 162, 74, 0.45)` + `var(--color-gold-brand)`
    - Slider thumb rules use `var(--color-gold-brand)` + `2px solid var(--color-bg-3)`
  </acceptance_criteria>
  <done>globals.css carries the additive Phase 9 token set, the rebound `--font-sans`, the `.theme-dark` utility, the gold-brand focus ring, and the pulse-gold keyframes — without removing or altering any legacy Phase 5/8 token value.</done>
</task>

<task type="auto">
  <name>Task 2: Rebind Inter → Inter_Tight and add JetBrains_Mono in fonts.ts; wire mono variable in root layout body className</name>
  <files>src/lib/fonts.ts, src/app/layout.tsx</files>
  <read_first>
    - src/lib/fonts.ts (all 22 lines — current Inter import is the landmark)
    - src/app/layout.tsx (read lines 1-40 — the `<body className>` line 28 is the landmark, plus the existing Google Fonts `<link>` at lines 21-26)
    - .planning/phases/09-brand-language-alignment-across-portal-dashboard-assessment-/09-RESEARCH.md §Pattern 2 + §Pitfall 8 (KEEP export name `inter` to avoid cascade rename)
    - .planning/phases/09-brand-language-alignment-across-portal-dashboard-assessment-/09-RESEARCH.md §Open Question 4 (KEEP existing `<link>` belt-and-braces; do NOT remove)
  </read_first>
  <action>
    In `src/lib/fonts.ts`: change the import line from `import { Montserrat, Open_Sans, Inter } from 'next/font/google';` to `import { Montserrat, Open_Sans, Inter_Tight, JetBrains_Mono } from 'next/font/google';`. Replace the `inter = Inter({ ... })` call with `inter = Inter_Tight({ subsets: ['latin'], variable: '--font-sans', display: 'swap', weight: ['300', '400', '500', '600'] })`. KEEP the export name `inter` literally — do not rename to `interTight` (per RESEARCH §Pitfall 8 to avoid cascade rename through layout.tsx). ADD a new export `export const jetbrainsMono = JetBrains_Mono({ subsets: ['latin'], variable: '--font-mono', display: 'swap', weight: ['400', '500'] });`. KEEP `montserrat` and `openSans` exports unchanged (landing page consumes them).

    In `src/app/layout.tsx`: update the import to include `jetbrainsMono` from `@/lib/fonts`. Change the `<body>` className from `` `${inter.variable} antialiased min-h-screen bg-background font-sans` `` to `` `${inter.variable} ${jetbrainsMono.variable} antialiased min-h-screen font-sans` `` — REMOVE `bg-background` (theme is segment-gated per D-05). KEEP the existing Google Fonts `<link>` tags at lines 21-26 verbatim (belt-and-braces per RESEARCH §Open Question 4; removal is a follow-up after landing-page QA).
  </action>
  <verify>
    <automated>grep -E "Inter_Tight" src/lib/fonts.ts && grep -E "JetBrains_Mono" src/lib/fonts.ts && grep -E "variable:\s*'--font-mono'" src/lib/fonts.ts && grep -E "variable:\s*'--font-sans'" src/lib/fonts.ts && grep -E "export const inter" src/lib/fonts.ts && grep -E "export const jetbrainsMono" src/lib/fonts.ts && grep -E "\\\$\{jetbrainsMono\.variable\}" src/app/layout.tsx && ! grep -E "bg-background" src/app/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/fonts.ts` imports `Inter_Tight` and `JetBrains_Mono` from `next/font/google`
    - `inter` export uses `Inter_Tight` with `variable: '--font-sans'` and weights `['300','400','500','600']`
    - `jetbrainsMono` export uses `JetBrains_Mono` with `variable: '--font-mono'` and weights `['400','500']`
    - `montserrat` and `openSans` exports unchanged
    - `src/app/layout.tsx` body className includes both `${inter.variable}` and `${jetbrainsMono.variable}`
    - `src/app/layout.tsx` body className no longer contains `bg-background`
    - The existing Google Fonts `<link>` block (lines 21-26 of layout.tsx) remains present
  </acceptance_criteria>
  <done>`--font-sans` resolves to Inter Tight and `--font-mono` resolves to JetBrains Mono via next/font/google self-hosting; the root body is theme-neutral and ready to receive segment-level dark canvas.</done>
</task>

<task type="auto">
  <name>Task 3: Add theme-dark wrappers to portal + login segment layouts; create NEW reset-password layout</name>
  <files>src/app/portal/layout.tsx, src/app/login/layout.tsx, src/app/reset-password/layout.tsx</files>
  <read_first>
    - src/app/portal/layout.tsx (current return block — fragment wrapping Sidebar + lg:pl-56 div is the landmark)
    - src/app/login/layout.tsx (full file — 5-line landmark from PATTERNS §1)
    - src/app/reset-password/page.tsx (verify the directory exists but layout.tsx does not — per RESEARCH §Pitfall 7)
    - .planning/phases/09-brand-language-alignment-across-portal-dashboard-assessment-/09-CONTEXT.md D-05, D-06 (segment-gating boundary, reset-password layout is the only NEW file)
    - .planning/phases/09-brand-language-alignment-across-portal-dashboard-assessment-/09-RESEARCH.md §Pitfall 1 (portal-level wrapper is sufficient; do NOT wrap src/app/portal/assessment/[id]/layout.tsx)
    - .planning/phases/09-brand-language-alignment-across-portal-dashboard-assessment-/09-PATTERNS.md §1 + §4 (target shapes)
  </read_first>
  <action>
    In `src/app/portal/layout.tsx`: wrap the existing return body (Sidebar + `<div className="lg:pl-56">{children}</div>`) in a `<div className="theme-dark">` element. Keep the existing `getValidSession()` + `redirect('/login')` auth guard verbatim. Do NOT add a wrapper to `src/app/portal/assessment/[id]/layout.tsx` — that layout inherits from portal (per RESEARCH §Pitfall 1).

    In `src/app/login/layout.tsx`: change `return children;` to `return <div className="theme-dark">{children}</div>;`. Keep `export const dynamic = 'force-dynamic';` and the function signature unchanged.

    CREATE `src/app/reset-password/layout.tsx` (NEW file, pattern-matched against login/layout.tsx):
      export const dynamic = 'force-dynamic';
      export default function ResetPasswordLayout({ children }: { children: React.ReactNode }) {
        return <div className="theme-dark">{children}</div>;
      }
  </action>
  <verify>
    <automated>grep -E "className=\"theme-dark\"" src/app/portal/layout.tsx && grep -E "className=\"theme-dark\"" src/app/login/layout.tsx && test -f src/app/reset-password/layout.tsx && grep -E "className=\"theme-dark\"" src/app/reset-password/layout.tsx && grep -E "ResetPasswordLayout" src/app/reset-password/layout.tsx && grep -c "theme-dark" src/app/portal/assessment/\[id\]/layout.tsx | awk '$1 == 0 { exit 0 } { exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/portal/layout.tsx` returns a JSX tree whose outermost element has `className="theme-dark"`
    - `src/app/login/layout.tsx` returns `<div className="theme-dark">{children}</div>`
    - `src/app/reset-password/layout.tsx` exists, exports `ResetPasswordLayout`, and returns `<div className="theme-dark">{children}</div>`
    - `src/app/portal/assessment/[id]/layout.tsx` contains NO occurrence of `theme-dark` (inherits from portal — Pitfall 1)
    - Existing `getValidSession()` + `redirect('/login')` auth guard in portal/layout.tsx preserved
  </acceptance_criteria>
  <done>The three segment layouts gate the dark canvas; the assessment route inherits via /portal; the body of every authenticated surface renders on `--color-bg` with cream text on first paint.</done>
</task>

<task type="auto">
  <name>Task 4: Create shared MonoEyebrow primitive in src/components/ui/</name>
  <files>src/components/ui/MonoEyebrow.tsx</files>
  <read_first>
    - .planning/phases/09-brand-language-alignment-across-portal-dashboard-assessment-/09-UI-SPEC.md §Typography (Eyebrow / Meta row — 11px JetBrains Mono, two visual modes: gold-brand uppercase 0.18em vs text-faint uppercase 0.16em)
    - .planning/phases/09-brand-language-alignment-across-portal-dashboard-assessment-/09-CONTEXT.md D-claude-1 (lift to primitive if used >3 places)
    - .planning/phases/09-brand-language-alignment-across-portal-dashboard-assessment-/09-PATTERNS.md §Shared Patterns "Mono Eyebrow Primitive" (PATTERNS confirms 7+ consumers: SectionHeader, Header, Sidebar logo + role, every page hero, Toast eyebrow, auto-save indicator)
    - src/components/ui/SectionHeader.tsx (the existing UI primitive — analog for file location + 'use client' pattern)
  </read_first>
  <action>
    Create `src/components/ui/MonoEyebrow.tsx` as a 'use client' presentation primitive (matches existing `src/components/ui/SectionHeader.tsx` pattern). Define an interface `MonoEyebrowProps` with: `children: React.ReactNode`, `variant?: 'hero' | 'meta'` (default `'hero'`), `as?: 'span' | 'div'` (default `'span'`), `className?: string` (additional classes for layout composition).

    Render shape: when `variant === 'hero'` emit `font-mono text-[11px] uppercase tracking-[0.18em] text-gold-brand`; when `variant === 'meta'` emit `font-mono text-[11px] uppercase tracking-[0.16em] text-text-faint`. Honour the `as` prop to render either a `<span>` (inline, default) or `<div>` (block). Append the optional `className` after the variant classes so callers can add layout utilities (`flex`, `mb-3`, etc.).

    Do NOT add letter-spacing inline styles, decorative line elements, or pulse dots — those are caller concerns. This is a single-responsibility text wrapper. Render children as plain React text nodes only — never via raw-HTML injection escape hatches. Export as default.

    The component is consumed by Sidebar logo eyebrow (PEAK360 / PORTAL), Sidebar user role chip (COACH / ADMIN / CLIENT), NavigationButtons auto-save mono labels, login hero (PEAK360 · ACCESS), reset-password hero (PEAK360 · RECOVERY), and downstream in 09-02 by SectionHeader + every page hero + Header CLIENT chip + Toast eyebrow.
  </action>
  <verify>
    <automated>test -f src/components/ui/MonoEyebrow.tsx && grep -E "^'use client'" src/components/ui/MonoEyebrow.tsx && grep -E "export default function MonoEyebrow" src/components/ui/MonoEyebrow.tsx && grep -E "variant\?:\s*'hero'\s*\|\s*'meta'" src/components/ui/MonoEyebrow.tsx && grep -E "text-gold-brand" src/components/ui/MonoEyebrow.tsx && grep -E "text-text-faint" src/components/ui/MonoEyebrow.tsx && grep -E "tracking-\[0\.18em\]" src/components/ui/MonoEyebrow.tsx && grep -E "tracking-\[0\.16em\]" src/components/ui/MonoEyebrow.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `src/components/ui/MonoEyebrow.tsx`
    - Marked `'use client'`
    - Exports default function `MonoEyebrow` with props `{ children, variant?: 'hero' | 'meta', as?: 'span' | 'div', className? }`
    - `variant="hero"` renders `font-mono text-[11px] uppercase tracking-[0.18em] text-gold-brand`
    - `variant="meta"` renders `font-mono text-[11px] uppercase tracking-[0.16em] text-text-faint`
    - `as="div"` renders a `<div>`; default renders a `<span>`
    - Accepts and appends `className` for layout composition
    - No new npm dependencies introduced
  </acceptance_criteria>
  <done>A single-responsibility presentation primitive that Sidebar, NavigationButtons, login, reset-password (in this plan) and SectionHeader/Header/Toast/page heroes (in 09-02) all consume. One file, no API churn downstream.</done>
</task>

<task type="auto">
  <name>Task 5: Restyle Sidebar.tsx to dark chrome with gold-brand active state, mono logo, mono role chip</name>
  <files>src/components/layout/Sidebar.tsx</files>
  <read_first>
    - src/components/layout/Sidebar.tsx (full file — 250+ lines; landmarks at lines 110-128 logo, 138-152 active nav item, 213-222 mobile hamburger, 225-236 desktop rail, 232 overlay scrim)
    - src/components/ui/MonoEyebrow.tsx (just-created primitive — import as `@/components/ui/MonoEyebrow`)
    - .planning/phases/09-brand-language-alignment-across-portal-dashboard-assessment-/09-UI-SPEC.md §Component Inventory row "Sidebar" + §Copywriting "Sidebar (collapsed rail, top)" and "Sidebar (logged-in user section)"
    - .planning/phases/09-brand-language-alignment-across-portal-dashboard-assessment-/09-PATTERNS.md §7 "Sidebar.tsx" (full landmark + swap table)
    - .planning/phases/09-brand-language-alignment-across-portal-dashboard-assessment-/09-CONTEXT.md D-14, D-15, D-16 (anti-patterns: no bright gold, no cream cards, no status decoration)
  </read_first>
  <action>
    Apply the full Sidebar restyle per PATTERNS §7 swap table:

    Desktop rail (current `bg-navy-dark` at lines 225/236): swap to `bg-bg-2` and add `border-r border-line`. Remove any shadow utilities.

    Logo block (lines 110-128): replace `text-white` heading with `text-text`; replace `text-gold` on the "360" with `text-gold-brand`. ABOVE the logo line, render `<MonoEyebrow variant="hero">PEAK360 / PORTAL</MonoEyebrow>` wrapped in a div with `mb-2` spacing. Keep the existing 64px logo block height.

    Active nav item (lines 138-152): replace `bg-gold/15 text-gold shadow-[inset_0_0_0_1px_rgba(245,166,35,0.15)]` with `border-l-2 border-gold-brand text-text font-medium` (no fill tint per D-14/D-15). Adjust padding to absorb the 2px left border without shifting label position.

    Inactive nav item: replace `text-white/50 hover:text-white/90 hover:bg-white/5` with `text-text-dim hover:text-text hover:bg-line`.

    User block at the bottom of the rail: render avatar + name (sans 13px / 500 / `text-text`) + role chip below using `<MonoEyebrow variant="hero">{role.toUpperCase()}</MonoEyebrow>` where `role` is one of `COACH`, `ADMIN`, `CLIENT`. Replace any existing user-block legacy tokens (text-white/*, border-white/*) with `text-text-dim` / `border-line`.

    Mobile hamburger button (lines 213-222): swap `bg-navy text-white/70` → `bg-bg-2 text-text-dim`. CHANGE `aria-label` from `"Open menu"` to `"Open navigation"` (per UI-SPEC §Layout Focus & keyboard contract + §Copywriting Contract).

    Mobile overlay scrim (line 232): replace `bg-black/50` with `bg-[rgba(10,10,11,0.7)]`.

    Mobile drawer panel: replace `bg-navy-dark` → `bg-bg-2`; `border-white/10` → `border-line`.

    Any close/back icon-only buttons inside the mobile drawer must carry an `aria-label` (`"Close navigation"`).

    Preserve all existing nav-item routing, link hrefs, role-filtering logic, and Sidebar prop API. Visual swap only — no behavioural change.
  </action>
  <verify>
    <automated>! grep -E "bg-navy|bg-navy-dark|text-white/|text-gold\b|border-white/|bg-black/50|bg-gold/15" src/components/layout/Sidebar.tsx && grep -E "bg-bg-2" src/components/layout/Sidebar.tsx && grep -E "border-gold-brand" src/components/layout/Sidebar.tsx && grep -E "text-text" src/components/layout/Sidebar.tsx && grep -E "MonoEyebrow" src/components/layout/Sidebar.tsx && grep -E "PEAK360" src/components/layout/Sidebar.tsx && grep -E "aria-label=\"Open navigation\"" src/components/layout/Sidebar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - No occurrences of `bg-navy`, `bg-navy-dark`, `text-gold` (legacy), `text-white/*` opacity, `border-white/*`, `bg-black/50`, or `bg-gold/15` in Sidebar.tsx
    - Desktop rail uses `bg-bg-2` + `border-r border-line`
    - Active nav item has `border-l-2 border-gold-brand` and `text-text font-medium`
    - Inactive nav items use `text-text-dim` with hover `text-text` + `bg-line`
    - Logo block renders `<MonoEyebrow variant="hero">` containing `PEAK360` text above the existing logo lines
    - User block renders role chip via `<MonoEyebrow variant="hero">` with role uppercase
    - Mobile hamburger has `aria-label="Open navigation"`
    - Mobile overlay scrim uses `rgba(10,10,11,0.7)`
    - Sidebar's existing routing / nav-item links / role-filtering logic unchanged
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Sidebar renders entirely on dark tokens; active state is gold-brand left border (no fill); mono PEAK360/PORTAL eyebrow above the logo; mono role chip at the bottom; all icon-only buttons carry aria-labels.</done>
</task>

<task type="auto">
  <name>Task 6: Restyle Header.tsx and ProgressBar.tsx to dark chrome</name>
  <files>src/components/layout/Header.tsx, src/components/layout/ProgressBar.tsx</files>
  <read_first>
    - src/components/layout/Header.tsx (full file — landmark lines 7-38 with inline `style={{ backgroundColor: '#0f2440' }}` and `text-white/*`)
    - src/components/layout/ProgressBar.tsx (full file — landmarks 17-72 including container, label, track, fill, step dots)
    - .planning/phases/09-brand-language-alignment-across-portal-dashboard-assessment-/09-UI-SPEC.md §Component Inventory rows "Header" and "ProgressBar"
    - .planning/phases/09-brand-language-alignment-across-portal-dashboard-assessment-/09-PATTERNS.md §7 "Header.tsx" and "ProgressBar.tsx"
  </read_first>
  <action>
    Header.tsx restyle:
    - Remove the inline `style={{ backgroundColor: '#0f2440' }}` on the outer `<header>` element. Replace with className tokens `bg-bg-2 border-b border-line text-text h-14` (56px height per UI-SPEC §Component Inventory).
    - Logo/title block: replace `text-white` → `text-text`; replace `text-gold` on the "360" → `text-gold-brand`; replace `text-white/60` on "Longevity Assessment" subtitle → `text-text-dim`.
    - Logout/action buttons (lines 23-34): swap `text-white/60 hover:text-white hover:bg-white/10` → `text-text-dim hover:text-text hover:bg-line`. If the logout button is icon-only, ensure `aria-label="Sign out"` is set.
    - Keep the existing Link wrapping, `max-w-6xl mx-auto px-6 py-3 flex items-center justify-between` layout container, and prop API.
    - Do NOT add the right-side CLIENT eyebrow on the assessment Header in this plan — that injection is handled in 09-02 step 4-5 (Header restyle for assessment form context). This task delivers the chrome restyle only; per-route enhancement comes later. (Note: PATTERNS §7 mentions the CLIENT eyebrow as a future enhancement; defer to 09-02.)

    ProgressBar.tsx restyle:
    - Container (line 18): swap `bg-white border-b border-border shadow-sm` → `bg-bg-2 border-b border-line` (no shadow). Set container height to 56px (`h-14`) per UI-SPEC §Layout sticky behaviour.
    - Section label (line 21): replace plain `text-navy` text with `<MonoEyebrow variant="hero">` for the "Section N of 11" eyebrow. (Section title can remain as 13px Label `text-text` adjacent to the eyebrow — preserve existing intent.)
    - Section title (line 24): replace `text-muted` → `text-text-dim`.
    - Progress track (line 28): replace `bg-surface-alt rounded-full h-1.5` → `bg-line h-1` (4px per UI-SPEC).
    - Progress fill (line 30): replace `bg-gradient-to-r from-gold-dark to-gold` → `bg-gold-brand` (solid, no gradient).
    - Step dots (lines 47-52): swap active `bg-gold text-navy shadow-[0_0_0_3px_rgba(245,166,35,0.2)]` → `bg-gold-brand text-bg`; completed `bg-navy text-white` → `bg-bg-3 text-text`; pending `border-gray-300 text-muted` → `border-line text-text-dim`.
    - Preserve sticky positioning, step indices, click handlers, and prop API.
  </action>
  <verify>
    <automated>! grep -E "backgroundColor:\s*'#0f2440'|bg-white|text-white\b|text-white/|text-navy|text-gold\b|bg-gold\b|bg-gold-dark|from-gold-dark|to-gold|bg-surface-alt|border-gray-300|text-muted" src/components/layout/Header.tsx && ! grep -E "bg-white|text-navy|bg-gold\b|from-gold-dark|to-gold|bg-surface-alt|border-gray-300|text-muted|bg-navy\b" src/components/layout/ProgressBar.tsx && grep -E "bg-bg-2" src/components/layout/Header.tsx && grep -E "bg-gold-brand" src/components/layout/Header.tsx && grep -E "bg-bg-2" src/components/layout/ProgressBar.tsx && grep -E "bg-gold-brand" src/components/layout/ProgressBar.tsx && grep -E "MonoEyebrow" src/components/layout/ProgressBar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Header.tsx: no inline `#0f2440`, no `text-white`, no `text-gold` (legacy), no `text-white/*`
    - Header.tsx uses `bg-bg-2`, `border-b border-line`, `text-text`, `text-gold-brand` on the 360 accent, `text-text-dim` on subtitle/logout
    - Header.tsx height is 56px (`h-14`)
    - ProgressBar.tsx: no `bg-white`, no `text-navy`, no `bg-gold` (legacy), no gradient fill, no `bg-surface-alt`, no `text-muted`, no `border-gray-300`
    - ProgressBar.tsx uses `bg-bg-2`, `bg-line` track, `bg-gold-brand` solid fill, 4px track height
    - ProgressBar.tsx imports + renders `<MonoEyebrow>` for the section eyebrow
    - Step dots use `bg-gold-brand` / `bg-bg-3` / `border-line` per state
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The two shared chrome bars on the assessment form (Header at top + ProgressBar sticky) render entirely on dark tokens with gold-brand accents and mono section eyebrow.</done>
</task>

<task type="auto">
  <name>Task 7: Restyle NavigationButtons.tsx — ghost Back button, gold-brand Save & continue, three mono auto-save variants</name>
  <files>src/components/layout/NavigationButtons.tsx</files>
  <read_first>
    - src/components/layout/NavigationButtons.tsx (full file — landmarks lines 30-44 auto-save block, 49-55 prev button, 67-79 second auto-save block, 91-100 next button)
    - src/components/ui/MonoEyebrow.tsx (just-created — for mono auto-save labels)
    - .planning/phases/09-brand-language-alignment-across-portal-dashboard-assessment-/09-UI-SPEC.md §Component Inventory row "NavigationButtons" + §Copywriting Contract "Primary CTAs" (Back to section {n-1} / Back to dashboard / Save & continue) + "Assessment form auto-save indicator" (three variants)
    - .planning/phases/09-brand-language-alignment-across-portal-dashboard-assessment-/09-PATTERNS.md §7 "NavigationButtons.tsx"
    - .planning/phases/09-brand-language-alignment-across-portal-dashboard-assessment-/09-RESEARCH.md §Pitfall 6 + §Code Examples lines 546-564 (auto-save three-variant pattern + UNSAVED CHANGES derived state per Assumption A5)
    - src/lib/store/assessment-store.ts (lines defining `isDirty`, `isSaving`, `lastSaved` Zustand state — confirm the three flags exist; no store change needed)
  </read_first>
  <action>
    Auto-save indicator (both occurrences at ~line 30 and ~line 67):
    - Remove the spinner span + `text-gold` "Saving..." text.
    - Replace with three render branches:
      * `isSaving` → render a mono 11px tracking-[0.18em] uppercase `text-text-dim` row (use `<MonoEyebrow variant="meta">` or inline equivalent classes — use inline for the row layout) containing a 6×6 (`w-1.5 h-1.5`) rounded gold-brand dot with `style={{ animation: 'pulse-gold 2s ease-out infinite' }}` followed by the literal text `Saving…` (ellipsis is the single-character `…`).
      * `lastSaved` (and not `isSaving`) → render a mono row with a 6×6 `bg-status-good` rounded dot (no pulse) followed by `Saved · {lastSaved}` (preserve the existing relative-time formatting that produces the `lastSaved` value).
      * `isDirty && !isSaving && !lastSaved` → render a mono row in `text-danger` colour with no dot, label `Unsaved changes` (derived proxy state per RESEARCH §Assumption A5; no Zustand store change).
    - Each row uses `font-mono text-[11px] uppercase tracking-[0.18em]` and `flex items-center gap-2`. Do NOT use `bg-rating-elite` anywhere.

    Prev button (line 49-55):
    - Replace `bg-surface-alt text-navy hover:bg-border` with `bg-transparent border border-line-2 text-text hover:border-gold-brand hover:text-gold-brand`.
    - Replace bare `"Back"` label with a destination-explicit label derived from props: when on section ≥ 2 render `"Back to section {currentSection - 1}"`; when on section 1 (entered from dashboard) render `"Back to dashboard"`. Use the existing `currentSection` prop (or equivalent) to drive the conditional.

    Next button (line 91-100):
    - Replace the conditional `${isLastSection ? 'bg-gold text-navy hover:bg-gold-light' : 'bg-navy text-white hover:bg-navy-light'}` with a single style: `bg-gold-brand text-bg hover:bg-champagne` for both intermediate and last-section variants (per UI-SPEC §Copywriting "Primary CTAs": both "Save & continue" and "Complete assessment" use gold-brand fill).
    - Label: keep existing "Save & continue" on intermediate, keep existing terminal label on last section (or update to "Complete assessment" if the existing copy was "Submit" — preserve intent; do not invent new copy beyond UI-SPEC §Copywriting Contract).
    - Disabled state: `disabled:opacity-40 disabled:cursor-not-allowed` per UI-SPEC §Focus & keyboard contract.

    Both buttons use `text-[13px] font-medium tracking-[0.02em]` (Label size, per UI-SPEC §Typography "Label" row).
  </action>
  <verify>
    <automated>! grep -E "bg-rating-elite|text-gold\b|bg-gold\b|text-navy|bg-navy|bg-surface-alt|bg-border|bg-gold-light|bg-navy-light|hover:bg-gold-light|hover:bg-navy-light" src/components/layout/NavigationButtons.tsx && grep -E "Saving…" src/components/layout/NavigationButtons.tsx && grep -E "Unsaved changes" src/components/layout/NavigationButtons.tsx && grep -E "Back to (section|dashboard)" src/components/layout/NavigationButtons.tsx && grep -E "bg-gold-brand" src/components/layout/NavigationButtons.tsx && grep -E "pulse-gold" src/components/layout/NavigationButtons.tsx && grep -E "border-line-2" src/components/layout/NavigationButtons.tsx</automated>
  </verify>
  <acceptance_criteria>
    - No legacy tokens (`bg-navy`, `bg-gold`, `text-navy`, `text-gold`, `bg-rating-elite`, `bg-surface-alt`, `bg-border`, `bg-gold-light`, `bg-navy-light`) in NavigationButtons.tsx
    - Three auto-save variants rendered: `Saving…` (gold-brand pulse dot), `Saved · {time}` (status-good dot), `Unsaved changes` (danger colour, no dot)
    - All three variants use `font-mono text-[11px] uppercase tracking-[0.18em]`
    - Saving dot has `pulse-gold` animation via inline `style`
    - Prev button label is destination-explicit (`Back to section {n-1}` or `Back to dashboard`)
    - Prev button uses ghost variant (`bg-transparent border border-line-2`)
    - Next button uses `bg-gold-brand text-bg hover:bg-champagne` on all states
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The assessment-form footer carries a clear mono status with three states; navigation labels are destination-explicit; primary CTA is gold-brand fill, ink text, sentence case.</done>
</task>

<task type="auto">
  <name>Task 8: Restyle /login + /reset-password pages with centred hero, mono eyebrow, gold-brand CTA</name>
  <files>src/app/login/page.tsx, src/app/reset-password/page.tsx</files>
  <read_first>
    - src/app/login/page.tsx (full file — current hero/card/form structure is the landmark)
    - src/app/reset-password/page.tsx (full file — verify current navy gradient + landmarks for the form card)
    - src/components/ui/MonoEyebrow.tsx (just-created — for hero eyebrows)
    - .planning/phases/09-brand-language-alignment-across-portal-dashboard-assessment-/09-UI-SPEC.md §Copywriting "Login page hero" / "Reset-password page hero" / "Primary CTAs" (Sign In / Reset password / Email me a sign-in link); §Layout & Responsive Contract "Login / Reset-password" row (centred 360px max card, 96px hero top padding)
    - .planning/phases/09-brand-language-alignment-across-portal-dashboard-assessment-/09-CONTEXT.md D-17 (96px hero padding is inline only, not a token — use `pt-24`)
    - src/components/forms/FormField.tsx (the form-field component — note: full restyle is in 09-02, but verify whether the login form uses FormField; if it does, the form fields will inherit dark styling once 09-02 lands. For 09-01, ensure the page-level chrome around the form card is fully dark-themed even if internal fields still resolve via legacy tokens.)
  </read_first>
  <action>
    Apply the centred-hero layout per UI-SPEC §Layout & Responsive Contract row "Login / Reset-password":
    - Outer wrapper: full-viewport centred column with `pt-24` (inline 96px hero top padding — D-17), centred horizontally with a max-width of 360px on the form card (`max-w-[360px]`).
    - Above the card: render `<MonoEyebrow variant="hero">PEAK360 · ACCESS</MonoEyebrow>` (for login) or `<MonoEyebrow variant="hero">PEAK360 · RECOVERY</MonoEyebrow>` (for reset-password) with `mb-3` spacing.
    - Display title below the eyebrow: 40px / 500 / `text-text` / `tracking-[-0.03em]` / `leading-none` (Display role per UI-SPEC §Typography), with `sm:text-[40px] text-[32px]` so mobile downscales to 32. Preserve existing intent copy (login page existing title; reset-password "Recovery" headline) — do NOT re-author copy beyond UI-SPEC §Copywriting Contract.

    Form card:
    - Background `bg-bg-3`, border `border border-line`, rounded corners, padding 24-32px (use the spacing token scale).
    - Preserve every existing form field, validation message, magic-link/forgot-password link wiring, and submit handler — visual only.
    - Replace any inline `text-navy` / `bg-navy` / `text-gold` / `bg-white` / `text-red-500` / `bg-gradient-*` / `from-navy*` / inline navy hex literals inside the page file with dark-canvas equivalents (`text-text`, `text-gold-brand`, `bg-bg-3`, `text-danger`).
    - Form-field internal styling: if /login or /reset-password renders `<FormField>` from `src/components/forms/`, leave the FormField internals untouched (those land in 09-02 Task 1). For inline `<input>` elements used directly in the page, swap to dark tokens (`bg-bg-3 border border-line text-text placeholder:text-text-faint focus:border-gold-brand`).

    Primary CTA:
    - Login Sign In button: `bg-gold-brand text-bg hover:bg-champagne text-[13px] font-medium tracking-[0.02em]` (sentence case "Sign in" or existing exact label "Sign In" — preserve existing copy).
    - Login magic-link CTA: ghost variant `bg-transparent border border-line-2 text-text hover:border-gold-brand hover:text-gold-brand`. Label per UI-SPEC: "Email me a sign-in link".
    - Reset-password CTA: `bg-gold-brand text-bg hover:bg-champagne`. Label "Reset password".

    Login footer line:
    - Render `<MonoEyebrow variant="hero">AUTHORISED ACCESS ONLY · ACTIVITY MONITORED</MonoEyebrow>` with letter-spacing slightly tighter than the default eyebrow if needed (UI-SPEC specifies 0.16em). To honour this, either pass a custom className appending `tracking-[0.16em]` (which will override `tracking-[0.18em]` due to Tailwind class order) or render an inline span with the exact classes. Replace any existing "Authorised access only…" string with the uppercased mono variant.

    All icon-only elements (eye-toggle on password input, etc.) must carry an explicit `aria-label`.

    Phase 8 + Phase 7 auth API + form-submit logic: untouched.
  </action>
  <verify>
    <automated>! grep -E "bg-navy|text-navy|from-navy|to-navy|bg-gold\b|text-gold\b|bg-white\b|text-red-500" src/app/login/page.tsx && ! grep -E "bg-navy|text-navy|from-navy|to-navy|bg-gold\b|text-gold\b|bg-white\b|text-red-500" src/app/reset-password/page.tsx && grep -E "PEAK360 · ACCESS" src/app/login/page.tsx && grep -E "PEAK360 · RECOVERY" src/app/reset-password/page.tsx && grep -E "AUTHORISED ACCESS ONLY" src/app/login/page.tsx && grep -E "bg-gold-brand" src/app/login/page.tsx && grep -E "bg-gold-brand" src/app/reset-password/page.tsx && grep -E "pt-24" src/app/login/page.tsx && grep -E "pt-24" src/app/reset-password/page.tsx && grep -E "max-w-\[360px\]" src/app/login/page.tsx && grep -E "max-w-\[360px\]" src/app/reset-password/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Neither page contains legacy `bg-navy`, `text-navy`, `from-navy*`, `to-navy*`, `bg-gold` (legacy), `text-gold` (legacy), `bg-white`, or `text-red-500`
    - Login renders `<MonoEyebrow variant="hero">PEAK360 · ACCESS</MonoEyebrow>` above the hero title
    - Reset-password renders `<MonoEyebrow variant="hero">PEAK360 · RECOVERY</MonoEyebrow>` above the hero title
    - Login footer renders `AUTHORISED ACCESS ONLY · ACTIVITY MONITORED` mono uppercase string
    - Both pages use `pt-24` (96px hero top padding) and `max-w-[360px]` on the form card
    - Form card uses `bg-bg-3 border border-line`
    - Primary CTAs use `bg-gold-brand text-bg hover:bg-champagne` with 13px / 500 / 0.02em letter-spacing
    - Magic-link CTA (login) is ghost variant (`bg-transparent border border-line-2`)
    - All icon-only buttons carry explicit `aria-label`
    - Existing auth API calls, form validation, and submit logic untouched
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>/login and /reset-password render the Phase 9 centred-hero pattern: mono eyebrow above 40px Display title above 360px-max form card on dark canvas, gold-brand CTA, mono footer.</done>
</task>

<task type="auto">
  <name>Task 9: Build verification + smoke-test grep audit for 09-01</name>
  <files>(no files modified; verification-only task)</files>
  <read_first>
    - .planning/phases/09-brand-language-alignment-across-portal-dashboard-assessment-/09-RESEARCH.md §Anti-Pattern Smoke Tests "09-01 (Foundations) pre-commit" (the exact grep commands)
    - .planning/phases/09-brand-language-alignment-across-portal-dashboard-assessment-/09-UI-SPEC.md §Acceptance Heuristics (the 10-item checklist)
    - .planning/phases/09-brand-language-alignment-across-portal-dashboard-assessment-/09-CONTEXT.md D-11, D-12 (verification strategy + build at end of each plan)
  </read_first>
  <action>
    Run the RESEARCH §Anti-Pattern Smoke Tests "09-01 (Foundations) pre-commit" greps and confirm the expected outcomes:

    1. `grep -rn "bg-white\|bg-surface\|bg-surface-alt\|#fff\|#f8fafc" src/app/login src/app/reset-password src/components/layout/Sidebar.tsx src/components/layout/Header.tsx src/components/layout/ProgressBar.tsx src/components/layout/NavigationButtons.tsx 2>/dev/null` — expected: no results (Heuristic 1).

    2. `grep -rn "text-navy\|bg-navy\|text-gold\b\|bg-gold\b" src/app/login src/app/reset-password src/components/layout/Sidebar.tsx src/components/layout/Header.tsx src/components/layout/ProgressBar.tsx src/components/layout/NavigationButtons.tsx 2>/dev/null` — expected: no results (Heuristic 5).

    3. `grep -n "Inter_Tight\|JetBrains_Mono" src/lib/fonts.ts` — expected: both present (Heuristic 7).

    4. `grep -n "inter.variable\|jetbrainsMono.variable" src/app/layout.tsx` — expected: both present.

    5. `grep -rn "<button" src/components/layout/Sidebar.tsx src/components/layout/Header.tsx src/components/layout/NavigationButtons.tsx | grep -v "aria-label\|aria-labelledby"` — review each result; expected near-zero (Heuristic 10).

    6. Run `npm run build` — expected exit code 0; no token-resolution warnings; no missing-class errors (D-12).

    Then perform a manual visual review (per UI-SPEC §Acceptance Heuristics #2, #3, #4, #6, #7):
    - `npm run dev`; navigate to `/login` — verify dark canvas, cream text, mono PEAK360 · ACCESS eyebrow, 40px Display title, gold-brand Sign In CTA, mono footer; Tab through inputs to verify gold-brand 45% focus ring.
    - Navigate to `/reset-password` — verify same dark-hero pattern with PEAK360 · RECOVERY eyebrow.
    - Log in; navigate to `/portal` — verify dark sidebar with mono PEAK360 / PORTAL eyebrow, gold-brand active state on current route, mono role chip at bottom.
    - Verify the Phase 8 report at `/portal/assessment/[id]/report` still renders inside a light card surface (Phase 9 only theme-darkens the area outside the card; the report card stays light — this is the D-09 contract; the inner light wrapper is delivered in 09-02 step 9, but the existing PillarsDisplay / ReportShell rendering must not break in 09-01).

    If any heuristic fails, fix in this plan (per D-11/D-12; failures route back into the same plan, not a new one). Document findings (if any) in the plan SUMMARY.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - Heuristic 1 smoke test: no `bg-white|bg-surface|bg-surface-alt|#fff|#f8fafc` in the six 09-01 surface files
    - Heuristic 5 smoke test: no `text-navy|bg-navy|text-gold|bg-gold` in the six 09-01 surface files
    - Heuristic 7 smoke test: Inter_Tight + JetBrains_Mono both wired
    - Heuristic 10 smoke test: every `<button>` in the three layout chrome files carries an `aria-label` or visible text label
    - Manual visual review on `/login`, `/reset-password`, `/portal` passes UI-SPEC heuristics #2 (cream body), #3 (mono eyebrow on hero), #4 (gold-brand fill CTA), #6 (gold-brand 45% focus ring), #7 (Inter Tight body, JetBrains Mono mono)
    - Phase 8 report route still renders without crash (report card surface itself may still be visually-broken at the outer frame until 09-02 step 9; that's expected)
  </acceptance_criteria>
  <done>09-01 foundations are stable: dark canvas committed at segment-layout level, fonts rebound, shared chrome restyled, auth surfaces shipped; the build is clean; 09-02 can proceed against this contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → app shell | Theme class injection happens at the route-segment layout layer (server-rendered RSC); no client-side theme decision |
| build → runtime | next/font/google self-hosts Inter Tight + JetBrains Mono; no runtime fetch from third parties for fonts |
| CSS tokens → DOM | `--color-*` tokens are CSS values only; never interpolated into JSX as raw-HTML escape hatches |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering / Information Disclosure | `theme-dark` class wrapper on segment layouts | accept | Pure CSS class on a `<div>`; no script execution surface; no user-controlled value enters the className. Severity: info. |
| T-09-01-02 | Tampering | New copy strings (`PEAK360 · ACCESS`, `AUTHORISED ACCESS ONLY · ACTIVITY MONITORED`, `Saving…`, etc.) | mitigate | All new copy is hard-coded as React text literals — never rendered through a raw-HTML injection escape hatch. Code review will reject any HTML-injection pattern. Severity: low. |
| T-09-01-03 | Information Disclosure | next/font/google `Inter_Tight` and `JetBrains_Mono` | accept | Both families are already declared via `<link>` in `layout.tsx` for the landing route; promotion to `--font-sans` / `--font-mono` introduces no NEW external font origin. next/font self-hosts assets at build time. Severity: info. |
| T-09-01-04 | Elevation of Privilege | Sidebar role chip rendering `{role}` (COACH / ADMIN / CLIENT) | mitigate | Role value flows from the authenticated session (`getValidSession()` in `portal/layout.tsx`); rendering as plain text in `<MonoEyebrow>` cannot escalate privileges. The chip is presentational; no authorization decision depends on it. Severity: info. |
| T-09-01-05 | Denial of Service | `@keyframes pulse-gold` infinite animation | mitigate | The pulse runs only on the auto-save "Saving…" indicator, which is short-lived (per-save). Disabled under `prefers-reduced-motion: reduce`. No DoS impact. Severity: info. |
| T-09-01-06 | Spoofing | Magic-link / Sign in CTAs (login/page.tsx) | accept | Phase 9 changes visual presentation only; the underlying Phase 7 auth endpoints (`signIn`, `requestMagicLink`, `requestPasswordReset`) are untouched. No new attack surface. Severity: info. |
</threat_model>

<verification>
- `npm run build` exits 0
- All 09-01 surface files (`src/app/login/page.tsx`, `src/app/reset-password/page.tsx`, `src/components/layout/{Sidebar,Header,ProgressBar,NavigationButtons}.tsx`) pass the legacy-token grep (no `text-navy`, `bg-navy`, `text-gold`, `bg-gold`, `bg-white`, `bg-surface*`)
- Manual visual review on `/login`, `/reset-password`, `/portal` confirms UI-SPEC §Acceptance Heuristics #1-#7, #10 on touched surfaces
- Phase 8 report route loads without runtime error (report card frame finalization deferred to 09-02 step 9)
</verification>

<success_criteria>
- 12 new colour tokens + `--font-mono` + `.theme-dark` utility + `@keyframes pulse-gold` present in `globals.css`
- All legacy tokens preserved verbatim
- Inter Tight + JetBrains Mono wired via next/font/google; body className carries both variables
- Three segment layouts (`portal`, `login`, `reset-password`) wrap children in `theme-dark`; reset-password layout is genuinely new
- `<MonoEyebrow>` primitive available at `src/components/ui/MonoEyebrow.tsx`
- Sidebar, Header, ProgressBar, NavigationButtons restyled — no legacy tokens
- `/login` and `/reset-password` render centred dark hero with mono eyebrow + Display title + gold-brand CTA
- Build is clean; smoke-test greps pass; manual visual review passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-brand-language-alignment-across-portal-dashboard-assessment-/09-01-SUMMARY.md` documenting:
- Files modified (with brief restyle summary per file)
- The `<MonoEyebrow>` primitive contract (props + variants) — downstream plans (09-02) will consume this
- Confirmation that all legacy tokens (`--color-navy`, `--color-gold`, etc.) are preserved
- Confirmation that `src/components/report/*` and `src/lib/pdf/**` were NOT touched
- Smoke-test grep results from Task 9
- Any visual-review findings (e.g., the Phase 8 report-card frame appearing on a dark page background — this is expected and will be finalised in 09-02 step 9)
- Deferred / Follow-ups note: `AdminPanel.tsx` (dead code per RESEARCH §Pitfall 2) and `AppShell.tsx` (dead code per RESEARCH §Pitfall 3) intentionally NOT restyled; flagged for a future hygiene phase
</output>
