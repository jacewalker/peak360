---
phase: 9
slug: brand-language-alignment-across-portal-dashboard-assessment-
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-12
---

# Phase 9 — UI Design Contract

> Visual and interaction contract for promoting the landing-page brand language (`src/app/landing.css` / `.v2-root` tokens) into every authenticated surface of Peak360: login, reset-password, sidebar, portal dashboard, assessments list, clients list, client detail, admin pages, and the 11-section assessment form. Generated by gsd-ui-researcher; verified by gsd-ui-checker.

This phase **propagates an existing design** (the landing page) outward. The landing page itself is the visual source of truth — Phase 9 does not re-design it. Phase 8's pillar contract (light surfaces, navy + gold) is the **report-shell convention** and is preserved: the printed/PDF report and the client-facing pillar report continue to use the Phase 8 light palette. Every other authenticated surface adopts the dark brand language defined below.

**Roadmap lock (from STATE.md 2026-05-10):** "dark across all surfaces; 2 fat plans (09-01 foundations+auth, 09-02 working surfaces)."

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (no `components.json`; Tailwind v4 with `@theme inline` in `src/app/globals.css`) |
| Preset | not applicable |
| Component library | none — hand-rolled primitives consistent with `src/components/forms/*`, `src/components/layout/*`, and the Phase 8 `Dialog` primitive. **No new npm dependencies.** |
| Icon library | inline SVG, stroke-width 1.8 (sidebar / nav) and 2 (inline UI), 20px or 16px box, `currentColor`. Existing sidebar SVGs are kept; no `lucide-react` / `heroicons` install. |
| Font (sans) | **Inter Tight** (300 / 400 / 500 / 600) — loaded via Google Fonts in `src/app/layout.tsx` (already linked for landing). Promote to `--font-sans` for the whole app. |
| Font (mono) | **JetBrains Mono** (400 / 500) — loaded via Google Fonts in `src/app/layout.tsx`. Used for eyebrows, numeric readouts, and small caps metadata labels. |
| Font (legacy `--font-sans` "Inter") | **Retired** from authenticated surfaces. Keep the Tailwind variable name `--font-sans` but point it at `Inter Tight`. Section 11 / portal report (Phase 8 contract) and the PDF (Phase 5 contract) stay on their existing typography — both are exempt from the font swap because their printed outputs are locked. |
| Existing landing CSS scope | `.v2-root` selector in `src/app/landing.css` remains the ONLY consumer of the `--v2-*` token names. This phase promotes the same hex values into `@theme inline` under stable token names (see Color) so the rest of the app uses Tailwind utilities, not `.v2-root`-scoped classes. |

**Out-of-scope surfaces (do NOT restyle in Phase 9):**
- `/` (landing page) — already in the target style.
- `/portal/assessment/[id]/report` — Phase 8 pillar report (light surfaces, navy + gold by D-01..D-30). Phase 9 only updates the report **frame** (page background outside the printable card and the top brand strip) so navigating between dashboard and report does not feel like switching apps. The pillar cards, modal, marker rows, and PDF are untouched.
- `src/lib/pdf/**` — Phase 5 PDF renderer.

---

## Spacing Scale

Tailwind's default 4px scale is the source of truth. Declared values for this phase:

| Token | Value | Tailwind | Usage in this phase |
|-------|-------|----------|---------------------|
| xs | 4px | `1` | Icon-to-text gaps, mono-eyebrow bullet-to-line spacing, focus-ring inset |
| sm | 8px | `2` | Compact inline gaps (form field rows, button icon gaps, toggle pill internal pad) |
| md | 16px | `4` | Default field padding, card internal padding (mobile), nav item vertical pad |
| lg | 24px | `6` | Card padding (desktop), gap between cards in dashboard grid, form-section spacing |
| xl | 32px | `8` | Page heading bottom margin, sidebar to main-content gap |
| 2xl | 48px | `12` | Major section breaks on dashboard / clients / admin pages |
| 3xl | 64px | `16` | Page-level top/bottom padding on portal shell |
| 4xl | 96px | `24` | Hero-style page intros (login, reset-password, "empty dashboard" first-run); roadmap-locked landing-style breathing room |

**Exceptions:**
- **Sidebar collapsed-rail width**: 56px (Tailwind `lg:pl-56` — preserved from existing implementation). Not a spacing token; it's the rail's fixed dimension.
- **Touch targets**: Any tappable icon-only button (sidebar mobile toggle, dialog close, "delete" icon in admin tables) must be **44 × 44px** minimum hit area, even if the visual icon is 16-20px. Padding fills to 44px.
- **Mono-eyebrow line decoration**: `28px × 1px` gold line (matches `.v2-root .eyebrow-line` from `landing.css`). Decorative; not a spacing token.
- **Container max-width**: 1280px for the portal main area (matches landing `--max`). Section 11 report keeps its own existing max-width.
- **Container side padding**: `clamp(20px, 4vw, 56px)` (matches landing `--pad`) on the portal main area outer container. Inner cards use the token scale above.
- **Form-field row height**: 48px on the assessment form (Section 1–11) to align with the dark theme's heavier visual weight. Mobile keeps 48px; do not shrink.
- **Sidebar nav item**: 44px tap height, 12px horizontal padding, 8px gap icon→label — matches the existing implementation, do not change.

---

## Typography

Four roles + mono utility row. Two weights for sans (400 regular, 500 medium); mono uses 400/500. Body line-height 1.55; heading line-height 1.0–1.05; mono line-height 1.4.

| Role | Size | Weight | Line Height | Family | Usage |
|------|------|--------|-------------|--------|-------|
| Body | 15px | 400 | 1.55 | Inter Tight | Default running text in cards, descriptions, form helper text. Slightly tighter than Phase 8's 16px because dark surfaces read denser; mirrors landing `.section-sub` / `.t-quote` body sizing. |
| Label | 13px | 500 | 1.4 | Inter Tight | Field labels, button labels, table column headers, sidebar nav items, primary/secondary CTA copy. Letter-spacing `0.02em` for buttons (matches landing `.btn`). |
| Heading | 20px | 500 | 1.15 | Inter Tight | Card titles, modal headings, section labels inside dashboard widgets, assessment-section heading. Letter-spacing `-0.015em`. |
| Display | 40px | 500 | 1.0 | Inter Tight | Page hero titles on dashboard, clients, admin landing, and the first frame of the assessment flow. Letter-spacing `-0.03em`. Mobile (< 640px) downscales to 32px. |

**Mono utility row** (single role, used wherever the landing page uses `.eyebrow` / `.link-mono` / `.hero-meta-label`):

| Use | Size | Weight | Letter Spacing | Transform | Color |
|-----|------|--------|----------------|-----------|-------|
| Eyebrow | 11px | 500 | 0.18em | uppercase | `--color-gold-brand` (see Color) |
| Meta label | 10px | 400 | 0.16em | uppercase | `--color-text-faint` |
| Numeric readout | 14px | 500 | 0 | none | `--color-text-strong`; `font-variant-numeric: tabular-nums` |

**Numeric exception**: large numerals (dashboard "12 active assessments" hero counter; assessment progress percentage; admin table row counts) use **48px / weight 500 / line-height 0.95 / `font-variant-numeric: tabular-nums`**. Mobile downscales to 36px. This is a deliberate carry-over from landing's `.hero-meta-num` / `.age-value` pattern.

**Form-field input typography**: 15px / 400 / line-height 1.4 (matches Body). Placeholders at `--color-text-faint`. Input height 48px (spacing exception above). This applies across all 11 assessment sections; form components in `src/components/forms/` keep their API but switch text/bg tokens.

**Section 11 report and the Phase 8 portal report**: governed by their existing contracts. Untouched.

---

## Color

The 60 / 30 / 10 split for this phase. All hex values are promoted from `src/app/landing.css` (`.v2-root` tokens) into `@theme inline` under stable Tailwind-readable names.

| Role | Value | Token | Usage |
|------|-------|-------|-------|
| Dominant (60%) | `#0a0a0b` | `--color-bg` | Body background on every authenticated surface (sidebar shell, portal pages, assessment form pages, admin pages, login, reset-password). Replaces the existing `#f8fafc` background. |
| Dominant alt 1 (still ~60%) | `#0e0e10` | `--color-bg-2` | Sidebar interior, sticky page headers, scrolled-nav backdrop. |
| Dominant alt 2 (still ~60%) | `#131316` | `--color-bg-3` | Cards, form fields' default background, table row hover. |
| Secondary (30%) | `#ece5d3` (cream) | `--color-text` | Default text color across all dark surfaces. Replaces the existing `#1a202c` foreground. |
| Secondary supports | `rgba(236, 229, 211, 0.62)` | `--color-text-dim` | Secondary copy (descriptions, helpers, table cell secondary lines). |
| Secondary supports | `rgba(236, 229, 211, 0.38)` | `--color-text-faint` | Meta labels, placeholders, disabled state, mono small caps. |
| Accent (10%) | `#c9a24a` (gold-dark) | `--color-gold-brand` | See "Gold reserved for" list below. **This is the only accent in Phase 9.** It replaces the existing `#F5A623` for all authenticated surfaces. |
| Accent supports | `#e8d6a8` (champagne) | `--color-champagne` | Hover state on filled gold buttons; ring colour on focused gold inputs. Never used as a fill colour on its own surface — only paired with gold-brand. |
| Borders / lines | `rgba(232, 214, 168, 0.10)` | `--color-line` | Default divider, card border, table-row divider. |
| Borders / lines (strong) | `rgba(232, 214, 168, 0.20)` | `--color-line-2` | Hover divider, focused field border, secondary CTA border. |
| Destructive | `#d97557` (warm coral, from landing readout `.bad`) | `--color-danger` | Destructive admin actions only (delete user, clear prescription confirm, "remove from coach"). Confirmation pattern uses inline dialog. |
| Status: good (info) | `#7fb37f` (sage, from landing readout `.good`) | `--color-status-good` | Success toasts, "Saved" indicator, healthy auto-save state. |
| Status: warn | `#c9a24a` (same as gold-brand) | n/a | Warning toasts reuse gold-brand. Do NOT introduce a new amber. |
| Legacy navy | `#1a365d` | `--color-navy` | **Retained only inside `/portal/assessment/[id]/report` (Phase 8) and PDF (Phase 5).** Not used in Phase 9 surfaces. |
| Legacy bright gold | `#F5A623` | `--color-gold` (existing) | **Retained only inside `/portal/assessment/[id]/report` (Phase 8) and PDF (Phase 5).** Not used in Phase 9 surfaces. |
| Rating tier palette (5-tier marker colours) | from `src/types/normative.ts` | preserved verbatim | Used only inside the assessment-form `Section 11` and admin marker editors. Untouched by this phase. |

**Token-rename discipline**: existing `--color-navy`, `--color-gold`, `--color-navy-light`, `--color-gold-light`, `--color-gold-dark`, `--color-navy-950`, `--color-gradient-*` stay in `globals.css` for Phase 5 / Phase 8 compatibility. The **new** tokens above are added alongside them. No existing token gets a new value.

**Gold-brand (`#c9a24a`) reserved for:**
- Page hero "eyebrow" labels (mono, 11px, uppercase) on every authenticated page
- The 28×1px eyebrow line decoration
- The vertical 1px gold accent on focused form fields (replaces the existing gold-tinted ring)
- Active sidebar nav item: 2px left border, gold; label colour stays cream
- Primary CTA fill (filled button: gold-brand bg, `#0a0a0b` text — "ink on gold", matches landing `.btn-gold`)
- Number-strong moments: large counters in dashboard hero ("12"), progress percentage in the assessment form ("46%")
- Mono `link-mono` hover state (text colour transitions from `--color-text-faint` to gold)
- Status pulse / dot animations (e.g. "live" indicator on the assessment auto-save status — `box-shadow 0 0 0 0 rgba(201,162,74,0.6)` pulse)
- Underline accent under the sidebar logo on hover
- NOTHING ELSE. Gold is **never** used as a card background, as a heading colour for body text, or as a "subtle" tint on neutral surfaces. The point of the dark palette is that gold is precious.

**60 / 30 / 10 audit:** the cream text (`#ece5d3`) is the secondary colour by surface area on a dark canvas. Although it appears on most pixels of text, the **canvas itself** is 60%+ of the rendered viewport — three near-black surfaces (`bg`, `bg-2`, `bg-3`) collectively dominate. Cream is the foreground of secondary elements (text, dividers as transparent cream). Gold is reserved for the accents above and stays under 10% of pixel area.

**Anti-patterns (prevent drift):**
- Do NOT use `--color-navy` on a Phase 9 dark surface. Navy is for Phase 8 report only — they coexist via route.
- Do NOT introduce a "light mode" toggle. The dark surfaces are not theme-switchable.
- Do NOT use the bright `--color-gold` (`#F5A623`) on dark surfaces. It vibrates against `#0a0a0b`. Use `--color-gold-brand` (`#c9a24a`).
- Do NOT promote `#ece5d3` to a "card surface" colour. Cream is text-only. Cards stay `#131316`.
- Do NOT use `--color-status-good` or `--color-danger` for non-status decoration (e.g. don't use sage for a generic icon tint).

---

## Copywriting Contract

Phase 9 changes appearance, not information architecture. **Copy is not re-authored** — existing strings on login, dashboard, clients, admin, and the 11 assessment sections are preserved verbatim. This contract specifies only the new copy that the visual changes introduce.

### New copy introduced by Phase 9

| Surface | Element | Copy |
|---------|---------|------|
| Sidebar (collapsed rail, top) | Logo eyebrow (mono, 10px) | **PEAK360 / PORTAL** |
| Sidebar (logged-in user section) | User role chip (mono, 9px, uppercase) | **{role}** — one of `COACH`, `ADMIN`, `CLIENT`, exactly that copy |
| Login page hero | Mono eyebrow above the form card | **PEAK360 · ACCESS** |
| Login page footer line (replaces existing "Authorised access only…") | Mono footer | **AUTHORISED ACCESS ONLY · ACTIVITY MONITORED** (uppercase, letter-spacing 0.16em, gold-brand) |
| Reset-password page hero | Mono eyebrow | **PEAK360 · RECOVERY** |
| Dashboard hero (replaces unstyled "Dashboard" h1) | Mono eyebrow | **YOUR PORTAL · {ROLE}** |
| Dashboard hero | Display title (preserves existing intent) | **Welcome back, {first_name}.** (was "Dashboard"; planner may keep "Dashboard" if `first_name` is unavailable) |
| Dashboard hero | Subtitle (mono row beneath title) | **{today_date_iso} · {n} ACTIVE · {m} COMPLETED** (mono, 10px, uppercase) |
| Clients page hero | Mono eyebrow | **PEOPLE · CLIENTS** |
| Assessments page hero | Mono eyebrow | **PORTAL · ASSESSMENTS** |
| Admin landing hero | Mono eyebrow | **ADMIN · CONTROL** |
| Admin sub-page hero (each of /pillars, /users, /invitations, /audit-logs, /normative) | Mono eyebrow | **ADMIN · {SECTION_NAME}** (uppercase) |
| Assessment form page hero (per section) | Mono eyebrow above the section title | **SECTION {N} / 11 · {SECTION_LABEL}** (uppercase). Replaces the existing "Section X of 11" line; copy is identical in spirit, mono styled. |
| Assessment form auto-save indicator | Mono status, gold-pulse dot | **SAVED · {relative_time}** when clean; **SAVING…** when in-flight; **UNSAVED CHANGES** in danger colour when offline failure. |
| Loading state (default skeleton replacement) | Mono | **LOADING…** (uppercase, letter-spacing 0.18em, gold-brand) — replaces the empty white skeleton frames. |

### Empty state

| Surface | Empty state copy |
|---------|------------------|
| Dashboard — no assessments yet (coach) | **Heading:** Nothing here yet. **Body:** Create your first assessment to start tracking a client. **CTA:** Start new assessment |
| Dashboard — no assessments yet (client) | **Heading:** Your first report will appear here. **Body:** Your coach will let you know when it is ready. **CTA:** none |
| Clients page — no clients yet | **Heading:** No clients yet. **Body:** Invite a client by email to begin. **CTA:** Invite client |
| Assessments page — no assessments | **Heading:** No assessments in scope. **Body:** Adjust your filter or create a new assessment. **CTA:** Start new assessment |
| Admin users page — empty group | **Heading:** No {role}s yet. **Body:** Invite someone to populate this list. **CTA:** Invite {role} |

### Error state

| Surface | Error copy |
|---------|-----------|
| Login — bad credentials | (existing) **Invalid email or password. Try again or reset your password.** — preserved verbatim |
| Login — network failure | (existing) **We couldn't reach the server. Check your connection and try again.** — preserved verbatim |
| Auto-save — offline | **Couldn't save. We'll retry — your work is held in this browser.** (danger colour, with mono `UNSAVED CHANGES` chip) |
| Generic page fetch failure | **Couldn't load this page. Refresh, or contact support if it persists.** |

### Primary CTAs (button labels — preserve existing intent, change visuals only)

| Surface | CTA label (unchanged) | Visual treatment |
|---------|----------------------|-------------------|
| Login | **Sign In** | gold-brand fill, ink text, 13px / 500 / 0.04em |
| Login (magic link row) | **Email me a sign-in link** | ghost button: transparent bg, `--color-line-2` border, cream text, hover border + text gold-brand |
| Reset-password | **Reset password** | gold-brand fill |
| Dashboard hero | **Start new assessment** | gold-brand fill |
| Assessment form footer | **Save & continue** (existing) | gold-brand fill |
| Assessment form footer | **Back** | ghost button |
| Admin save buttons | **Save changes** / **Save plan** (existing from Phase 8) | gold-brand fill on dark; Phase 8 light-surface saves on the report-edit pages stay navy-filled |

### Destructive actions

Phase 9 introduces **no new destructive actions.** Existing destructive actions (clear prescription, remove user, delete invitation) keep their confirmation copy. Visual change only:
- Confirm button: `--color-danger` fill, `#0a0a0b` text, 13px / 500.
- Cancel button: ghost variant.
- Dialog shell: `--color-bg-3` panel, `--color-line-2` border, mono eyebrow above the heading reads **DESTRUCTIVE · CONFIRM**.

### Tone & voice (preserved from current product)

- Direct, second-person ("Your coach hasn't…").
- No emoji.
- No exclamation marks in error states.
- Mono is for system / metadata voice (timestamps, counts, role labels, statuses). Sans is for human voice (descriptions, prompts, CTAs).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — shadcn is not initialised in this project | not applicable |
| third-party | none | not applicable |

No third-party registry blocks enter the codebase in this phase. The existing `Dialog` primitive (Phase 8 `src/components/ui/Dialog.tsx`), the existing form components in `src/components/forms/`, and the existing `Sidebar` / `Header` / `AppShell` / `ProgressBar` / `NavigationButtons` in `src/components/layout/` are restyled in place. No `npx shadcn add` invocations. No new npm dependencies.

---

## Layout & Responsive Contract

Breakpoints follow Tailwind's defaults: `sm 640`, `md 768`, `lg 1024`, `xl 1280`.

| Surface | Mobile (< 768px) | Desktop (≥ 1024px) |
|---------|------------------|---------------------|
| Portal shell | Sidebar collapses to a top bar with hamburger; main content full-width, 16px gutter | Sidebar fixed-rail 56px wide on the left (existing `lg:pl-56`); main content max-width 1280px, 32–56px gutter (clamp), centred |
| Sidebar rail | Full-screen overlay sheet when opened (existing pattern, restyled — backdrop scrim 70% black, slide from left over 220ms, `prefers-reduced-motion` reduces to instant) | Fixed rail `bg-2`, 1px right border `--color-line`, logo block top 64px, nav items, user block bottom |
| Dashboard | Single column, 16px gap, hero stacked above counter strip | 12-col grid; hero spans 7, counter strip spans 5 to its right; below the hero, recent-assessments table spans 12 |
| Clients page | Single column list of client cards | 3-col grid of client cards at `xl`, 2-col at `lg`, 1-col below |
| Assessments page | Single-column card list | Single column table (full-width), columns: client, section, status (mono pill), updated, action |
| Admin pages | Single-column form stack | Single-column form stack, max-width 720px, centred (same as existing) |
| Assessment form page | Single column, full-width fields | Two-column form grid where existing copy already uses one (no information re-architecture) — max content width 880px centred, ProgressBar sticky to top of main content |
| Login / Reset-password | Full-screen card centred, 360px max card width, page hero gold-eyebrow + display title above the card | Same — these screens are always centred regardless of viewport |
| Section 11 portal report | UNCHANGED (Phase 8 contract) | UNCHANGED (Phase 8 contract) |

**Sticky behaviour:**
- Dashboard / Clients / Assessments page: page hero is NOT sticky.
- Assessment form: ProgressBar IS sticky (offset 0, `bg-2` with bottom 1px `--color-line` border, 56px height).
- NavigationButtons in the assessment form: sticky to bottom on mobile (full-width), inline on desktop. 64px height, `bg-2` background, 1px top border.
- Toasts (success / error): top-right, 24px inset, mono eyebrow above the message, gold-pulse dot for live "Saving…" state.

**Focus & keyboard contract** (a11y):
- All interactive elements: visible focus ring `box-shadow: 0 0 0 2px rgba(201,162,74,0.45)` (gold-brand at 45% alpha) inset 0. Replaces the existing `0 0 0 2px rgba(245, 166, 35, 0.25)` ring in `globals.css` for Phase 9 surfaces; the existing ring is preserved only inside the Phase 8 report shell.
- Sidebar mobile sheet: `Esc` closes, focus trap when open, focus returns to hamburger on close (existing implementation, restyled).
- Form fields: tab order matches DOM, no `tabindex` overrides, placeholders never replace labels.
- Buttons disabled state: 40% alpha, `cursor: not-allowed`, no hover transitions.

**Reduced motion:** at `prefers-reduced-motion: reduce`, replace all transforms with instant state changes. Sidebar slide → instant. Auto-save pulse dot → static dot at peak colour. Page transitions → no fade.

**Print** (`@media print`): authenticated pages other than the Phase 8 report use `display: none` for sidebar + nav, white background, black text — same as the existing print contract. The PDF pipeline (`src/lib/pdf/**`) is untouched. The existing `globals.css` print block stays.

---

## Component Inventory (downstream planner reference)

No new components are introduced. Every existing component is restyled in place. The planner expands these into discrete tasks; the executor implements against the contracts above.

| Component | Path | Change kind | Notes |
|-----------|------|-------------|-------|
| `globals.css` | `src/app/globals.css` | **Token additions** | Add `--color-bg`, `--color-bg-2`, `--color-bg-3`, `--color-text`, `--color-text-dim`, `--color-text-faint`, `--color-gold-brand`, `--color-champagne`, `--color-line`, `--color-line-2`, `--color-danger`, `--color-status-good`. Add `--font-mono` mapped to `"JetBrains Mono"`. Repoint `--font-sans` to `"Inter Tight"`. Move the global `body` background from `--color-background` to `--color-bg` **gated** on a route check (see "Token application" below). Preserve every existing `--color-*` token unchanged. |
| `layout.tsx` | `src/app/layout.tsx` | **Font wiring** | Keep the existing Google Fonts `<link>` for Inter Tight + JetBrains Mono (already loaded for landing). Update the `body` className to include the mono variable. The page-level theme decision is made by the route segment, not the root layout. |
| Token application boundary | new file `src/app/portal/layout.tsx` (existing) + `src/app/login/layout.tsx` (existing) + `src/app/reset-password/layout.tsx` (NEW) + `src/app/assessment/[id]/layout.tsx` (existing) | **Theme wrapping** | Each of these layouts wraps its children in `<div className="theme-dark">` (a class to be added in `globals.css` that sets `background: var(--color-bg); color: var(--color-text);`). The root `<body>` no longer hard-sets the light background — it inherits from the segment layout. This isolates the Phase 8 report route, which keeps a light wrapper. |
| `Sidebar` | `src/components/layout/Sidebar.tsx` | **Restyle in place** | Background `bg-2`, border-right `--color-line`, nav items mono `link-mono` styling on inactive (uppercase 11px gold-brand on hover), active state gold-brand left border 2px + label cream weight 500. Logo block uses the landing's existing 64px height. User block bottom: avatar + name (sans 13px) + role chip (mono 9px gold-brand). No layout changes. |
| `Header` | `src/components/layout/Header.tsx` | **Restyle in place** | Inside the assessment form, becomes a thin top bar (`bg-2`, 56px height, 1px bottom border `--color-line`). Logo left, client name + section indicator right (mono eyebrow "CLIENT · {name}" plus sans 15px). |
| `ProgressBar` | `src/components/layout/ProgressBar.tsx` | **Restyle in place** | 4px track height, `--color-line` background, gold-brand fill, mono percentage label right-aligned ("46%" sans 48px on desktop hero variant; mono 10px inline variant in sticky bar). |
| `NavigationButtons` | `src/components/layout/NavigationButtons.tsx` | **Restyle in place** | Back = ghost button, Save & continue = gold-brand fill button. Both use the Button visual contract from Color §Gold-brand reserved-for list. |
| `FormField` / `SelectField` / `RadioGroup` / `SliderField` / `SignaturePad` / `FileUploadZone` | `src/components/forms/*` | **Restyle in place** | Input bg `--color-bg-3`, border `--color-line`, text cream, placeholder faint, focus ring gold-brand 2px @ 45%. Labels 13px / 500 / cream. Helper / error text 13px / 400 / `--color-text-dim` (helper) or `--color-danger` (error). Radio + checkbox accent `--color-gold-brand`. Slider thumb gold-brand (existing thumb implementation works as-is once `--color-gold` swap is gated). |
| `Section1`…`Section11` | `src/components/sections/Section{N}.tsx` | **Restyle in place** | Section heading uses the Heading role (20px / 500 / cream / `-0.015em`). Each section gets a mono eyebrow above the heading ("SECTION {N} / 11 · {LABEL}"). No information architecture change. |
| Phase 8 `Dialog` | `src/components/ui/Dialog.tsx` | **Token swap only** | Panel bg switches from white to `--color-bg-3`, scrim bg `rgba(10, 10, 11, 0.7)`. Used only by destructive confirmations + assessment-form modals. The Phase 8 portal report keeps its own light dialog scope inside the report route. |
| Toast (existing pattern from Phase 7) | inline | **Restyle in place** | Panel bg `--color-bg-3`, border `--color-line-2`, mono eyebrow `SAVED` / `ERROR` / `INFO` (uppercase 10px), body 13px / 400. Live "saving…" toast has gold pulse dot. |
| `AdminPanel` | `src/components/layout/AdminPanel.tsx` | **Restyle in place** | Switch the assessment-form admin-only sidebar panel to dark tokens. |
| Recharts (`src/components/charts/*`) | client-trend charts in `/portal/page.tsx` | **Restyle in place** | Axis text mono 10px `--color-text-faint`, grid lines `--color-line`, default series stroke gold-brand, secondary stroke champagne, tooltip bg `--color-bg-3` with `--color-line-2` border. |
| Phase 8 `PillarCard` / `PillarsGrid` / `PillarModal` / `DetailedMarkerResultsDisclosure` / PDF `PillarsPage` | `src/components/report/*`, `src/lib/pdf/*` | **UNCHANGED** | Phase 8 contract is sovereign inside the report route + PDF. |

---

## Token-Naming Map (executor reference)

Authoritative variable-to-purpose map for `globals.css`. Add the right column verbatim; never duplicate hex values in component files.

| New token | Hex / value | Maps from landing `.v2-root` | Phase 9 purpose |
|-----------|-------------|------------------------------|-----------------|
| `--color-bg` | `#0a0a0b` | `--bg` | Page canvas |
| `--color-bg-2` | `#0e0e10` | `--bg-2` | Sidebar, sticky bars, scrolled-nav |
| `--color-bg-3` | `#131316` | `--bg-3` | Cards, inputs, dialog panels |
| `--color-text` | `#ece5d3` | `--text` (cream) | Default text |
| `--color-text-dim` | `rgba(236,229,211,0.62)` | `--text-dim` | Secondary text |
| `--color-text-faint` | `rgba(236,229,211,0.38)` | `--text-faint` | Meta, placeholder |
| `--color-gold-brand` | `#c9a24a` | `--gold` | Accent |
| `--color-champagne` | `#e8d6a8` | `--champagne` | Gold hover companion |
| `--color-line` | `rgba(232,214,168,0.10)` | `--line` | Subtle divider |
| `--color-line-2` | `rgba(232,214,168,0.20)` | `--line-2` | Stronger divider, hover border |
| `--color-danger` | `#d97557` | `.readout-value.bad` | Destructive |
| `--color-status-good` | `#7fb37f` | `.readout-value.good` | Success |
| `--font-sans` (rebound) | `"Inter Tight", system-ui, -apple-system, sans-serif` | `--font-sans-v2` | Default font |
| `--font-mono` (new) | `"JetBrains Mono", ui-monospace, monospace` | `--font-mono-v2` | Eyebrows, meta, numerics |

---

## Acceptance Heuristics (for ui-checker / ui-auditor)

A surface is "Phase-9 aligned" iff:

1. Background is one of `--color-bg`, `--color-bg-2`, `--color-bg-3`. No `#fff`, `#f8fafc`, `--color-background`, `--color-surface`, or `--color-surface-alt` in the rendered DOM (inspect the live page) **outside** the report route.
2. Default text colour resolves to `--color-text` (cream). Body text never resolves to the legacy `#1a202c`.
3. At least one mono eyebrow appears above each page hero on dashboard, clients, assessments, admin/*, and section-{1..11} pages.
4. The primary CTA on the page is gold-brand fill with `#0a0a0b` text. The label uses sentence case for human voice (e.g. "Start new assessment") not uppercase. (Uppercase is reserved for mono eyebrows / meta.)
5. No `--color-navy` or `--color-gold` (legacy bright gold) is used. (The report route is exempt and lives at `/portal/assessment/[id]/report`.)
6. Focus ring on any interactive element resolves to gold-brand @ 45%.
7. Font-family on body resolves to Inter Tight. Font-family on every mono element (eyebrow, meta, numeric readout) resolves to JetBrains Mono.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
