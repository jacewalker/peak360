---
phase: 10-section-11-pdf-brand-language-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/components/sections/Section11.tsx
autonomous: false
requirements:
  - REQ-9.1
must_haves:
  truths:
    - "Section 11 renders as a light cream/navy card inside the dark portal frame (matches Phase 8 /report page pattern)"
    - "Zero hex literals matching #1a365d|#F5A623 remain in Section11.tsx"
    - "Zero text-white|bg-white utility classes remain in Section11.tsx"
    - "Phase 8 tier-pill palette on the Results Overview grid is preserved verbatim (emerald-600 / blue-600 / gray-500 / amber-500 / red-600 etc.)"
    - "Phase 8 emerald/amber/red status status hexes (#10b981 / #f59e0b / #ef4444 etc.) on tier dots and pillar pills remain — only legacy brand navy/gold are swept"
    - "globals.css exposes paper / ink / ink-dim / ink-faint / line-light tokens consumable as Tailwind utilities (bg-paper, text-ink, etc.)"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Phase 10 print-safe alias tokens (paper, ink, ink-dim, ink-faint, line-light) added to @theme inline"
      contains: "--color-paper"
    - path: "src/components/sections/Section11.tsx"
      provides: "Section 11 longevity report retokenized to Phase 8 light-card vocabulary, wrapped in light card surface"
      min_lines: 600
  key_links:
    - from: "src/components/sections/Section11.tsx"
      to: "globals.css @theme tokens"
      via: "Tailwind utilities bg-paper / text-ink / text-gold-brand / border-line-light"
      pattern: "bg-paper|text-ink|text-gold-brand"
    - from: "src/components/sections/Section11.tsx outer container"
      to: "section/[num]/page.tsx dark portal frame"
      via: "self-contained light card wrapper that mirrors /report page line 122 inner wrapper"
      pattern: "bg-paper rounded-2xl"
---

<objective>
Retokenize `src/components/sections/Section11.tsx` (the in-app Complete Longevity Analysis report rendered at `/portal/assessment/[id]/section/11`) so it renders as a light report card sitting inside the dark portal frame — matching the Phase 8 pattern already shipped at `/portal/assessment/[id]/report` (see 09-PATTERNS.md §10). Eliminate every legacy `#1a365d`, `#F5A623`, `text-white`, and `bg-white` literal in this file.

Purpose: Phase 9's brand sweep skipped Section 11 because it has a dense, custom-styled report layout that didn't match the form-component sweep template. The page currently renders bare on the dark portal canvas with navy/gold/white tokens that produce no contrast — the user's visible complaint. This plan resolves it without touching the Phase 8 sovereign report components (`src/components/report/*`).
Output: A Section 11 that visually matches the Phase 8 `/report` page surface (cream/navy/gold-brand light card) and a small additive token expansion in `globals.css` so the print-safe alias tokens have stable Tailwind utility names.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-section-11-pdf-brand-language-alignment/10-CONTEXT.md
@.planning/phases/09-brand-language-alignment-across-portal-dashboard-assessment-/09-PATTERNS.md

# Phase 8 light report card — TOKEN SOURCE OF TRUTH
@src/app/portal/assessment/[id]/report/page.tsx
@src/components/report/ReportShell.tsx
@src/components/report/PillarCard.tsx

# The file we are rewriting
@src/components/sections/Section11.tsx

# Tokens we will extend (additive only — do not touch existing entries)
@src/app/globals.css

# Where Section 11 mounts (already wraps in dark portal frame + adds MonoEyebrow above)
@src/app/portal/assessment/[id]/section/[num]/page.tsx

<interfaces>
<!-- The Phase 8 reference implementation — copy this surface pattern -->
<!-- Source: src/app/portal/assessment/[id]/report/page.tsx lines 102-131 -->
<!-- The dark outer chrome sits on the portal frame; the inner light wrapper is the report card.
     Section 11 needs the SAME inner wrapper applied to its own content. -->

Outer (dark portal frame, already provided by section/[num]/page.tsx line 201-206):
```tsx
<main className="flex-1 max-w-6xl mx-auto w-full px-4 sm:px-6 py-6 sm:py-8">
  <MonoEyebrow variant="hero" as="div" className="mb-3">
    SECTION 11 / 11 · LONGEVITY ANALYSIS
  </MonoEyebrow>
  <Section11 assessmentId={id} />   {/* ← The current outer <div className="bg-white"> needs to behave like Phase 8's inner light wrapper */}
</main>
```

Phase 8 inner light wrapper (the model — line 122 of /report page):
```tsx
<div className="bg-white rounded-2xl p-6 sm:p-8 my-6 shadow-sm">
  <ReportShell ... />
</div>
```

Phase 8 token usage in actual report components (cream/navy/gold-brand values our heuristic excludes only because they appear via TOKEN form, not hex):
- `text-navy` (TIER) — used in ReportShell.tsx:52, PillarCard.tsx:118, 125
- `bg-white` — used in PillarCard.tsx:99, 117 (BUT this plan's heuristic forbids `bg-white` in Section11.tsx, so swap to `bg-paper` token)
- Phase 8 tier hexes (#10b981, #3b82f6, #6b7280, #f59e0b, #ef4444 and 50-suffixed surfaces) — PRESERVED VERBATIM per CONTEXT decisions
</interfaces>

<token_swap_table>
<!-- Section11.tsx swap reference. Apply in Task 2. -->
<!-- LEFT = current literal in Section11.tsx; RIGHT = replacement.
     Each replacement is a Tailwind utility (preferred) OR an inline style referencing a CSS var. -->

| Current (banned) | Replacement |
|------------------|-------------|
| `bg-white` (line 294 outer; line 461 tier-card; line 558 insight-card) | `bg-paper` |
| `text-[#1a365d]` (lines 94, 274, 278, 362, 451, 466, 486, 505, 561) | `text-ink` (Tailwind utility from new --color-ink token) |
| `text-[#1a365d]/70` (line 505) | `text-ink/70` |
| `bg-[#1a365d]` (line 602 button) | `bg-ink` |
| `hover:bg-[#2d5986]` (line 602 button hover) | `hover:bg-ink/85` (no separate navy-light token in print-safe palette; alpha shift) |
| `bg-[#F5A623]` (lines 93, 303, 304, 315, 361, 450, 485, 567, 629 — the gold accent bars + button) | `bg-gold-brand` (existing Phase 9 token, #c9a24a) |
| `from-[#F5A623]` `to-[#d4891a]` (line 559 gradient) | `from-gold-brand to-champagne` (existing Phase 9 tokens) |
| `text-[#1a202c]` (lines 104, 274, 278, 396, 405, 411, 417, 430, 568) | `text-ink` |
| `text-[#64748b]` (lines 103, 114, 280, 382, 395, 404, 410, 416, 433, 439, 453, 475, 562, 582) | `text-ink-dim` |
| `text-[#94a3b8]` (lines 285, 386, 582) | `text-ink-faint` |
| `text-[#cbd5e1]` (line 589) | `text-ink-faint/60` |
| `bg-[#f8fafc]` (lines 346, 371, 427, 539) | `bg-paper-alt` (new token, #f8fafc) |
| `bg-[#f1f5f9]` (line 517) | `bg-paper-alt` (same alias) |
| `bg-[#e2e8f0]` gradient stop (line 506) | `from-line-light to-transparent` (use `from-line-light` Tailwind utility) |
| `bg-gradient-to-br from-[#0f2440] via-[#1a365d] to-[#2d5986]` (line 296 cover) | Replace cover entirely — see "Cover decision" below |
| `text-white` (lines 296, 308 alt text, 310, 312, 318, 322, 326, 330) | Eliminated when cover is replaced |
| `text-white/60` (line 310), `text-white/50` (lines 318, 322, 326, 330), `bg-white/20` (line 309 divider) | Eliminated when cover is replaced |
| `radial-gradient(... #F5A623 ...)` (lines 298, 301) | Eliminated when cover is replaced |
| `bg-[#F5A623] text-[#1a365d]` (line 629 — Save & Complete button) | `bg-gold-brand text-ink` |
| `hover:bg-[#f7bc5a]` (line 629) | `hover:bg-champagne` |

**Cover decision (Task 2):** Replace the dark-gradient cover block (lines 295–335) with a light header that mirrors the Phase 8 `/report` page chrome (line 108–120): a flex row containing a left-side title (`text-[20px] font-medium text-ink tracking-[-0.015em]`) and a right-side gold-brand "Export PDF" CTA. The client/date/age/gender grid below becomes a four-cell mono-eyebrow meta strip in `text-ink-dim` against `bg-paper-alt` rather than against the dark gradient. Page-level `<MonoEyebrow>` "SECTION 11 / 11 · LONGEVITY ANALYSIS" is already provided by `section/[num]/page.tsx` line 202-204 — do not duplicate inside Section 11.

**Preserved (must NOT change — all per CONTEXT decisions):**
- `TIER_DOT` map at lines 30–36 — Phase 8 tier hex palette
- `TIER_ROW_BG` / `TIER_ROW_BORDER` / `TIER_TEXT` maps at lines 38–60 — Phase 8 tier-row backgrounds & text
- `TierPill` background palette at lines 76–82 — Phase 8 tier pill colors
- All status hexes for medical screening dots (`bg-red-500`, `bg-emerald-500`, `bg-amber-100`, `bg-red-50`, etc.) at lines 113, 364, 394, 429 — status colors are sovereign per Phase 9 D-16
- PillarsDisplay import — Phase 8 component, untouched
- Spinner block at lines 236–248 already uses `border-gold-brand` and `text-text-dim` (Phase 9 dark tokens) — KEEP AS IS (this is the loading state that renders BEFORE the light card is mounted, so it sits on the dark portal frame)
</token_swap_table>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add print-safe alias tokens to globals.css</name>
  <files>src/app/globals.css</files>
  <action>
Open `src/app/globals.css`. Inside the existing `@theme inline { ... }` block (currently lines 3–59), append a new commented section immediately AFTER the Phase 9 brand language tokens (after line 58 `--font-mono: "JetBrains Mono", ui-monospace, monospace;`) but BEFORE the closing `}` of the @theme block. Append these tokens verbatim:

```
  /* ──────────────────────────────────────────────────────────────────────
     Phase 10 Print-Safe Alias Tokens (additive — Section 11 + PDF light card)
     Stable Tailwind utility names for the Phase 8 light-card palette
     (cream paper surface + navy ink + dim/faint text + light dividers).
     Values mirror existing legacy tokens by design — Phase 8 sovereign
     components (src/components/report/*) keep using text-navy/bg-white;
     Section 11 + PDF reference these new aliases so the grep heuristic
     (no #1a365d / #F5A623 / text-white / bg-white) passes cleanly.
     gold-brand + champagne already exist above (lines 49-50) — Section 11
     adopts them per CONTEXT.md "cream/navy/gold-brand language".
     ────────────────────────────────────────────────────────────────────── */
  --color-paper: #ffffff;
  --color-paper-alt: #f8fafc;
  --color-ink: #1a365d;
  --color-ink-dim: #64748b;
  --color-ink-faint: #94a3b8;
  --color-line-light: #e2e8f0;
```

Do NOT modify any existing tokens. Do NOT touch the `body`, `.theme-dark`, focus-ring, slider-thumb, scrollbar, or print rules. Save the file.
  </action>
  <verify>
    <automated>grep -c "^  --color-paper:" src/app/globals.css | grep -q "^1$" &amp;&amp; grep -c "^  --color-ink:" src/app/globals.css | grep -q "^1$" &amp;&amp; grep -c "^  --color-paper-alt:" src/app/globals.css | grep -q "^1$" &amp;&amp; grep -c "^  --color-ink-dim:" src/app/globals.css | grep -q "^1$" &amp;&amp; grep -c "^  --color-line-light:" src/app/globals.css | grep -q "^1$" &amp;&amp; echo OK</automated>
  </verify>
  <done>
globals.css contains six new `--color-paper`, `--color-paper-alt`, `--color-ink`, `--color-ink-dim`, `--color-ink-faint`, `--color-line-light` token definitions inside the `@theme inline` block. No existing tokens were touched. `npm run dev` continues to start without CSS errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Retokenize Section11.tsx — sweep legacy literals + replace dark cover with light card chrome</name>
  <files>src/components/sections/Section11.tsx</files>
  <action>
Open `src/components/sections/Section11.tsx`. Apply the `<token_swap_table>` from the `<context>` section above as a complete sweep. Specifically:

1. **Outer container (line 294):** change `<div className="bg-white">` to `<div className="bg-paper rounded-2xl shadow-sm">`. This makes Section 11 a self-contained light card that visually mirrors the Phase 8 `/report` page inner wrapper (`bg-white rounded-2xl p-6 sm:p-8 my-6 shadow-sm`). Padding stays internal (existing `px-6` on the body wrapper at line 338 plus the header section that follows). Do NOT modify `section/[num]/page.tsx` — the page route already wraps Section 11 in the dark portal `<main>` and provides the page-level MonoEyebrow at lines 202–204.

2. **Replace the dark-gradient cover (lines 295–335) with a Phase 8-style light header.** Delete the entire `<div className="report-header relative overflow-hidden rounded-2xl print:rounded-none bg-gradient-to-br from-[#0f2440] via-[#1a365d] to-[#2d5986] text-white p-8 sm:p-10">` block and its contents. Replace with a light light-card header that contains:
   - A top row: left-side `<h1 className="text-[28px] sm:text-[32px] font-medium text-ink leading-[1.1] tracking-[-0.02em]">Complete Longevity Analysis</h1>` and right-side existing `<img src="/logo.png" .../>` (height `h-10 sm:h-12`).
   - A gold-brand divider bar: `<div className="h-1 w-16 bg-gold-brand rounded-full mt-4 mb-6" />`.
   - The existing 4-column client/date/age/gender grid (`<div className="grid grid-cols-2 sm:grid-cols-4 gap-x-8 gap-y-3">`), but eyebrow labels become `text-[10px] uppercase tracking-[0.15em] text-ink-dim mb-0.5` and values become `text-sm font-semibold text-ink`. Drop "capitalize" on gender if you wish; preserve text content.
   - Wrap the header in: `<div className="px-6 sm:px-8 pt-8 pb-6 border-b border-line-light">` so it has separator below it and reads as a single chrome strip on the light card.
   - REMOVE the "Longevity Assessment Report" wordmark line and the small divider (lines 309–311). The page-level MonoEyebrow injected by section/[num]/page.tsx already labels this surface.
   - REMOVE the four absolutely-positioned gold-radial-gradient + horizontal accent bar decorations (lines 297–304) — they belong only to the dark cover.

3. **Body wrapper:** the existing `<div className="px-6">` at line 338 should remain — but bump padding to `px-6 sm:px-8 pb-8`. The body wraps PillarsDisplay + Section 2 readiness + Section 3 medical + Section 4 consent + Tier Summary + Detailed Results + Tier Legend + Insights + Footer + Action Buttons.

4. **Apply EVERY row in the token swap table to the body.** Specifically, use grep on the file after each pass — every literal `#1a365d`, `#F5A623`, `#1a202c`, `#0f2440`, `#2d5986`, `#64748b`, `#94a3b8`, `#cbd5e1`, `#f8fafc`, `#f1f5f9`, `#e2e8f0`, `text-white`, `bg-white`, `from-[#`, `via-[#`, `to-[#` (excluding `to-transparent`) must be eliminated and replaced with a Tailwind utility from the new `--color-paper`/`--color-ink`/`--color-ink-dim`/`--color-ink-faint`/`--color-paper-alt`/`--color-line-light`/`--color-gold-brand`/`--color-champagne` tokens.

5. **`SectionHeading` helper (lines 90–97):** The orange bar `bg-[#F5A623]` becomes `bg-gold-brand`. The `text-[#1a365d]` becomes `text-ink`.

6. **`ContextCell` helper (lines 99–107):** Eyebrow `text-[#64748b]` becomes `text-ink-dim`. Value `text-[#1a202c]` becomes `text-ink`. The conditional `warn` `text-red-600` stays (status color).

7. **`YesNoDot` helper (lines 109–119):** Status colors (`bg-red-500`, `bg-emerald-500`, `text-red-700`) STAY (status palette). The `text-[#64748b]` for "No" becomes `text-ink-dim`.

8. **Detailed marker rows (renderMarkerRow, lines 267–289):** `text-[#1a202c]` → `text-ink`, `text-[#64748b]` → `text-ink-dim`, `text-[#94a3b8]` → `text-ink-faint`, `border-gray-100` → `border-line-light`, `bg-gray-50/40 border-l-gray-200` → `bg-paper-alt border-l-line-light` for the no-tier fallback. Tier-row backgrounds (TIER_ROW_BG) stay — they are Phase 8 tier palette.

9. **Tier Summary grid (lines 457–479):** `bg-white` → `bg-paper`, `border-gray-100` → `border-line-light`, `text-[#1a365d]` → `text-ink`, `text-[#64748b]` → `text-ink-dim`, `bg-gray-100` (line 469 unfilled bar track) → `bg-line-light`. Inline `style={{ backgroundColor: TIER_DOT[tier] }}` and color references — KEEP, those are Phase 8 tier hexes.

10. **Detailed Results category bar (line 506):** the gradient `from-[#e2e8f0]` becomes `from-line-light`.

11. **Tier Legend (line 539):** `bg-[#f8fafc]` → `bg-paper-alt`, `border-gray-100` → `border-line-light`. `TIER_TEXT` map stays (Phase 8 status text colors).

12. **Insights cards (lines 558–572):** outer card `bg-white border border-gray-100` → `bg-paper border border-line-light`. Gradient stripe `from-[#F5A623] to-[#d4891a]` → `from-gold-brand to-champagne`. Title `text-[#1a365d]` → `text-ink`. Why text `text-[#64748b]` → `text-ink-dim`. Bullet color `bg-[#F5A623]` → `bg-gold-brand`. Do-now item `text-[#1a202c]` → `text-ink`.

13. **Report footer (lines 581–593):** `border-gray-200` → `border-line-light`, `text-[#94a3b8]` → `text-ink-faint`, `text-[#cbd5e1]` → `text-ink-faint/60`. The opacity-40 logo stays as-is.

14. **Action buttons (lines 597–633):**
    - Export PDF button `bg-[#1a365d] text-white hover:bg-[#2d5986]` → `bg-ink text-paper hover:bg-ink/85`. Replace `text-white/30 border-t-white` on the spinner with `text-paper/30 border-t-paper`. The `border-white/30 border-t-white` literal becomes `border-paper/30 border-t-paper`.
    - Save & Complete button `bg-[#F5A623] text-[#1a365d] hover:bg-[#f7bc5a]` → `bg-gold-brand text-ink hover:bg-champagne`.

15. **Do NOT modify:**
    - The `TIER_DOT` / `TIER_ROW_BG` / `TIER_ROW_BORDER` / `TIER_TEXT` constants (lines 30–60) — Phase 8 tier palette is sovereign.
    - The `TierPill` background map inside its function (lines 76–82) — Phase 8 tier palette.
    - The loading state spinner at lines 236–248 (already uses Phase 9 dark tokens, rendered before the light card mounts).
    - The PillarsDisplay import or its usage at line 341.
    - The medical-flag amber badge (line 364) and red-warning surgery-details panel (line 394) and the consent green/gray dot logic (lines 429–430) — these use status palette which is sovereign.
    - Any logic, data flow, useEffect, useCallback, or component structure. This task is **token swap only**.

After the sweep, verify with `grep -E "#1a365d|#F5A623|text-white|bg-white" src/components/sections/Section11.tsx` returns ZERO matches. If any match remains, address it (it is either a literal that needs swapping or a justified exception which must be commented inline with `// Phase 10 justified exception: <reason>` so the post-implementation grep can `grep -v` it).
  </action>
  <verify>
    <automated>grep -E "#1a365d|#F5A623|text-white|bg-white" src/components/sections/Section11.tsx | grep -vE "Phase 10 justified exception" | wc -l | grep -q "^0$" &amp;&amp; npx --yes tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -v "^$" | wc -l | grep -q "^0$" &amp;&amp; echo OK</automated>
  </verify>
  <done>
Zero matches for `#1a365d|#F5A623|text-white|bg-white` in `src/components/sections/Section11.tsx` (excluding justified exceptions commented inline). TypeScript compiles cleanly. The Phase 8 tier palette constants (TIER_DOT, TIER_ROW_BG, TIER_ROW_BORDER, TIER_TEXT, TierPill bg map) are unchanged. Status hexes (red/emerald/amber) on medical screening + consent dots are unchanged. PillarsDisplay import and usage unchanged.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — Section 11 renders as a light card on the dark portal frame</name>
  <what-built>
Section 11 longevity report retokenized to Phase 8 light-card vocabulary, wrapped as a self-contained `bg-paper rounded-2xl shadow-sm` card. Dark-gradient cover replaced with a light Phase 8-style header. globals.css has six new `--color-paper/-alt/-ink/-ink-dim/-ink-faint/-line-light` tokens.
  </what-built>
  <how-to-verify>
1. Start dev server if not running: `npm run dev`.
2. Open `http://localhost:3000/login` and sign in as a coach or admin.
3. Navigate to an assessment that has Section 1–10 data (any in-progress or completed assessment). Visit `/portal/assessment/{id}/section/11`.
4. **Expected appearance:**
   - The page background remains dark (#0a0a0b) — the portal frame is intact.
   - The sidebar, progress bar at top, navigation buttons at bottom — all Phase 9 dark.
   - The MonoEyebrow "SECTION 11 / 11 · LONGEVITY ANALYSIS" appears in gold-brand mono above the report.
   - Section 11 itself sits inside a single LIGHT (cream/white #ffffff) rounded card with a subtle shadow.
   - The report header inside the card shows "Complete Longevity Analysis" in navy ink (~28–32px), a short gold-brand divider bar, then a 4-column client/date/age/gender meta grid in muted ink-dim labels + ink values.
   - Peak Living pillars below render as before (Phase 8 component — untouched).
   - Results Overview tier cards still show emerald/blue/gray/amber/red tier dots and percent bars (Phase 8 tier palette preserved).
   - Detailed Results rows still show emerald/blue/gray/amber/red left-border accents and tier pills (Phase 8 preserved).
   - Insight cards show a gold-brand-to-champagne gradient stripe on the left edge (no longer bright orange).
   - Bottom action buttons: "Export PDF" is navy ink on cream; "Save & Complete Assessment" is gold-brand on navy ink.
5. **Confirm zero bright `#F5A623` orange remains** in section 11 (gold accents should all be the duller `#c9a24a` gold-brand). Spot-check the SectionHeading vertical bar — it should be `#c9a24a`, not bright orange.
6. **Confirm tier badges still pop** with their Phase 8 emerald/blue/gray/amber/red palette in both the Results Overview grid and the Detailed Results rows.
7. **Confirm the loading state** still renders correctly. Hit refresh and observe the spinner during data load — it should remain Phase 9 gold-brand mono on the dark portal frame (NOT inside a light card; the card mounts after `loading` resolves).

**Issues to flag:**
- If Section 11 is rendering directly on the dark frame with no light card → `bg-paper` token not resolving; check globals.css imports.
- If text inside the report is invisible (cream-on-cream) → swap missed something; run the grep heuristic.
- If tier pills/dots lost their colors → an unintended sweep of the Phase 8 tier palette occurred; revert.
- If the dark gradient cover is still showing → Task 2 step 2 was incomplete.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | This plan is pure presentation token swap. No new inputs, no new boundaries, no data flow changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | I (Information disclosure) | Section11.tsx visual rendering | accept | Section 11 already renders on the authenticated portal route which enforces session + ownership in the parent route (verified in Phase 7). Token swap has no effect on access control. |
| T-10-02 | T (Tampering — CSS injection) | globals.css token additions | accept | New tokens are static CSS custom properties added inside the `@theme inline` block; no dynamic value, no user input, no XSS surface. |
</threat_model>

<verification>
- `grep -E "#1a365d|#F5A623|text-white|bg-white" src/components/sections/Section11.tsx | grep -vE "Phase 10 justified exception"` returns no matches.
- `grep "^  --color-paper:\|^  --color-ink:\|^  --color-paper-alt:\|^  --color-ink-dim:\|^  --color-ink-faint:\|^  --color-line-light:" src/app/globals.css` returns six matches.
- `npx tsc --noEmit -p tsconfig.json` exits 0.
- `npm run build` exits 0.
- Visual verification at `/portal/assessment/{id}/section/11` shows light card on dark frame matching Phase 8 `/report` page surface language.
- Phase 8 tier palette (TIER_DOT, TIER_ROW_BG, TIER_ROW_BORDER, TIER_TEXT, TierPill bg map) constants in Section11.tsx are identical to pre-change (`git diff` shows no edits to lines 30–60 and 76–82).
</verification>

<success_criteria>
- Section 11 renders as a light cream/navy card on the dark portal canvas — same brand language as Phase 8 `/report` page.
- Zero `#1a365d` / `#F5A623` / `text-white` / `bg-white` matches in `src/components/sections/Section11.tsx` (justified exceptions documented inline).
- Phase 8 tier-pill palette + status colors preserved verbatim.
- No functional regression: pillars compute, markers render, insights generate, PDF export button works, Save & Complete button completes the assessment.
- New tokens (`--color-paper`, `--color-paper-alt`, `--color-ink`, `--color-ink-dim`, `--color-ink-faint`, `--color-line-light`) available globally as Tailwind utilities and consumed by Section 11.
</success_criteria>

<output>
After completion, create `.planning/phases/10-section-11-pdf-brand-language-alignment/10-01-SUMMARY.md` following the standard summary template (Files modified, Tokens added, Decisions deferred, Verification result, Follow-ups for 10-02).
</output>
