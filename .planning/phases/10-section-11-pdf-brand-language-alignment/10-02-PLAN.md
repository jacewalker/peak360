---
phase: 10-section-11-pdf-brand-language-alignment
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/lib/pdf/colors.ts
  - src/lib/pdf/components/MarkerTable.tsx
  - src/lib/pdf/components/MarkerRow.tsx
  - src/lib/pdf/components/ConsentStatus.tsx
autonomous: false
requirements:
  - REQ-9.1
must_haves:
  truths:
    - "Exported PDF accent palette is gold-brand (#c9a24a) + champagne (#e8d6a8), matching the in-app Section 11 light card from Plan 10-01"
    - "PDF cover navy + paper-white surfaces remain (print medium — paper IS light), but the navy hex literal #1a365d is eliminated from the entire src/lib/pdf/ tree by routing through COLORS.navy = '#1a365d' centrally"
    - "Zero hex literals matching `#1a365d|#F5A623` remain anywhere in src/lib/pdf/ (the legacy bright-gold and bright-navy hexes — replaced by either gold-brand or routed through token)"
    - "Phase 8 sovereign palettes preserved verbatim: TIER_COLORS_PDF, TIER_ROW_BG_PDF, TIER_BORDER_PDF, TIER_TEXT_PDF (the 5-tier marker palette), STATUS_TEXT in PillarsPage (#047857 / #b45309 / #b91c1c / #cbd5e1), TRAFFIC_LIGHT_HEX re-exports, and status surface tints (#fffbeb / #fef2f2 / #fcd34d / #ef4444 / #10b981 / #f59e0b / #92400e / #fca5a5 / #dc2626 / #b45309) — these are the medical/status palette and are sovereign per Phase 9 D-16"
    - "Stray hex literals that bypass the token file for non-status surfaces are routed through existing COLORS tokens (e.g. MarkerTable subcategory header `#f1f5f9` → `COLORS.borderLight`; MarkerRow no-tier fallback `#f9fafb` → `COLORS.bgLighter`, `#e5e7eb` → `COLORS.border`; ConsentStatus unsigned dot `#9ca3af` → `COLORS.textMuted`)"
    - "Visual diff: PDF cover + accents + body align brand language with the in-app Section 11 light card from Plan 10-01 (same gold-brand accents, same navy ink) — the PDF and on-screen report read as the same artefact"
  artifacts:
    - path: "src/lib/pdf/colors.ts"
      provides: "Phase 8 print-safe brand alignment — gold-brand + champagne values shipped via COLORS.gold and COLORS.goldDark; navy/navyDark/navyLight/white routed cleanly; the bright-gold #F5A623 / bright-gold-dark #d4891a literals are eliminated"
      contains: "#c9a24a"
    - path: "src/lib/pdf/components/MarkerTable.tsx"
      provides: "Subcategory header background routed through COLORS.borderLight (eliminates one stray #f1f5f9 literal)"
    - path: "src/lib/pdf/components/MarkerRow.tsx"
      provides: "No-tier fallback colors routed through COLORS tokens (eliminates #f9fafb and #e5e7eb literals)"
    - path: "src/lib/pdf/components/ConsentStatus.tsx"
      provides: "Unsigned-state dot routed through COLORS.textMuted (eliminates one stray #9ca3af literal)"
  key_links:
    - from: "src/lib/pdf/components/*.tsx (~83 token references)"
      to: "src/lib/pdf/colors.ts COLORS export"
      via: "import { COLORS } from '@/lib/pdf/colors'"
      pattern: "COLORS\\.(navy|gold|white|border|bgLight|textPrimary|textSecondary|textMuted|textFaint)"
    - from: "src/lib/pdf/Peak360Report.tsx"
      to: "ReportHeader (navy cover) + PillarsPage + TierSummary + MarkerTable + InsightsSection"
      via: "Document → Page composition; all downstream components consume tokens via COLORS"
      pattern: "Peak360Report"
---

<objective>
Align the exported PDF (`src/lib/pdf/`) with the Phase 8 / Plan 10-01 light-card brand language. The PDF is centralized through `colors.ts` (~83 token consumer references across 14 components), so the bulk of the change is one file: update the hex VALUES of `COLORS.gold` and `COLORS.goldDark` to the Phase 9 brand palette (gold-brand + champagne) so the PDF's gold accents match the in-app Section 11 card from Plan 10-01. Then sweep a small number of stray hex literals in PDF components that bypassed the token file for non-status surfaces.

Purpose: The PDF and the in-app Section 11 report should read as the same artefact (CONTEXT.md acceptance). After Plan 10-01 shifts Section 11 accents to `gold-brand (#c9a24a)` and `champagne (#e8d6a8)`, the PDF still ships bright legacy gold `#F5A623` and `#d4891a`, breaking the visual cohesion. This plan closes that gap.
Output: A PDF renderer whose token file matches Plan 10-01's accent palette, plus three component edits to route the last stray non-status hex literals through tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-section-11-pdf-brand-language-alignment/10-CONTEXT.md
@.planning/phases/10-section-11-pdf-brand-language-alignment/10-01-PLAN.md

# Phase 8 sovereign in PDF — preserve verbatim
@src/lib/pdf/components/PillarsPage.tsx

# Token file (source of truth — primary edit target)
@src/lib/pdf/colors.ts

# Style helpers consuming the token file (no edit needed — derive their values from COLORS)
@src/lib/pdf/styles.ts

# Cover header consuming COLORS.navy + COLORS.white (no edit needed — tokens absorb the change)
@src/lib/pdf/components/ReportHeader.tsx

# Sections with stray hex literals to route through tokens
@src/lib/pdf/components/MarkerTable.tsx
@src/lib/pdf/components/MarkerRow.tsx
@src/lib/pdf/components/ConsentStatus.tsx

# Sections with sovereign status hexes (DO NOT MODIFY)
@src/lib/pdf/components/MedicalSection.tsx
@src/lib/pdf/components/ReferralFlagPdf.tsx

<interfaces>
<!-- Current COLORS export (the file we are editing). -->
<!-- Shape stays identical; only the hex VALUES of gold + goldDark change.
     Field names stay so the ~83 downstream consumers (across 14 files) require zero edits. -->

```ts
// src/lib/pdf/colors.ts (BEFORE — line 3–18)
export const COLORS = {
  navy: '#1a365d',           // KEEP — Phase 8 navy ink for PDF cover surface (a print medium where dark ink reads on paper)
  navyDark: '#0f2440',       // KEEP
  navyLight: '#2d5986',      // KEEP
  gold: '#F5A623',           // ← CHANGE to '#c9a24a' (Phase 9 gold-brand, used by Section 11 after Plan 10-01)
  goldDark: '#d4891a',       // ← CHANGE to '#e8d6a8' (Phase 9 champagne, used by Section 11 after Plan 10-01)
  textPrimary: '#1a202c',    // KEEP
  textSecondary: '#64748b',  // KEEP
  textMuted: '#94a3b8',      // KEEP
  textFaint: '#cbd5e1',      // KEEP
  border: '#e2e8f0',         // KEEP
  borderLight: '#f1f5f9',    // KEEP
  bgLight: '#f8fafc',        // KEEP
  bgLighter: '#f9fafb',      // KEEP
  white: '#ffffff',          // KEEP (paper IS white — print medium light surface, per CONTEXT.md decision)
};

// All five TIER_*_PDF maps and the re-export block (lines 20–59) — UNTOUCHED.
// Note: TIER_BORDER_PDF.normal at line 39 IS the literal '#9ca3af' — this is a
// legitimate sovereign tier-palette value and is exempt from the #9ca3af sweep below.
```

**Why navy stays #1a365d:** Per CONTEXT.md "Keep PDF surface light. PDF is a print medium. Dark theme does not apply. Use the same cream/navy/gold-brand language as the in-app report card." The Phase 9 brand-language navy ink IS `#1a365d` (verified — Phase 8 PillarCard.tsx line 118 + 125 use `text-navy` which resolves to `var(--color-navy)` = `#1a365d`). So COLORS.navy already aligns with Phase 8's navy ink. The grep heuristic bans the literal `#1a365d` in *files outside the token file*, but the value living inside `colors.ts` as `COLORS.navy = '#1a365d'` is the legitimate token definition — analogous to how `globals.css` `--color-navy: #1a365d;` is allowed. **Confirmation:** running `grep -rEn "#1a365d" src/lib/pdf/` returns ONLY the colors.ts line — no component bypasses the token.

**Why gold changes:** `COLORS.gold` is `#F5A623` today. Plan 10-01 shipped Section 11 with `--color-gold-brand: #c9a24a`. To get visual brand cohesion between the PDF and Section 11, the PDF's gold token must shift to the same value. Same for goldDark → champagne.

**Stray hex literals to route through tokens (not status — these are surfaces):**
- `src/lib/pdf/components/MarkerTable.tsx:59` — `backgroundColor: '#f1f5f9'` → `backgroundColor: COLORS.borderLight` (same hex value).
- `src/lib/pdf/components/MarkerRow.tsx:25` — `'#f9fafb'` no-tier fallback → `COLORS.bgLighter` (same hex value).
- `src/lib/pdf/components/MarkerRow.tsx:27` — `'#e5e7eb'` no-tier border fallback → `COLORS.border` (existing token, `#e2e8f0` — visually equivalent, slightly lighter — acceptable per visual diff tolerance).
- `src/lib/pdf/components/ConsentStatus.tsx:35` — `'#9ca3af'` unsigned dot → `COLORS.textMuted` (existing token, `#94a3b8` — visually equivalent neutral gray). **Note:** the same hex literal `#9ca3af` also legitimately appears at `src/lib/pdf/colors.ts:39` as `TIER_BORDER_PDF.normal` (sovereign tier palette per D-28); that occurrence is exempt — the sweep targets the ConsentStatus literal only.

**Sovereign palettes — DO NOT TOUCH:**
- `src/lib/pdf/colors.ts` lines 20–50 (`TIER_COLORS_PDF`, `TIER_ROW_BG_PDF`, `TIER_BORDER_PDF`, `TIER_TEXT_PDF`) — Phase 8 marker tier palette per D-28. Specifically `TIER_BORDER_PDF.normal = '#9ca3af'` (line 39) is a legitimate sovereign value and MUST remain.
- `src/lib/pdf/colors.ts` line 59 (`TRAFFIC_LIGHT_HEX` re-export from `@/lib/pillars/colors`) — Phase 8 pillar palette per D-28.
- `src/lib/pdf/components/PillarsPage.tsx` STATUS_TEXT + PENDING_RING hexes (lines 64–70) — Phase 8 sovereign palette per D-28.
- `src/lib/pdf/components/MedicalSection.tsx` hexes (`#ef4444`, `#10b981`, `#fffbeb`, `#fcd34d`, `#92400e`, `#fef2f2`) — status palette (yes/no dots, medical-flag amber callout, surgery-details red callout). Phase 9 D-16 reserves status colors.
- `src/lib/pdf/components/ReferralFlagPdf.tsx` hexes (`#fef2f2`, `#fca5a5`, `#dc2626`, `#fffbeb`, `#fcd34d`, `#b45309`) — referral flag status palette.
- `src/lib/pdf/Peak360Report.tsx` lines 50–58 (`#fffbeb`, `#fcd34d`, `#92400e` for gender-warning callout) — status palette.
- `src/lib/pdf/components/MarkerRow.tsx` `TIER_ROW_BG_PDF` / `TIER_BORDER_PDF` references — sovereign tier palette.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update colors.ts — shift gold + goldDark to Phase 9 brand values</name>
  <files>src/lib/pdf/colors.ts</files>
  <action>
Open `src/lib/pdf/colors.ts`. Inside the `COLORS` export object (lines 3–18):

1. Change line 7 from `gold: '#F5A623',` to `gold: '#c9a24a', // Phase 9 gold-brand — matches in-app Section 11 (--color-gold-brand)`.
2. Change line 8 from `goldDark: '#d4891a',` to `goldDark: '#e8d6a8', // Phase 9 champagne — matches in-app Section 11 (--color-champagne)`.

Do NOT modify any other field. Do NOT modify `TIER_COLORS_PDF`, `TIER_ROW_BG_PDF`, `TIER_BORDER_PDF`, `TIER_TEXT_PDF`, or the `TRAFFIC_LIGHT_HEX` re-export block.

**Critical:** the field NAMES (`gold`, `goldDark`) stay identical — only the hex VALUES change. This is the centralized swap that flows through ~83 downstream consumers (ReportHeader gold accent bar, styles.ts sectionHeadingBar, InsightsSection gold stripe + bullets, etc.) with zero per-component edits.
  </action>
  <verify>
    <automated>grep -E "gold: '#c9a24a'" src/lib/pdf/colors.ts | wc -l | grep -q "^1$" &amp;&amp; grep -E "goldDark: '#e8d6a8'" src/lib/pdf/colors.ts | wc -l | grep -q "^1$" &amp;&amp; grep -E "#F5A623|#d4891a" src/lib/pdf/colors.ts | wc -l | grep -q "^0$" &amp;&amp; echo OK</automated>
  </verify>
  <done>
`COLORS.gold` is `'#c9a24a'` and `COLORS.goldDark` is `'#e8d6a8'`. No `#F5A623` or `#d4891a` literals remain in `src/lib/pdf/colors.ts`. All other token entries, all TIER_*_PDF maps, and the TRAFFIC_LIGHT_HEX re-export are byte-identical to pre-change.
  </done>
</task>

<task type="auto">
  <name>Task 2: Route stray hex literals in MarkerTable, MarkerRow, ConsentStatus through existing COLORS tokens</name>
  <files>src/lib/pdf/components/MarkerTable.tsx, src/lib/pdf/components/MarkerRow.tsx, src/lib/pdf/components/ConsentStatus.tsx</files>
  <action>
Make three precise replacements:

**1. `src/lib/pdf/components/MarkerTable.tsx` — subcategory header row (around line 59):**
   - Find: `<View style={{ backgroundColor: '#f1f5f9', paddingHorizontal: 8, paddingVertical: 3 }}>`
   - Replace with: `<View style={{ backgroundColor: COLORS.borderLight, paddingHorizontal: 8, paddingVertical: 3 }}>`
   - `COLORS` is already imported (line 4 of this file imports `borderLight` via the same import). Verify the import line `import { COLORS } from '@/lib/pdf/colors';` (or equivalent) is present at the top of the file. If `COLORS` is imported but with named field destructuring instead of object import, adapt accordingly — the existing `MarkerTable.tsx:47` reference `COLORS.border` confirms the object form is in use.

**2. `src/lib/pdf/components/MarkerRow.tsx` — no-tier fallback row (lines 25 and 27):**
   - Find line 25: `backgroundColor: marker.tier ? TIER_ROW_BG_PDF[marker.tier] : '#f9fafb',`
   - Replace with: `backgroundColor: marker.tier ? TIER_ROW_BG_PDF[marker.tier] : COLORS.bgLighter,`
   - Find line 27: `borderLeftColor: marker.tier ? TIER_BORDER_PDF[marker.tier] : '#e5e7eb',`
   - Replace with: `borderLeftColor: marker.tier ? TIER_BORDER_PDF[marker.tier] : COLORS.border,`
   - Verify `COLORS` is imported at top of file. If only `TIER_ROW_BG_PDF` and `TIER_BORDER_PDF` are currently destructured from `@/lib/pdf/colors`, extend the import to also include `COLORS` (e.g. `import { COLORS, TIER_ROW_BG_PDF, TIER_BORDER_PDF } from '@/lib/pdf/colors';`).

**3. `src/lib/pdf/components/ConsentStatus.tsx` — unsigned dot color (line 35):**
   - Find: `backgroundColor: consentSigned ? '#10b981' : '#9ca3af',`
   - Replace with: `backgroundColor: consentSigned ? '#10b981' : COLORS.textMuted,`
   - **Keep** `'#10b981'` literal — it's a status color (consent SIGNED = emerald, sovereign per Phase 9 D-16). Only the unsigned-state neutral gray routes through the token.
   - Verify `COLORS` is imported at top of file. Add the import if missing.

Do NOT modify anything else in these three files. Do NOT touch MedicalSection.tsx, ReferralFlagPdf.tsx, PillarsPage.tsx, ReportHeader.tsx, TierSummary.tsx, InsightsSection.tsx, ReadinessSection.tsx, DisclaimerPdf.tsx, ReportFooter.tsx, RangeBarPdf.tsx, TierPillPdf.tsx, fonts.ts, styles.ts, types.ts, or Peak360Report.tsx — these either consume COLORS cleanly (automatic alignment after Task 1) or contain sovereign status hexes that must be preserved.

**IMPORTANT — sovereign exemption for `#9ca3af`:** The literal `#9ca3af` ALSO appears legitimately at `src/lib/pdf/colors.ts:39` as `TIER_BORDER_PDF.normal` (sovereign Phase 8 tier palette per D-28). That occurrence MUST remain untouched. The sweep in this task only targets the `ConsentStatus.tsx:35` instance.
  </action>
  <verify>
    <automated>grep -E "'#f1f5f9'|'#f9fafb'|'#e5e7eb'|'#9ca3af'" src/lib/pdf/components/MarkerTable.tsx src/lib/pdf/components/MarkerRow.tsx src/lib/pdf/components/ConsentStatus.tsx | wc -l | grep -q "^0$" &amp;&amp; npx --yes tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -v "^$" | wc -l | grep -q "^0$" &amp;&amp; echo OK</automated>
  </verify>
  <done>
Zero matches for `'#f1f5f9'`, `'#f9fafb'`, `'#e5e7eb'`, `'#9ca3af'` literals in MarkerTable.tsx, MarkerRow.tsx, ConsentStatus.tsx. TypeScript compiles cleanly. Each file has a valid `COLORS` import that resolves to `@/lib/pdf/colors`. No other PDF component was modified. The `#9ca3af` occurrence at `src/lib/pdf/colors.ts:39` (TIER_BORDER_PDF.normal — sovereign) is unchanged.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — exported PDF brand matches in-app Section 11 card</name>
  <what-built>
PDF token file `src/lib/pdf/colors.ts` shifts `COLORS.gold` to `#c9a24a` (gold-brand) and `COLORS.goldDark` to `#e8d6a8` (champagne) — propagating through the ~83 downstream consumer references. Three small literal sweeps route MarkerTable subcategory-header `#f1f5f9`, MarkerRow no-tier fallback `#f9fafb`/`#e5e7eb`, and ConsentStatus unsigned dot `#9ca3af` through existing COLORS tokens. All status hexes, the navy cover surface, paper-white backgrounds, and the 5-tier marker palette (including the legitimate `TIER_BORDER_PDF.normal = '#9ca3af'` at colors.ts:39) are preserved verbatim.
  </what-built>
  <how-to-verify>
1. Open the in-app report side by side: `/portal/assessment/{id}/section/11` (Plan 10-01 output). Note the gold-brand color of the SectionHeading vertical bars, the gradient stripe on insight cards, and the "Save & Complete Assessment" button background.
2. From the same Section 11 page, click "Export PDF" (the navy ink button at the bottom of Section 11). Wait for the PDF download. Open it.
3. **Expected appearance in the PDF (matches the in-app Section 11 light card):**
   - **Cover page (Page 2 of PDF, after Pillars page):** Dark navy cover surface remains (`COLORS.navy = #1a365d` unchanged). The gold accent bar under "Complete Longevity Analysis" title is now a softer dull-gold (`#c9a24a` gold-brand) — NOT bright orange. The PEAK360 logo + "Longevity Assessment Report" eyebrow + 4-column meta grid all read normally on the navy surface.
   - **Section Headings throughout PDF** (Daily Readiness, Medical Screening, Results Overview, Detailed Marker Results, Insights & Recommendations): the small vertical accent bar to the left of each heading text is dull-gold-brand, NOT bright orange.
   - **Insight cards:** left-edge accent stripe is now dull-gold-brand. Bullet dots before each "do-now" item are dull-gold-brand. Insight title text is navy. Body text is gray. Surface is white (paper).
   - **Tier Summary cards:** 5-tier color top bars (emerald / blue / gray / amber / red) are unchanged — Phase 8 marker palette sovereign. Tier label uppercase text is colored. Count is large navy. Progress bar fill is the tier color. White background. Light border.
   - **Detailed Marker Results table:** tier-rated rows show their tier-color left border and pale tier-color row background — unchanged. Untiered rows have a soft gray-fallback background (`#f9fafb` was previously a literal — now `COLORS.bgLighter` with the same value). Subcategory header strip (e.g. "Lipid Panel", "Glucose") has a slightly different gray surface — same value as before, just routed through `COLORS.borderLight` now.
   - **Medical Screening:** Yes/No dots — red for Yes, emerald for No (unchanged status). Surgery-details red callout panel (if shown) unchanged. Medical-flag amber callout unchanged.
   - **Consent Status:** dot is emerald when signed, neutral gray when unsigned. The unsigned dot may look very slightly cooler (`#94a3b8` vs `#9ca3af`) — both are neutral grays, the shift is imperceptible to the eye.
   - **Referral Flags (if marker is critically out of range):** unchanged red callout panel.
   - **Footer:** unchanged faint gray "Generated by Peak360 Longevity Program — {date}" line.
4. **Critical brand cohesion check:** Place the in-app Section 11 page next to the PDF cover page. The dull gold-brand accents should match between the two surfaces. There should be NO bright orange `#F5A623` visible in either rendering. The 5-tier marker palette (emerald / blue / gray / amber / red) should be IDENTICAL across both surfaces (Phase 8 sovereign).
5. **PDF size + selectable text sanity:** PDF should still be < 500KB, text should still be selectable (Phase 5 acceptance — not regressed). Verify by selecting a marker label with the cursor and confirming the text highlights.

**Issues to flag:**
- If the PDF cover or any other gold accent still appears bright orange → Task 1 hex change didn't propagate (likely cached Next build; try `rm -rf .next && npm run build`).
- If the tier palette in the PDF marker table no longer matches the in-app Section 11 tier pills → an accidental edit hit `TIER_COLORS_PDF` or related sovereign maps; revert.
- If the medical-flag amber callout or referral red callout looks different → status palette was touched accidentally; revert.
- If the PDF fails to generate or PDF generation throws a TS error → check the `COLORS` import was added to MarkerRow.tsx / ConsentStatus.tsx as part of Task 2.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Pure presentation token swap inside a server-side PDF renderer. No new boundaries, no new inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | I (Information disclosure) | Exported PDF | accept | PDF generation is already gated by `/api/assessments/[id]/pdf` route ownership check (Phase 7 BL-05). Token swap has no effect on access control or data exposure. |
| T-10-04 | D (Denial of service) | Peak360Report render path | accept | Token changes are static hex string literals; @react-pdf/renderer compilation cost is unchanged. PDF size + render time identical to pre-change. |
| T-10-05 | T (Tampering — wrong palette) | colors.ts | mitigate | Verification task does side-by-side visual diff against the in-app Section 11 card to confirm brand cohesion and that no sovereign palette (tier / status) regressed. |
</threat_model>

<verification>
- `grep -E "#F5A623|#d4891a" src/lib/pdf/colors.ts` returns no matches.
- `grep -rE "'#1a365d'|'#F5A623'" src/lib/pdf/` returns no matches outside `colors.ts` line for `navy: '#1a365d'` (which is the legitimate token definition — analogous to globals.css). Confirm with: `grep -rE "'#1a365d'|'#F5A623'" src/lib/pdf/ | grep -v "src/lib/pdf/colors.ts:" | wc -l` returns `0`.
- `grep -E "'#f1f5f9'|'#f9fafb'|'#e5e7eb'|'#9ca3af'" src/lib/pdf/components/MarkerTable.tsx src/lib/pdf/components/MarkerRow.tsx src/lib/pdf/components/ConsentStatus.tsx` returns no matches.
- **Exclusions for legitimate sovereign palette values in `src/lib/pdf/colors.ts`:** The token file `src/lib/pdf/colors.ts` is excluded from the bare-hex sweep for two specific legitimate values: (a) `navy: '#1a365d'` at line 7 (the canonical navy token definition, analogous to `globals.css --color-navy`), AND (b) `TIER_BORDER_PDF.normal = '#9ca3af'` at line 39 (the sovereign Phase 8 tier palette border for the "normal" tier per D-28). These two literals are the source-of-truth definitions; they are NOT bypasses. Any sweep grep over `src/lib/pdf/` for `#1a365d` or `#9ca3af` MUST exclude `src/lib/pdf/colors.ts` so the verification does not false-positive on these legitimate sovereign values. Confirm with: `grep -rE "'#9ca3af'" src/lib/pdf/ | grep -v "src/lib/pdf/colors.ts:" | wc -l` returns `0` (the only `#9ca3af` should be the sovereign `TIER_BORDER_PDF.normal` entry in the token file).
- `npx tsc --noEmit -p tsconfig.json` exits 0.
- `npm run build` exits 0.
- PDF export from `/portal/assessment/{id}/section/11` "Export PDF" button completes and produces a valid PDF.
- Visual diff: PDF gold accents match in-app Section 11 gold-brand accents (dull gold #c9a24a, NOT bright orange #F5A623). 5-tier marker palette identical across PDF + in-app. Status palette (red/emerald/amber callouts) unchanged. Phase 8 sovereign components (PillarsPage, TIER_*_PDF, TRAFFIC_LIGHT_HEX) untouched.
- PDF file size < 500KB (Phase 5 acceptance not regressed).
</verification>

<success_criteria>
- `COLORS.gold = '#c9a24a'`, `COLORS.goldDark = '#e8d6a8'` in `src/lib/pdf/colors.ts`.
- No `#F5A623` or `#d4891a` literals anywhere in `src/lib/pdf/`.
- No `#1a365d` literals anywhere in `src/lib/pdf/` outside the single `navy: '#1a365d'` line in `colors.ts` (the legitimate token definition).
- No `#9ca3af` literals anywhere in `src/lib/pdf/` outside the single `TIER_BORDER_PDF.normal: '#9ca3af'` line in `colors.ts` (sovereign Phase 8 tier palette per D-28).
- Three stray non-status hex literals (`#f1f5f9`, `#f9fafb`, `#e5e7eb`, `#9ca3af`) eliminated from MarkerTable / MarkerRow / ConsentStatus by routing through existing COLORS tokens.
- Phase 8 sovereign palettes (tier colors, traffic-light pillars, status callouts) preserved verbatim — diffable via `git diff src/lib/pdf/colors.ts` showing changes confined to lines 7–8.
- PDF visual cohesion with in-app Section 11 card confirmed by human checkpoint.
- No functional regression: PDF generates, downloads, opens, has selectable text, weighs < 500KB.
</success_criteria>

<output>
After completion, create `.planning/phases/10-section-11-pdf-brand-language-alignment/10-02-SUMMARY.md` following the standard summary template. Include before/after screenshots of the PDF cover page and one insight card if available.
</output>
</output>
