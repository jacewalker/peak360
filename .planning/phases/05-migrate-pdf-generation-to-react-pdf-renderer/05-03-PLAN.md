---
phase: 05-migrate-pdf-generation-to-react-pdf-renderer
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - src/components/sections/Section11.tsx
  - package.json
autonomous: false
requirements:
  - PDF-07
  - PDF-08

must_haves:
  truths:
    - "Export PDF button in Section 11 triggers a fetch to /api/assessments/[id]/pdf and downloads the resulting file"
    - "The old html2canvas+jsPDF export code is completely removed from Section11.tsx"
    - "html2canvas-pro and jspdf are uninstalled from package.json"
    - "No DOM manipulation spacer algorithm exists in the codebase"
    - "Section 11 still displays the HTML report for on-screen viewing (unchanged)"
    - "The downloaded PDF contains all report sections (header, readiness, medical, markers, insights)"
  artifacts:
    - path: "src/components/sections/Section11.tsx"
      provides: "Updated export button using fetch-and-download pattern"
      contains: "fetch(`/api/assessments/${assessmentId}/pdf`)"
    - path: "package.json"
      provides: "Clean dependencies without html2canvas-pro or jspdf"
  key_links:
    - from: "src/components/sections/Section11.tsx"
      to: "/api/assessments/[id]/pdf"
      via: "fetch call in exportPdf callback"
      pattern: "fetch.*api/assessments.*pdf"
---

<objective>
Replace Section 11's html2canvas+jsPDF export with a fetch-and-download call to the new PDF API route, visually verify the complete report, then remove the old dependencies.

Purpose: Completes the migration by connecting the UI to the new PDF pipeline and cleaning up the old rasterize-and-slice approach, eliminating page-break bugs, producing vector PDFs, and reducing bundle size.

Output: Working PDF export from Section 11 using the new @react-pdf/renderer pipeline; old PDF dependencies removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-migrate-pdf-generation-to-react-pdf-renderer/05-RESEARCH.md
@.planning/phases/05-migrate-pdf-generation-to-react-pdf-renderer/05-01-SUMMARY.md
@.planning/phases/05-migrate-pdf-generation-to-react-pdf-renderer/05-02-SUMMARY.md
@src/components/sections/Section11.tsx

<interfaces>
<!-- The new API endpoint created in Plans 01-02 -->

GET /api/assessments/[id]/pdf
  Response: application/pdf binary buffer
  Headers: Content-Type: application/pdf, Content-Disposition: attachment; filename="Peak360_Report_{name}.pdf"
  Error: 500 { error: string }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace exportPdf with fetch-and-download, remove old html2canvas+jsPDF code and dependencies</name>
  <files>
    src/components/sections/Section11.tsx
    package.json
  </files>
  <read_first>
    src/components/sections/Section11.tsx (full file — need to understand all references to exportPdf, reportRef, actionsRef, actionsTopRef, html2canvas, jspdf, spacer logic)
    package.json (to identify html2canvas-pro and jspdf entries)
  </read_first>
  <action>
1. In `src/components/sections/Section11.tsx`, replace the `exportPdf` callback (lines ~144-266) with a simple fetch-and-download:

   ```typescript
   const exportPdf = useCallback(async () => {
     if (exporting) return;
     setExporting(true);
     try {
       const response = await fetch(`/api/assessments/${assessmentId}/pdf`);
       if (!response.ok) throw new Error('PDF generation failed');
       const blob = await response.blob();
       const url = URL.createObjectURL(blob);
       const a = document.createElement('a');
       a.href = url;
       a.download = `Peak360_Report_${((clientInfo.clientName as string) || 'Client').replace(/\s+/g, '_')}.pdf`;
       a.click();
       URL.revokeObjectURL(url);
     } catch (err) {
       console.error('PDF export failed:', err);
     } finally {
       setExporting(false);
     }
   }, [exporting, clientInfo, assessmentId]);
   ```

2. Remove these refs that are no longer needed (they were only used for the old html2canvas approach):
   - `actionsRef` — remove the `useRef` declaration AND the `ref={actionsRef}` attribute on the bottom action buttons div
   - `actionsTopRef` — remove the `useRef` declaration AND the `ref={actionsTopRef}` attribute on the top action bar div
   - `reportRef` — remove the `useRef` declaration AND the `ref={reportRef}` attribute on the outer container div
   - Note: Keep the `exporting` state and the `assessmentId` prop — those are still used.

3. Remove these CSS classes/attributes that were only for the old PDF spacer algorithm:
   - `report-container` class on the outer div (was only used as a ref target, not styled)
   - `report-marker-row` class on marker row divs (was used by spacer selector)
   - `report-category` class on category wrapper divs (was used by spacer selector)
   - `report-section` class on section wrapper divs (was used by spacer selector)
   - `data-pdf-break` attribute on the Strength Testing category div
   - `data-pdf-page-break="before"` attribute on the Insights section div
   - Note: Keep all other classes — they are used for on-screen styling.

   IMPORTANT: Only remove classes/attributes if they are exclusively used by the old PDF spacer algorithm. If a class is also used for CSS styling in the on-screen report, keep it. Check by searching for the class name in CSS/Tailwind usage — `report-container`, `report-marker-row`, `report-category`, `report-section` are not Tailwind classes and not referenced in any CSS file, so they are safe to remove.

4. Remove the `html2canvas-pro` and `jspdf` imports (the dynamic imports on lines ~148-150 are already inside the old exportPdf function, so they will be removed when the function is replaced).

5. Uninstall old dependencies:
   ```bash
   npm uninstall html2canvas-pro jspdf
   ```

6. Verify no other file imports html2canvas-pro or jspdf:
   ```bash
   grep -r "html2canvas" src/ --include="*.ts" --include="*.tsx"
   grep -r "jspdf" src/ --include="*.ts" --include="*.tsx"
   ```
   If any other imports exist, remove them too.
  </action>
  <verify>
    <automated>cd /Users/jace/Code/peak360 && grep -c "html2canvas" src/components/sections/Section11.tsx && echo "FAIL: html2canvas still referenced" || echo "OK: no html2canvas" && grep -c "jspdf" src/components/sections/Section11.tsx && echo "FAIL: jspdf still referenced" || echo "OK: no jspdf" && grep -c "spacer" src/components/sections/Section11.tsx && echo "FAIL: spacer code remains" || echo "OK: no spacers" && grep -c "fetch.*api/assessments.*pdf" src/components/sections/Section11.tsx | grep -q "1" && echo "OK: fetch call present" || echo "FAIL: no fetch call" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/sections/Section11.tsx contains `fetch(\`/api/assessments/${assessmentId}/pdf\`)`
    - src/components/sections/Section11.tsx does NOT contain "html2canvas" anywhere
    - src/components/sections/Section11.tsx does NOT contain "jspdf" or "jsPDF" anywhere
    - src/components/sections/Section11.tsx does NOT contain "spacer" (the spacer algorithm is removed)
    - src/components/sections/Section11.tsx does NOT contain "actionsRef" or "actionsTopRef" or "reportRef" as useRef declarations
    - package.json does NOT contain "html2canvas-pro" in dependencies
    - package.json does NOT contain "jspdf" in dependencies
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Export PDF button uses fetch-and-download to the new API route. All html2canvas+jsPDF code, spacer algorithms, and related refs/attributes removed. Old dependencies uninstalled. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify PDF report output</name>
  <files>none (visual verification only)</files>
  <action>
    Human verifies the complete PDF output by exporting from Section 11 and reviewing the downloaded file.
    Complete PDF generation migration from html2canvas+jsPDF to @react-pdf/renderer. The Export PDF button in Section 11 now fetches from /api/assessments/[id]/pdf which renders a native vector PDF server-side.

    Steps for verification:
    1. Start dev server: `npm run dev`
    2. Open an assessment with data: http://localhost:3000/assessment/{id}/section/11
    3. Click "Export PDF" button
    4. Open the downloaded PDF and verify:
       a. Navy header with client name, date, age, biological sex
       b. Medical disclaimer at top
       c. Daily readiness grid with values
       d. Medical screening section with yes/no dots
       e. Consent status bar
       f. 5 tier summary cards (Elite/Great/Normal/Cautious/Poor) with counts
       g. Detailed results grouped by category (Blood Tests subcategorized)
       h. Each marker row has: label, value + unit, tier pill, range bar (5-color segments with navy needle)
       i. Poor markers have "Refer to GP" flag, cautious have "Monitor" flag
       j. Insights and Recommendations on a new page
       k. Medical disclaimer at bottom
       l. Footer with "Generated by Peak360 Longevity Program" and page numbers on every page
    5. Verify text is selectable (not a raster image)
    6. Verify file size is reasonable (should be under 500KB vs 2-5MB for old approach)
    7. Check page breaks do not split marker rows mid-row
  </action>
  <verify>User confirms PDF visual quality by typing "approved"</verify>
  <done>User has approved the PDF report output as visually correct and functionally complete</done>
</task>

</tasks>

<verification>
1. `npm run build` passes
2. No references to html2canvas-pro or jspdf in src/ or package.json
3. Export PDF button downloads a valid PDF from the API route
4. PDF contains all report sections with proper formatting
5. Text in PDF is selectable (vector, not raster)
</verification>

<success_criteria>
- Export PDF in Section 11 uses the new @react-pdf/renderer pipeline via API route
- All old html2canvas+jsPDF code and dependencies removed
- PDF output contains all sections matching the on-screen report
- No page-break issues (marker rows stay together)
- File size under 500KB
- User confirms PDF visual quality
</success_criteria>

<output>
After completion, create `.planning/phases/05-migrate-pdf-generation-to-react-pdf-renderer/05-03-SUMMARY.md`
</output>
