---
phase: 06-routing-infrastructure-design-system
task: complete (plan 01), not started (plan 02)
total_tasks: 4
status: in_progress
last_updated: 2026-04-12T04:24:38.035Z
---

<current_state>
Plan 06-01 (routing + design system) is DONE and committed. Major routing architecture change happened mid-session: user pivoted from hostname-based routing to path-based routing.

New architecture:
- `peak360.com.au/` → landing page (public, no auth)
- `peak360.com.au/portal/*` → dashboard, assessments, clients, admin (auth-protected)
- `portal.peak360.com.au/*` → redirects to `peak360.com.au/portal/*`

Full landing page built from brochure content (PDF + HTML). Build passes. All routes tested in browser.

**UNCOMMITTED**: All routing restructure + landing page changes are staged but NOT committed yet. Must commit before any other work.
</current_state>

<completed_work>

- Plan 06-01 Task 1: Font definitions (Montserrat, Open Sans, Inter via next/font) + design tokens - Done (commit b60d02f)
- Plan 06-01 Task 2: Layout restructure - Done (commit 8307329, then reworked)
- Plan 06-01 Task 3: Middleware routing - Done (commit da857d6, then rewritten)
- EXTRA: Routing architecture pivot - path-based instead of hostname-based
- EXTRA: All portal routes moved to /portal/* (admin, assessment, assessments, clients)
- EXTRA: All internal links updated with /portal prefix (~20 files)
- EXTRA: Full landing page built from brochure content (hero, philosophy, testing, timeline, pricing, CTA)
</completed_work>

<remaining_work>

- Commit all uncommitted routing + landing page changes
- Review and refine landing page styling in browser
- Plan 06-02: DNS configuration (checkpoint — requires manual DigitalOcean setup)
- Deploy and test in production
</remaining_work>

<decisions_made>

- Switched from hostname-based to path-based routing per user request: one app, one domain
- Removed AppShell wrapper from root layout — portal layout now provides Sidebar directly
- Built full landing page content from brochures rather than placeholder
- Login page stays at /login (not /portal/login) — it's a public route
- API routes stay at /api/* — no /portal prefix needed
- localhost allows direct /landing access for dev (blocked in production)
</decisions_made>

<blockers>
- None
</blockers>

<context>
User wants the landing page to be the main deliverable. The routing restructure was a prerequisite. The landing page has all content sections from the brochures. Next step is to commit, then review in browser for styling tweaks. Plan 06-02 (DNS) is a human-action checkpoint that can be deferred.
</context>

<next_action>
Start with: `git add -A && git commit` for all the routing + landing page changes. Then open http://localhost:3000 in browser to review the landing page and make any styling refinements.
</next_action>
