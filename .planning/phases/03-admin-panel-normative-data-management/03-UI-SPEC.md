---
phase: 3
slug: admin-panel-normative-data-management
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-31
---

# Phase 3 — UI Design Contract

> Visual and interaction contract for Admin Panel & Normative Data Management. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none (custom components in `src/components/`) |
| Icon library | Heroicons (inline SVG, strokeWidth 1.8, 24x24 viewBox) |
| Font | Inter, system-ui, -apple-system, sans-serif |

Source: `globals.css` `@theme inline` block, existing Sidebar/Header components.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding, tight pill padding |
| sm | 8px | Compact element spacing, input internal padding |
| md | 16px | Default element spacing, card padding |
| lg | 24px | Section padding, card body padding |
| xl | 32px | Layout gaps between sections |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level vertical spacing |

Exceptions: none

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px (text-sm) | 400 (font-normal) | 1.5 |
| Label | 14px (text-sm) | 700 (font-bold) | 1.5 |
| Heading | 20px (text-xl) | 700 (font-bold) | 1.2 |
| Page title | 24px (text-2xl) | 700 (font-bold) | 1.2 |

Supplementary size: 12px (text-xs) is permitted for badge/pill text and helper captions only.

Declared sizes: 12px, 14px, 20px, 24px (4 total).
Declared weights: 400 (font-normal), 700 (font-bold) (2 total).

Source: Existing codebase uses `text-sm font-normal` for body, `text-sm font-bold` for labels requiring emphasis, `text-xl font-bold` for section headings, `text-2xl font-bold` for page-level headings. This phase follows the same scale.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#f8fafc` (background) / `#ffffff` (surface) | Page background, card backgrounds |
| Secondary (30%) | `#0f2440` (navy-dark) / `#1a365d` (navy) | Sidebar, nav, headings, labels |
| Accent (10%) | `#F5A623` (gold) | Save Range button, active sidebar item, focus rings, severity weight slider thumb |
| Destructive | `#ef4444` (rating-poor / red-500) | Delete marker override, reset to defaults confirmation |

Accent reserved for: primary CTA buttons ("Save Changes"), active nav indicator, input focus rings, severity slider thumb. Never used for passive text or decorative borders.

### Semantic Rating Colors (existing, reused in admin preview)

| Tier | Color | Hex |
|------|-------|-----|
| Elite | Emerald | `#10b981` |
| Great | Blue | `#3b82f6` |
| Normal | Gray | `#6b7280` |
| Cautious | Amber | `#f59e0b` |
| Poor | Red | `#ef4444` |

These are used in the inline tier preview when editing ranges. Each tier column header uses its corresponding color as a top border or background tint.

---

## Focal Point

Primary focal point: the category accordion list on the Normative Browser page, specifically the marker count badges and DB override status pills. These communicate at a glance which markers have been customized vs. remain hardcoded, guiding the admin toward markers that need attention.

Secondary focal point: the 5-column tier range grid on the Marker Range Editor page, where the colored tier headers draw the eye to the editing surface.

---

## Component Inventory

### Page: Admin Normative Browser (`/admin/normative`)

**Layout:** Existing sidebar (navy-dark, 224px / `w-56`) + main content area with `max-w-6xl mx-auto p-6` container. Sidebar gets a new "Normative Ranges" nav item under an "Admin" section divider.

**Category Accordion/Section List:**
- Each category (Blood Tests, Body Composition, Cardiovascular, Strength, Mobility & Flexibility, Balance & Power) renders as a collapsible section
- Section header: `text-xl font-bold text-navy`, bottom border `border-b border-border pb-2` (matches existing `TestCategory` pattern)
- Marker count badge next to category name: `text-xs text-muted` showing "12 markers" or "4 with DB overrides"

**Marker Row (within category):**
- Clickable row: `flex justify-between items-center p-3 bg-surface border border-border rounded-lg hover:border-gold transition-colors`
- Left: marker label (`text-sm font-bold text-navy`)
- Right: status indicator:
  - "DB override" pill: `text-xs px-2 py-1 rounded-full bg-gold/10 text-gold font-bold`
  - "Hardcoded" pill: `text-xs px-2 py-1 rounded-full bg-surface-alt text-muted font-bold`
  - "No ranges" pill: `text-xs px-2 py-1 rounded-full bg-red-50 text-red-500 font-bold` (for `hasNorms: false`)
- Clicking navigates to `/admin/normative/[marker]`

**Search/Filter Bar:**
- Sticky top bar below page title: text input with placeholder "Search markers..."
- Filter dropdown: "All" / "DB overrides only" / "Hardcoded only"
- Styled same as existing form inputs: `px-3 py-2 border border-border rounded-lg text-sm`

### Page: Marker Range Editor (`/admin/normative/[marker]`)

**Layout:** Same sidebar + main content. Back link at top: left arrow icon + "Back to Normative Ranges" in `text-sm text-muted hover:text-navy`.

**Marker Header:**
- Marker name: `text-2xl font-bold text-navy`
- Category breadcrumb: `text-sm text-muted` (e.g., "Blood Tests > Lipid Panel")
- Unit badge: `text-xs px-2 py-1 rounded bg-surface-alt text-muted font-bold` (e.g., "mmol/L")

**Variant Tabs (if gendered/age-bucketed):**
- For unisex markers: no tabs, show single tier form
- For gendered markers: horizontal tab bar with "All" / "Male" / "Female" tabs
- For gendered + age-bucketed: two-level selector: gender tabs + age group dropdown
- Tab styling: `text-sm font-bold px-4 py-2 rounded-t-lg`, active tab uses `border-b-2 border-gold text-navy`, inactive uses `text-muted hover:text-navy`

**Tier Range Form (one per variant):**
- 5-column grid (one per tier): Poor | Cautious | Normal | Great | Elite
- Each column header: tier label in `text-xs font-bold uppercase tracking-wide` with tier color as top border (4px solid)
- Each column contains two inputs: "Min" and "Max"
- Input styling: `w-full px-2 py-2 border border-border rounded text-sm text-center tabular-nums` with `focus:border-gold focus:ring-2 focus:ring-gold/25`
- Validation: min must be less than max; no gaps between tiers (max of poor must equal min of cautious). Validation errors shown as red border + tooltip.

**Severity Weight:**
- Below tier form: "Red Flag Severity" section
- Horizontal slider (0-10 scale) using existing slider pattern from `SliderField`
- Labels at each end: "Low (0)" and "Critical (10)"
- Current value displayed as `text-xl font-bold text-navy tabular-nums` next to slider
- Helper text: `text-xs text-muted` -- "Controls visual prominence of referral flags when this marker is poor or cautious"

**Action Bar:**
- Sticky bottom bar or fixed at section bottom: `flex justify-between items-center p-4 border-t border-border bg-surface`
- Left: "Reset to Defaults" button (ghost/outline style, destructive variant)
- Right: "Save Changes" button (gold CTA: `bg-gold text-white font-bold px-6 py-2.5 rounded-lg hover:bg-gold-dark`)
- Disabled state when no changes: `opacity-50 cursor-not-allowed`

### Page: Version History (`/admin/normative/versions`)

**Version List:**
- Table or list of version snapshots: ID (truncated), created date, marker count, number of assessments using this version
- Each row: `text-sm`, alternating row backgrounds (`bg-surface` / `bg-surface-alt`)
- "Current" badge on the active version: `text-xs px-2 py-1 rounded-full bg-emerald-50 text-emerald-600 font-bold`

---

## Interaction Patterns

### Save Flow
1. Admin edits tier values in the range form
2. "Save Changes" button enables (transitions from disabled to gold CTA)
3. On click: button shows "Saving..." with gold text (matches existing save indicator pattern)
4. On success: brief green checkmark flash, button returns to disabled state
5. On error: red error banner below action bar with message + "Try again" link

### Inline Validation
- As admin types min/max values, validate in real-time:
  - Red border on input if min >= max within a tier
  - Red border if gap exists between adjacent tiers (e.g., poor max=3.0 but cautious min=3.5 leaves a gap)
  - Validation message appears below the tier grid: `text-xs text-red-500`

### Optimistic Locking
- When loading a marker for edit, store the `updatedAt` timestamp
- On save, include the timestamp; if it differs (another admin edited), show conflict warning: "This marker was updated by another admin. Reload to see their changes before saving."

### Navigation Guard
- If form has unsaved changes and admin navigates away, show browser confirmation dialog (same `beforeunload` pattern used elsewhere)

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | "Save Changes" |
| Secondary CTA | "Reset to Defaults" |
| Empty state heading | "No DB Overrides Yet" |
| Empty state body | "All markers are using hardcoded defaults. Select a marker to customize its ranges." |
| Error state (save failure) | "Failed to save range changes. Check your connection and try again." |
| Error state (load failure) | "Could not load normative data. Refresh the page to try again." |
| Validation: min >= max | "Min must be less than max" |
| Validation: tier gap | "Gap between tiers: {prev_tier} max ({value}) must equal {next_tier} min ({value})" |
| Destructive: reset | Reset to Defaults: "This will remove all custom ranges for {marker_name} and revert to hardcoded values. This cannot be undone." |
| Destructive: confirmation button | "Yes, Reset to Defaults" |
| Destructive: cancel button | "Keep Current Ranges" |
| Version snapshot info | "Ranges are versioned automatically. Existing assessments keep the ranges they were created with." |
| Severity helper | "Controls how prominently this marker's referral flag appears in reports (0 = subtle, 10 = critical)" |
| Search placeholder | "Search markers..." |
| Filter: all | "All Markers" |
| Filter: db overrides | "DB Overrides Only" |
| Filter: hardcoded | "Hardcoded Only" |
| Breadcrumb back | "Back to Normative Ranges" |

---

## States Matrix

| Component | Default | Loading | Empty | Error | Success | Dirty |
|-----------|---------|---------|-------|-------|---------|-------|
| Category list | Shows all categories expanded | Skeleton: 6 category blocks with 4 shimmer rows each | "No DB Overrides Yet" card | "Could not load normative data" banner | n/a | n/a |
| Marker row | Shows label + status pill | Shimmer placeholder | n/a (always has markers) | n/a | n/a | n/a |
| Tier form | Populated from DB or hardcoded | Spinner in form area | Pre-filled with hardcoded values (for new override) | Red banner above form | Brief green flash on save button | Gold dot indicator next to page title |
| Save button | Disabled (`opacity-50`) | "Saving..." text | n/a | Enabled, red error shown separately | Green check flash, returns to disabled | Enabled with gold background |
| Severity slider | Shows current value (default 0) | n/a | Shows 0 | n/a | n/a | Included in dirty tracking |

---

## Responsive Behavior

| Breakpoint | Layout Change |
|------------|---------------|
| >= 1024px (lg) | Sidebar visible (fixed 224px), main content fills remaining width |
| < 1024px | Sidebar hidden, hamburger menu (existing pattern). Main content full width with `px-4` |
| Tier form >= 768px (md) | 5-column grid for all tiers side by side |
| Tier form < 768px | Stack tiers vertically, each tier as a card with colored left border |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| No registries | n/a | not applicable |

No shadcn or third-party registries are used. All components are custom-built following existing project patterns.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
